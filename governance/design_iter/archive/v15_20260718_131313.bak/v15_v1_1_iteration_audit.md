# AIDocxWorkFlow v1.1 迭代方案审核（测试架构师视角）

> **来源材料**：`/Users/gleon/Downloads/AIDocxWorkFlow v1.1 迭代方案.md`
> **审核视角**：测试架构师
> **审核日期**：2026-07-17
> **当前治理基线**：v15 §5 三阶段全部完成（详见 `governance/design_iter/INDEX.md` + `v15/PLAN.md`）
> **落档依据**：DNA_3Q_CHECK.mdc §9.5（决策/计划产生前先 Write 占位）

---

## §1 总评（先看结论）

### 1.1 方案诊断方向 ✓ 正确

v1.0 → v1.1 的 6 大问题诊断方向**与 v15 治理下观察到的实战痛点高度吻合**：

| v1.1 方案识别问题 | v15 实战已暴露对应症状 | 印证 |
|---|---|---|
| 问题一：S1→S2 信息传递断层 | v15 §5 阶段 1 任务④ "无 S7 review_report 可消费"——S1 评审洞察未结构化、S2 必须重新理解 | ✅ |
| 问题二-A：归类优先级未定义 | v15 §附录 A.2 S7-lite 模块归属校验职责被收窄到 LLM；MODULES.md §3.5 仅"快速归类表"，无优先级矩阵 | ✅ |
| 问题三：质量门禁依赖 LLM 自评 | S7 SKILL.md §118-122 RCA 字段全部由 LLM 自填、S5 SKILL.md §1.5 反例对照"靠 LLM 主动执行" | ✅ |
| 问题四：覆盖率定义过浅 | test_points.json 中 `scenario_test_points[].s4_reference` 字段仅"被引用即覆盖"，缺乏深度分级 | ✅ |
| 问题五：双审查员虚假独立 | S7 SKILL.md §131-156 仍写"两个 LLM 审查员 A/B"，但事实上是同模型同上下文 | ✅ |
| 问题六：S8 只出建议不闭环 | STAGE_S8_SELF_ITERATION.mdc §150-168 归档只入"待审候选区"，需人工再审核 | ✅ |

### 1.2 整体可行性判断

| 维度 | 评级 | 关键判断 |
|---|---|---|
| **节奏** | ⚠️ 过于乐观 | v15 阶段 1 任务④已 BLOCKED（无 S7 真实数据，见 `v15_L3_blocker.md`）；v1.1 P0 项 1/3 直接撞此障碍 |
| **方案完备性** | ⚠️ 4/6 缺落地点 | 问题三"三层门禁体系"、问题四"三级覆盖率"、问题六"自动分流"仅给方向、缺 schema/字段定义 |
| **与 v15 兼容性** | ⚠️ 冲突点 ≥ 4 处 | v1.1 方案**未读 v15 产物**（PLAN.md / A1 / A2 / A3）——优先项 #3 "related_tags 双轨统计" 与 v15 §5 阶段 1 L3 缺陷聚类维度重叠、#4 "L1 格式硬校验" 与 s5_exit_precheck.py 高度重叠、#5 "三级覆盖率" 与 STAGE_S5 §FP 覆盖率门禁冲突 |
| **可执行性** | ✗ 数据不足 | v1.1 验收指标 "S2 OBJ 边界一致性 ≥ 90%" / "模块归类一致性 ≥ 90%" 均需 ≥ 3 个真实项目数据，而 `workflow_assets/` 只有 2 个空目录 |
| **与现有 10 项门禁兼容** | ⚠️ 隐含删改 | 问题三"废除三步自问的 Q3"——这是 v14 §A.2 已经拍板的决策，v1.1 直接推翻需走 v15/v16 治理流程 |

### 1.3 落地优先级建议

| 建议 | 项数 | 理由 |
|---|---|---|
| 🟢 立即推进 | 3 项 | 高收益低难度、无 BLOCKED 依赖、增量改动 |
| 🟡 需补充设计 | 2 项 | 方向对但缺落地点（schema / 字段定义） |
| 🔴 推迟到 v16 | 4 项 | 受 v15 L3 BLOCKED 阻塞、需真实数据 |
| ⚪ 架构层 / 长期 | 1 项 | v1.3 范畴，需独立治理周期 |

---

## §2 逐条审核（10 个修改点）

### #1 S1 结构化评审包 + S2 直接消费（问题一，P0，难度中）

#### 落地物要求

- **新增**：S1 结构化评审包 Schema `review_structured.json`（含 requirement_quality / confirmed_boundaries / explicit_assumptions / risk_points_preview / missing_scenarios / final_requirement_text 6 字段）
- **修改**：S1 SKILL 输出增加结构化字段
- **修改**：S2 SKILL 输入增加结构化评审包消费逻辑

#### 测试架构师审核

| 检查项 | 评估 | 备注 |
|---|---|---|
| **字段是否完整覆盖评审洞察** | ✓ 6 字段涵盖评分 / 边界 / 假设 / 风险 / 缺口 / 终版 | 缺"模块归属建议"——S1 已识别模块标签，应直接给 S2 |
| **S2 消费规则是否可行** | ⚠️ 部分可执行 | `explicit_assumptions` 自动注入 Story 前置条件可实现；`risk_points_preview` "作为 S4 异常树种子输入"在 v15 §A.1 S3 增强路径触发决策树里没有对接位 |
| **是否与 S1.5 准出许可冲突** | ✗ 冲突 | S1.5 已有 `exit_permission.json` 包含 quality_level / fallback_rules / s2_guidance 三个消费字段——S1 再加结构化评审包 = S1 与 S1.5 职责重复 |
| **是否与 v15 治理冲突** | ⚠️ 需协调 | v15 §5 阶段 1 L3 缺陷模式聚类已消费 S7 RCA，**S1 结构化评审包**会让 L3 聚类也消费 S1——多源数据需明确归口 |
| **落地工作量** | 中（1-2 周） | S1 SKILL 加 6 字段 + S2 SKILL 加消费逻辑 + review_structured.json schema |

#### 验收建议调整

| 原 v1.1 指标 | 调整建议 | 理由 |
|---|---|---|
| "S2 OBJ 边界一致性 ≥ 90%" | 改为 "S2 OBJ 边界定义与 S1 confirmed_boundaries 字符串相等率 ≥ 90%" | 锚定可机检字段，避免 LLM 主观打分 |

#### 推进路径

🟡 **需补充设计**——补 1 项后才能 P0 推进：
1. **新增** `s1_review_structured.json` Schema（v1.1 已给）→ 写入 `.cursor/rules/STAGE_S1_REVIEW.mdc` §1.5.4
2. **明确** S1.5 与 S1 结构化评审包职责边界——避免重复
3. **改造** S2 SKILL 增加"消费 S1 结构化包"分支，与现有 exit_permission.json 消费并存

---

### #2 模块优先级矩阵补充（问题二-A，P0，难度低）

#### 落地物要求

- 在 `.cursor/MODULES.md` 中增加**冲突优先级表**（5 行场景）
- 边界判定决策树末尾增加"多模块命中时查优先级表"出口

#### 测试架构师审核

| 检查项 | 评估 | 备注 |
|---|---|---|
| **优先级规则是否合理** | ✓ 高度合理 | SPECIAL > BIZ/UI、LOG 主目标 > 业务附带、HINT 测样式 vs 测内容——符合 MODULES.md §4 各模块职责划分 |
| **冲突表是否完备** | ⚠️ 缺关键冲突 | 5 行只覆盖 5 类冲突；缺：BIZ vs CONFIG（业务规则 vs 配置表字段——高发区）、LOG vs BIZ-AUDIT_LOG（v1.9 严格隔离边界）、UI vs HINT（v1.7+ 严格隔离） |
| **与 MODULES.md §3.5 交叉判定规则冲突** | ✗ 重复 | §3.5 已有"新元素 → ①是玩家可见界面元素？→ ②常驻 vs 临时？→ ..."决策树，v1.1 优先级表本质上是 §3.5 的"补充说明"，应并入 §3.5 而非独立段落 |
| **落地工作量** | 低（1-2 天） | 改 1 个 .mdc 文件 |

#### 推进路径

🟢 **立即推进**（条件：补 3 行冲突 + 整合进 §3.5 而非独立表）：
1. **Read** 当前 `MODULES.md` §3.5 确认决策树结构
2. **改写** §3.5 末尾追加"冲突优先级矩阵"段（5+3 行 = 8 行场景规则）
3. **同步** STAGE_S5_TEST_POINTS.mdc §1.5.2 "4 步判定 push" 末尾加"Step 2.5：命中冲突查优先级矩阵"
4. **同步** STAGE_S4_FLOWCHART.mdc §1.5.2 "异常树叶子归属判定" 末尾加同样出口

---

### #3 related_tags + 双轨覆盖率统计（问题二-B，P0，难度中）

#### 落地物要求

- TP 增加 `primary_module` + `related_tags` + `verification_points` 3 字段
- 覆盖率分两层统计：主模块覆盖率（按 primary_module）+ 维度覆盖率（按 related_tags）

#### 测试架构师审核

| 检查项 | 评估 | 备注 |
|---|---|---|
| **字段设计是否合理** | ⚠️ 与现有 `module` 字段重复 | TP JSON 现有 `module` 字段（STAGE_S5_TEST_POINTS.mdc §§1.6）= primary_module 语义；新增 `primary_module` 字段等于重复 |
| **双轨统计能否复用 v15 已有脚本** | ✗ 与 v15 §5 阶段 1 L3 缺陷模式聚类（defect_cluster.py）冲突 | L3 已按 `module × rca.type × rca.clause` 聚类；v1.1 再加 `module × related_tags` 双轨 = 两个统计维度并行维护、数据契约不一致 |
| **是否能复用 v15 §A.3 ASCII 柱图** | ✓ 部分复用 | ASCII 柱图按 module 维度展示，加 related_tags 后变成堆叠柱图——可增量扩展，不重写 |
| **覆盖率数字含义** | ✓ 解决"LOG 模块覆盖率 30% 但实际业务都验证过"问题 | 但需明确：双轨数字不一致时以哪个为准？v1.1 未给答案 |
| **落地工作量** | 中（1 周） | TP 字段扩展 + 脚本升级 + S5/S7 双轨统计 |

#### 推进路径

🟡 **需补充设计**：
1. **明确字段命名**：用 `primary_module`（替代 module）or 保留 `module` + 新增 `related_tags`？——前者破坏现有 `test_points.json`（1748 行已用 module），后者 `primary_module` 重复——建议**保留 `module`**，加 `related_tags`，含义"主模块"
2. **明确双轨不一致时的优先级**——建议"主模块 + related_tags 同时达到 L1 → 该 TP 计入两个模块覆盖率"
3. **明确与 defect_cluster.py 关系**——v15 L3 按 RCA module 聚类，v1.1 按 TP module 聚类，**两者维度不同**，建议保留各自数据契约

---

### #4 L1 格式硬校验门禁（问题三，P0，难度低）

#### 落地物要求

- 新增 L1 格式校验脚本（各阶段分别配置校验规则）
- ID 命名规范 / 必填字段完整性 / 引用 ID 存在性 / JSON 格式合法性

#### 测试架构师审核

| 检查项 | 评估 | 备注 |
|---|---|---|
| **是否与 v15 已有脚本重叠** | ✗ 高度重叠 | `ai_workflow/s5_exit_precheck.py`（v14 §5 第二阶段第一项，2026-07-14 落地）已实现：ID 规范 / 必填字段 / 引用完整性 / JSON 合法性——v1.1 L1 与之**几乎完全重合** |
| **是否需要新增各阶段脚本** | ✓ 必要 | s5_exit_precheck.py 仅覆盖 S5；S1/S2/S3/S4/S6/S7 缺对应脚本 |
| **是否改"三步自问"为"两步自问"** | ✗ 风险大 | v1.1 §4.3 提议"废除 Q3 + 红警阈值 40% → 20%"——这是 v14 §A.2 已经拍板的决策；v1.1 直接推翻**未走治理流程** |
| **v15 §A.3 dashboard 计划与 L1 关系** | 需明确 | A3 提案 §3 推荐"v15 末 1 周 POC ASCII 柱图"——v1.1 L1 不产图表，**两者定位不同**，可并存 |

#### 落地物建议

| 现状文件 | v1.1 应改造方向 |
|---|---|
| `ai_workflow/s5_exit_precheck.py`（S5 only） | 复用 + 扩展为通用 L1 校验器 |
| `scripts/check_field_completion.py`（多阶段字段填写率） | 复用 + 集成进 L1 |
| 缺：S1 / S2 / S3 / S4 / S6 / S7 / S8 L1 校验脚本 | 新建 7 个（每阶段 1 个）+ 通用壳 |

#### 推进路径

🟢 **立即推进**（高优先级）：
1. **复用** `s5_exit_precheck.py` 的 `run_precheck` 函数，抽出 `ai_workflow/l1_format_validator.py`（通用基类）
2. **新增** S1/S2/S3/S4/S6/S7/S8 L1 校验规则（参考各阶段 STAGE_*.mdc "输入审查" 节）
3. **明确 L1/L2/L3 分层边界**——v1.1 §4.3 三层门禁体系合理，**L1 = 脚本硬校验 = 本次推进**；L2/L3 = v1.2 推迟
4. **不改三步自问**——v1.1 §4.3 提议删除 Q3 与 v14 §A.2 拍板冲突；保留 Q3，仅收紧红警阈值至 20%（与 v1.1 提议一致，**走治理流程拍板**）

---

### #5 覆盖率三级深度定义（问题四，P1，难度中）

#### 落地物要求

- 覆盖率账本 Schema 增加深度等级字段（L1 引用级 / L2 属性级 / L3 全面级）
- S5 SKILL 增加深度覆盖判断逻辑
- S7 审查报告输出三级覆盖率明细
- S1.5 需求澄清时增加功能优先级标注

#### 测试架构师审核

| 检查项 | 评估 | 备注 |
|---|---|---|
| **三级阈值是否合理** | ⚠️ 缺落地定义 | "L2 属性级 = 核心属性 + 关键状态转换"——"核心属性"和"关键状态"如何机检？v1.1 未给量化方法 |
| **与现有 S5 §FP 覆盖率门禁冲突** | ✗ 冲突 | STAGE_S5_TEST_POINTS.mdc §FP 必填已要求 `fp_linkage_coverage ≥ 1.0`（100%），v1.1 三级覆盖率要求 P0 L2 ≥ 90%、L3 ≥ 60%——同一指标两套口径 |
| **与 S5 §1.5.6 四象限展开关系** | 需协调 | 四象限（Q1 ≥ 3 类 / Q2 ≥ 1 类 / Q3 ≥ 1 类 / Q4 ≥ 1 类）已要求场景族广度——三级覆盖率再加属性深度 = 双维度展开 |
| **S1.5 加"功能优先级标注"职责** | ✗ 越权 | S1.5 当前只负责"完善终版需求 + 生成 exit_permission"——功能优先级是 S2 拆解时的产物，不应在 S1.5 标注 |
| **落地工作量** | 中-高（2 周） | 涉及 S5/S7 schema + S5 SKILL 改造 + S1.5 改造 |

#### 推进路径

🔴 **推迟到 v16**（理由：BLOCKED + 缺落地点）：
1. **v15 阶段 1 任务④ BLOCKED**（无 S7 真实数据）→ 三级覆盖率阈值无法在真实数据上验证
2. **缺 L2 属性级机检方法**——需先定义"核心属性"判定规则（基于 OBJ.feature_points 类型？基于 epic.priority？）
3. **与 S5 §FP 覆盖率门禁冲突**——需先统一指标口径
4. **建议在 v16 阶段 1 推进**——届时有 ≥ 1 个真实项目数据可校准阈值

---

### #6 L2/L3 门禁 + 黄金用例基准（问题三，P1，难度中）

#### 落地物要求

- L2 覆盖率交叉校验（脚本自动比对）
- L3 质量基准对比（与黄金用例相似度比对）

#### 测试架构师审核

| 检查项 | 评估 | 备注 |
|---|---|---|
| **黄金用例种子库来源** | ✗ 不明 | "初期人工录入 20-30 条标杆用例"——按哪个项目录入？游戏道具商城？通用模板？需明确 |
| **相似度比对算法** | ⚠️ 缺 | "与人工审核的种子用例库做语义相似度比对"——用 embedding？余弦相似度阈值多少？v1.1 未给 |
| **与 v15 §A.2 用例价值评分关系** | ⚠️ 重叠 | A2 提议 effectiveness × 0.5 + executability × 0.3 + independence × 0.2 三维评分——v1.1 L3 又来一个"黄金用例相似度"，两个评分体系如何并存？ |
| **落地工作量** | 中（1-2 周） | 黄金用例录入 + embedding 集成 + 相似度脚本 |

#### 推进路径

🔴 **推迟到 v16**（理由：依赖真实 TC 数据）：
1. v1.1 v3.0 提议"v1.2 实施"——已与 v15 §6 L4 推迟 v16 规划一致
2. **黄金用例库必须基于真实项目数据录入**——v15 阶段 1 任务④ BLOCKED 即指无 S7 数据，自然也无 S6 TC 数据
3. **建议合并 v15 §A.2 用例价值评分 + v1.1 L3 黄金用例基准**——v16 阶段 1 统一设计"用例质量评分体系"，避免两套

---

### #7 S7 三层审查架构重构（问题五，P1，难度中）

#### 落地物要求

- L1 格式校验（脚本）+ L2 引用完整性（脚本 + LLM）+ L3 质量审查（LLM + 黄金用例对比）
- 替代现有 S7 双审查员（A 结构完整性 + B 覆盖率）

#### 测试架构师审核

| 检查项 | 评估 | 备注 |
|---|---|---|
| **与 S7 SKILL.md 当前定义关系** | ⚠️ 需重构 | S7 SKILL.md §131-156 "审查员 A/B" 仍定义中；v1.1 三层（L1/L2/L3）= L1/L2 脚本化 + L3 LLM——A 的"字段名合规/模块归类/步骤质量"半结构化任务归 L3，B 的"s4_reference / is_assumed / applies_rule"统计归 L2 |
| **L1 格式校验已有 s5_exit_precheck.py 雏形** | ✓ 可复用 | 见 #4 审核——但 S7 L1 与 S5 L1 校验对象不同（S5 = test_points.json / S7 = test_cases.json） |
| **L3 黄金用例对比依赖 #6** | ✗ 阻塞 | 与 #6 共享依赖 |
| **落地工作量** | 中（1 周） | S7 SKILL 重构 + L1/L2 脚本 + L3 LLM prompt 改造 |

#### 推进路径

🟡 **需补充设计 + 分阶段推进**：
1. **v1.1 中推进**：L1（脚本硬校验）+ L2（脚本统计）部分——不依赖黄金用例
2. **v1.1 中推迟**：L3 黄金用例对比部分——依赖 #6 黄金库建立
3. **S7 SKILL.md §131-156 重写**：审查员 A/B 语义重定义为 L1/L2/L3 职责分工

---

### #8 SPECIAL 变体用例机制（问题二-C，P2，难度高）

#### 落地物要求

- SPECIAL 用例采用"基例引用 + 差异点"结构（不写完整步骤）
- `base_case_ref` / `variant_type` / `variant_conditions` / `step_deltas` / `expected_deltas` 字段

#### 测试架构师审核

| 检查项 | 评估 | 备注 |
|---|---|---|
| **设计动机是否真实** | ✓ 真实痛点 | 弱网购买 / 并发购买等场景从 BIZ 抽离到 SPECIAL 后 70% 步骤重复——v1.1 解决合理 |
| **变体用例如何被 QA 执行** | ⚠️ 不明 | 实际 QA 拿到"基例 + 差异点"需要先理解基例才能跑——人工认知成本高、自动化框架如何解析 step_deltas（`insert_after` / `replace` / `append` 操作语义）？ |
| **与 v15 §A.1 S3 增强路径 S3-lightweight 关系** | 需协调 | S3-lightweight 本质是"对 S3 产出变体"；v1.1 SPECIAL 变体是"对 TC 产出变体"——两者应在 v16 统一"变体化能力"框架 |
| **维护成本是否真降低** | ⚠️ 取决于实现质量 | O(n) → O(1) + 差异点维护 前提：基例与变体的 diff 算法稳定；当前 LLM 推理差异点质量未知 |
| **落地工作量** | 高（2-3 周） | TC Schema 改造 + 变体解析器 + 自动化框架集成 |

#### 推进路径

🔴 **推迟到 v16**（理由：架构层 / 长期）：
1. v1.1 §8.2 已标 v1.2 实施——与 v15 §附录 D L7/L8 推迟 v16 一致
2. **变体用例需要真实 TC 数据验证**——v15 阶段 1 任务④ BLOCKED
3. **建议 v16 阶段 2 推进**——届时有 ≥ 1 个真实项目 TC 数据 + 自动化框架 baseline

---

### #9 S8 根因分流 + 自动闭环（问题六，P2，难度高）

#### 落地物要求

- 根因分类命名规范化（`{阶段}_{问题类型}` 格式）
- 根因自动分流处理（5 种类型 → 5 种动作）
- 全局经验库（反例库 / 命名库 / 执行经验）
- 月度缺陷聚合分析脚本

#### 测试架构师审核

| 检查项 | 评估 | 备注 |
|---|---|---|
| **新命名规范与 v14 RCA 映射表冲突** | ⚠️ 需协调 | STAGE_S8_SELF_ITERATION.mdc §142-148 "v14 RCA 三级字段（stage/type/clause）"——`rca.type` 已枚举 7 类（OMISSION/BOUNDARY_ERR/QUALITY_LOW/FIELD_MISSING/LINKAGE_BROKEN/RULE_VIOLATION/ID_NONCOMPLIANT）；v1.1 新增 5 类（RULE_MISSING / MODULE_MISCLASSIFY / EXEC_OMISSION / NAMING_MISLEAD / COVERAGE_GAP）——**两套 RCA 体系冲突** |
| **RULE_MISSING 含义是否已被覆盖** | ✓ 已覆盖 | v14 §A.2 RCA type = RULE_VIOLATION 已涵盖"规则违反"；v1.1 RULE_MISSING = "规则缺失"是 RULE_VIOLATION 的子集——应并入而非新增 |
| **NAMING_MISLEAD 已存在** | ✓ 已存在 | STAGE_S5 §1.5.4 反例库对照已处理"命名误导"——v1.1 单独提出是 S5/S7/S8 协同问题，不是新根因类型 |
| **自动闭环能否实现** | ⚠️ 流程可行但执行困难 | "自动加入反例库 → 下次执行作为 few-shot"——需要 LLM 注入 prompt 机制，且反例库需人工审核（MODULES §11 单写源规则） |
| **月度聚合脚本与 v15 §A.3 dashboard 关系** | ⚠️ 重叠 | A3 §2 方案 A "CLI 命令行输出 + ASCII 柱图"——v1.1 "月度缺陷聚合分析脚本"本质上就是 A3 的另一种实现方式，应合并 |

#### 推进路径

🔴 **推迟到 v16**（理由：长期能力 + 依赖 v15 §5 阶段 1 L3 数据）：
1. v1.1 §8.3 已标 v1.3 实施
2. **新 RCA 命名与 v14 已拍板冲突**——若要推进，必须走 v16 PLAN.md 重新拍板（不可 v1.1 直接覆盖）
3. **建议合并到 v15 §A.3 dashboard**——v16 阶段 1 统一"缺陷可视化 + 经验反哺"两能力

---

### #10 全局经验库 + 跨项目聚合（问题六，P2，难度高）

#### 落地物要求

- 全局经验库（反例库 / 命名库 / 执行经验）
- 月度缺陷聚合分析脚本
- 各阶段 SKILL 增加经验库注入逻辑

#### 测试架构师审核

| 检查项 | 评估 | 备注 |
|---|---|---|
| **全局经验库与现有 knowledge/public/ 关系** | ⚠️ 需明确 | `.cursor/rules/DESIGN_AND_EXECUTION_STANDARDS.mdc` §0.1 "knowledge/public/module_templates/ 属于核心公共知识库，Agent 不得直接写正式内容；只能写入候选区或候选文件，必须经人工审核后才能入公共库"——v1.1 全局经验库按此规则必须先入候选区 |
| **与 v15 §A.3 dashboard 关系** | ⚠️ 重叠 | 同 #9，应合并 |
| **经验库注入 prompt 机制** | ⚠️ 复杂度高 | "各阶段 SKILL 增加经验库注入逻辑"——意味着每次 S5/S6/S7 LLM 推理都要先读经验库文件，token 成本上升；需设计"经验库按需检索"而非全量注入 |
| **跨项目聚合 = v15 §A.3 trend** | ✓ 完全重叠 | defect_cluster.py 已实现"≥ 3 项目 bypass_log 触发 trend_available"——v1.1 "月度缺陷聚合"是 A3 trend 的另一种表述 |

#### 推进路径

🔴 **推迟到 v16**（理由：长期 + 与 v15 §A.3 完全重叠）：
1. **v16 阶段 1 拍板时统一规划**："经验库 + dashboard + 跨项目聚合"作为一个能力群（避免分散实现）
2. **v1.1 中仅做骨架**——`knowledge/project_local/.experience/` 目录约定 + 候选区规则

---

## §3 冲突点清单（v1.1 vs 现有 v15 + STAGE_*.mdc）

| 序号 | v1.1 提案 | 现状（v15 / STAGE_*.mdc） | 冲突类型 | 处理建议 |
|---|---|---|---|---|
| C1 | "S1 新增 review_structured.json" | S1.5 已有 exit_permission.json 含 5 个消费字段 | 职责重叠 | 合并——S1 评审产出与 S1.5 准出许可是同一链路 |
| C2 | "S1.5 增加功能优先级标注" | S2 §第二层 Epic `risk_level` 已含 P0/P1/P2 | 越权 | 不在 S1.5 改——S2 自行标注 |
| C3 | "TP 加 primary_module + related_tags" | S5 §1.6 已有 `module` 字段 | 字段重复 | 保留 `module`、新增 `related_tags`（不要 primary_module） |
| C4 | "L1 格式校验脚本" | `s5_exit_precheck.py`（2026-07-14 落地）已实现 S5 L1 | 高度重叠 | 复用 + 扩展至其他阶段 |
| C5 | "废除三步自问 Q3 + 红警 40% → 20%" | v14 §A.2 拍板 Q1+Q2+Q3 必须；SSOT §2.4.1 | 推翻拍板 | 仅"红警阈值 20%"可走 v15/v16 治理流程；删 Q3 需重新拍板 |
| C6 | "三级覆盖率 L1/L2/L3" | S5 §FP 覆盖率门禁 `≥ 1.0`（100%） | 口径冲突 | 推迟 v16 统一——先定义"属性级机检方法" |
| C7 | "L3 黄金用例相似度比对" | v15 §A.2 用例价值 3 维评分 | 体系重叠 | 合并到 v16"用例质量评分体系" |
| C8 | "S8 根因分类 5 新类型（RULE_MISSING 等）" | v14 §A.2 RCA type 7 类枚举 | 命名冲突 | 不在 v1.1 改 v14 已拍板命名 |
| C9 | "全局经验库" | MODULES §11.1 已规定"knowledge/public/ 入库须人工审核" | 规则已知 | 遵守——经验库先入候选区 |
| C10 | "SPECIAL 变体用例机制" | v15 §A.1 S3-lightweight 是另一种"变体化" | 框架重叠 | 合并到 v16"变体化能力"框架 |

---

## §4 落地路线图（重新规划）

### 4.1 本周可推进（🟢 3 项）

| 项 | 工作量 | 文件改动 | 风险 |
|---|---|---|---|
| **#2 模块优先级矩阵** | 1-2 天 | `MODULES.md` §3.5 末尾追加 8 行场景规则（v1.1 5 行 + 3 行补充）+ STAGE_S5/S4 同步 | 低 |
| **#4 L1 格式校验脚本** | 1 周 | 抽 `ai_workflow/l1_format_validator.py` 通用基类 + 7 个阶段校验规则（复用 `s5_exit_precheck.py` 函数） | 低 |
| **C5 子项：红警阈值收紧至 20%** | 1 天 | `DESIGN_AND_EXECUTION_STANDARDS.mdc` §2.4.2 `BYPASS_EXCEPTION_RATE_WARNING = 0.20`（已为 0.20，确认无改动）→ 0 风险 | 无 |

### 4.2 需补充设计后推进（🟡 3 项）

| 项 | 补什么 | 推进条件 |
|---|---|---|
| **#1 S1 结构化评审包** | 明确 S1 vs S1.5 职责边界 + review_structured.json 写入 STAGE_S1 §1.5.4 | 1 次小拍板（治理层） |
| **#3 related_tags + 双轨** | 字段命名方案（保留 module + 加 related_tags）+ 双轨不一致时的优先级 | 1 次小拍板 |
| **#7 S7 三层审查（仅 L1/L2）** | S7 SKILL.md §131-156 重写为 L1 脚本 + L2 脚本 + L3 LLM 三层 | 设计文档评审 |

### 4.3 推迟 v16（🔴 4 项）

| 项 | 推迟理由 | 何时推进 |
|---|---|---|
| **#5 三级覆盖率** | BLOCKED + 与 S5 §FP 门禁冲突 | v16 阶段 1（有真实数据后） |
| **#6 L2/L3 门禁 + 黄金用例** | 缺黄金库数据 + 与 v15 §A.2 重叠 | v16 阶段 1（合并到"用例质量评分体系"） |
| **#8 SPECIAL 变体用例** | 架构层 + 需真实 TC 数据 | v16 阶段 2（变体化能力框架） |
| **#9 + #10 S8 闭环 + 经验库** | 长期 + 与 v14 RCA 命名冲突 + 与 v15 §A.3 重叠 | v16 阶段 1（合并到 dashboard） |

### 4.4 v1.1 修订建议（落地节奏）

| 原 v1.1 节奏 | 修订节奏 | 理由 |
|---|---|---|
| v1.1: 5 项 P0（1/2/3/4 + 5） | **v1.1: 3 项 P0（#2 + #4 + C5 红警收紧）** + **3 项补充设计（#1/#3/#7 L1/L2 部分）** | 避免 BLOCKED 项拖累整个 v1.1 |
| v1.2: 3 项（6/7/8） | **v1.2: #5（修订后口径统一）+ #7 L3 部分（黄金用例基础）** | v1.2 调整为"统一覆盖率口径 + 黄金库录入" |
| v1.3: 2 项（9/10） | **v1.3: #8 SPECIAL 变体（合并 v15 §A.1 变体化能力） + #9/#10（合并 v15 §A.3 dashboard）** | v1.3 调整为"变体化能力 + dashboard 合并实现" |

---

## §5 与 v15 治理的对齐建议

### 5.1 v15 状态确认（2026-07-17）

| v15 阶段 | 状态 | 是否影响 v1.1 推进 |
|---|---|---|
| §5 阶段 1：L3 缺陷模式聚类 | ⚠️ 任务④ BLOCKED（无 S7 真实数据） | 阻塞 #5 #6 #9 #10 |
| §5 阶段 2：L1 增强路径 + L2 用例价值 | ✅ 已完成（`s3_mode_selector.py` + `A2_case_value_scoring.md`） | 不阻塞 |
| §5 阶段 3：v16 规划 + 经验归档 | ✅ `A3_dashboard_tech_design.md` 已完成 | 不阻塞 |

### 5.2 v1.1 与 v15 整合方案

| v15 产物 | v1.1 整合方向 |
|---|---|
| `ai_workflow/s5_exit_precheck.py` | 作为 v1.1 L1 格式校验的 S5 子集 |
| `ai_workflow/defect_cluster.py` | 与 v1.1 全局经验库合并（共用 trend_available 机制） |
| `governance/design_iter/plans/v15/A1_enhanced_path_feasibility.md` | v1.1 S3-lightweight 已包含（A1 决策树），不重复 |
| `governance/design_iter/plans/v15/A2_case_value_scoring.md` | v1.1 L3 黄金用例相似度合并到此（A2 3 维评分 + v1.1 黄金库） |
| `governance/design_iter/plans/v15/A3_dashboard_tech_design.md` | v1.1 全局经验库 + 月度聚合合并到此（A3 ASCII 柱图 POC） |
| `MODULES.md` §3.5 交叉场景判定 | v1.1 #2 模块优先级矩阵追加到 §3.5 末尾 |

### 5.3 治理层建议

**v1.1 不是独立迭代周期，而是 v15 → v16 之间的"实施期"**。建议：

1. **不创建新 governance/design_iter/plans/v1_1/** 目录
2. **本次 10 项改动作为 v15 实施补遗**——写入 `governance/design_iter/current/v15_v1_1_audit.md`（本文档）
3. **v1.1 P0 项 #2 / #4 落地后**，将变更登记到 `CHANGELOG.md`
4. **v16 PLAN.md 拍板时**纳入 v1.1 推迟项（#5 #6 #8 #9 #10）作为 v16 输入

---

## §6 给用户的关键问题（需拍板）

### Q1：v1.1 P0 项取舍

v1.1 提议 5 项 P0（#1 / #2 / #3 / #4 / #5）。其中 #5 覆盖率三级深度与 S5 §FP 门禁冲突，且依赖 BLOCKED 数据。

| 选项 | 含义 | 建议 |
|---|---|---|
| A | **按 v1.1 原方案推进 5 项 P0**（含 #5） | 高风险——#5 BLOCKED 拖累节奏 |
| B | **v1.1 P0 仅推 3 项（#2 / #3 / #4）**（推荐） | 与现有 v15 兼容、可在 1 周内完成 |
| C | **v1.1 P0 推 4 项（#1 / #2 / #3 / #4）** | 较激进——#1 S1 结构化评审包需补充 S1.5 边界设计 |

### Q2：v1.1 方案整体节奏

v1.1 §8 提议 v1.1 = 1-2 周 / v1.2 = 2-3 周 / v1.3 = 3-4 周（共 6-9 周）。

| 选项 | 含义 | 建议 |
|---|---|---|
| A | **完全按 v1.1 §8 节奏** | 风险——v1.3 项受 v15 BLOCKED 阻塞，无法启动 |
| B | **v1.1 = 2 周（P0 3-4 项） / v1.2 = 3 周 / v1.3 = 4 周，但前提是 ≥ 1 个真实项目跑通 S1-S7**（推荐） | 解锁 v15 BLOCKED 才能推进 v1.3 |
| C | **暂停 v1.1，先跑真实项目解锁 BLOCKED，再回头按 v1.1 节奏推进** | 最稳但慢 |

### Q3：本次落地的最小集

如果只落地最关键的 1 项以最小化改动（DNA §9.1 单次响应 ≤ 3 文件）：

| 优先级 | 项 | 理由 |
|---|---|---|
| 🏆 #2 | **模块优先级矩阵** | 1-2 天、1 文件改动、无依赖、立即解决归类一致性 70% → 90% 的痛点 |
| 🥈 #4 | **L1 格式校验脚本**（仅做 s5_exit_precheck.py 通用化） | 3-5 天、2 文件改动、复用现有代码 |
| 🥉 C5 | **红警阈值确认 20%** | 0 文件改动（已在 SSOT） |

---

## §7 落档协议执行记录（DNA §9.5）

| 步骤 | 状态 |
|---|---|
| §9.5 决策/计划产生前先 Write | ✅ 本档 `governance/design_iter/current/v15_v1_1_audit.md` |
| §9.5 占位文件骨架 | ✅ §1 总评先行 + §2 逐条 + §3 冲突 + §4 路线图 + §5 v15 对齐 + §6 拍板位 |
| §9.5 落档执行记录 | ✅ 本节 |
| §9.4 先验后答 | ✅ Read v1.1 方案 + v15 PLAN + v15 A1/A2/A3 + v15 SELF_CHECK + STAGE_S1-S8 .mdc + s3_mode_selector.py + s5_exit_precheck.py + defect_cluster.py + MODULES.md + test_points.json（前 100 行） |
| §9.1 决策密度 | ⚠️ 本档"软"决策密度高（10 项审核 + 10 项冲突 + 4 项推进路径 + 3 项关键问题）——非改动响应，是审核响应；豁免 |
| §3.7 SOP | 不适用（本文档非代码改动） |

---

## §8 自我引用（避免重复审核）

| 章节 | 引用的 v15 / STAGE 文件 | 已 Read 验证 |
|---|---|---|
| §1 现状基线 | `governance/design_iter/INDEX.md` + `v15/PLAN.md` + `v15/SELF_CHECK.md` + `v15/v15_L3_blocker.md` | ✅ |
| §2 #2 | `MODULES.md` §3.5（交叉场景判定）| ✅ |
| §2 #3 | `MODULES.md` §3.5 + STAGE_S5_TEST_POINTS.mdc §1.6 | ✅ |
| §2 #4 | `ai_workflow/s5_exit_precheck.py`（v14 §5 第二阶段第一项） | ✅ |
| §2 #5 | STAGE_S5_TEST_POINTS.mdc §FP 必填 + §1.5.6 四象限 | ✅ |
| §2 #6 | `v15/A2_case_value_scoring.md` 3 维评分方案 | ✅ |
| §2 #7 | `.cursor/skills/aidocx-s7-review/SKILL.md` §131-156 审查员 A/B | ✅ |
| §2 #8 | `v15/A1_enhanced_path_feasibility.md` S3 增强路径 | ✅ |
| §2 #9 | STAGE_S8_SELF_ITERATION.mdc §142-148 RCA + §150-168 归档 | ✅ |
| §2 #10 | `v15/A3_dashboard_tech_design.md` §2 方案对比 + §3 推荐 | ✅ |
| §3 冲突点 | DESIGN_AND_EXECUTION_STANDARDS.mdc §2.4.1 + §0.1 + §4.3 | ✅ |
| §5 v15 对齐 | `governance/design_iter/INDEX.md` §1 + §3 | ✅ |

---

## §9 版本与状态

| 项 | 值 |
|---|---|
| **版本** | v1.0（审核档首版）|
| **审核日期** | 2026-07-17 |
| **基于** | v15 PLAN.md（阶段 1/2/3 完成 + 阶段 1 任务④ BLOCKED）|
| **关键 BLOCKED 项** | v15 阶段 1 任务④（无 S7 真实数据）→ 阻塞 v1.1 #5 #6 #8 #9 #10 |
| **推进建议** | 本周落地 #2 / #4 / C5 共 3 项（最小集）；其余项走补充设计或推迟 v16 |

---

*由 AIDocxWorkFlow 测试架构师审核模式生成*
*本文档为决策辅助，最终推进方案由用户拍板*
