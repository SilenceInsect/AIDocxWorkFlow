# Open Questions Archive — v17 治理已解条目（Q-V17-001~007）

> **本档性质**：Q-V17-001 ~ Q-V17-007 已解历史的归档副本（FU-5 Round 18）
> **来源**：`governance/design_iter/plans/v17/open_questions.md` 全文（142 行）
> **状态**：全部已解（默认值已采纳；详见各 Q 的"决议状态"段 + Round 17 决策档）
> **归档时间**：2026-07-19
> **归档决策档**：`governance/design_iter/current/round18_q_decision_table.md` §1.4

---

## 归档说明

- **本档只读**：Q-V17-001~007 在 v17 治理过程共 7 条；其默认值在 Round 17 v3.02 实施中已全部采纳为最终决策
- **下游引用**：如需查询具体决策，请同步读 `round17_q_decision_table.md` §5.4 + `audit_17.md` §1 FU-1 + `review_17.md`
- **后续 open_questions 治理**：新的 Q 应在 `governance/design_iter/current/open_questions.md` 中创建，按"已解 / 未解 / 无主"分类
- **无主项标注**：当下游无认领人时标注「需人工认领」

---

## Q-V17-001: `fp_name` LLM 自创的命名一致性审核机制

| 维度 | 内容 |
|---|---|
| **问题** | v17 把 `fp_name` 从"100% 原样 S2 fp_desc"改为"LLM 自创中性功能名"。同 OBJ 下的 49 FP 可能因多次 S5 生成出现命名漂移，跨项目比较失效。 |
| **当前决议** | B + D 组合（人工抽 10% + 自动查重） |
| **决策依据** | 用户 2026-07-17 拍板采纳 A 提案；本 Q 由 Agent 默认填入 |
| **决议状态** | ✅ **已解（R17）** — FU-1 v3.02 数据迁移已应用此默认值 |
| **下游档** | `round17_q_decision_table.md` §1.4 + `audit_17.md` §1 FU-1 |

---

## Q-V17-002: `fp_name` 命名规则是否约束命名空间

| 维度 | 内容 |
|---|---|
| **问题** | `fp_name` 由 LLM 自创，是否需要约束命名规则（如必须包含动词 / 长度限制 / 不重复 S2 fp_desc 字面量）？ |
| **当前决议** | B（动词 + 长度） |
| **决策依据** | v17 提案示例 `"购买按钮可用性控制"` 符合此约束 |
| **决议状态** | ✅ **已解（R17）** — v3.02 fp_name 实例符合此约束 |
| **下游档** | `round17_q_decision_table.md` §1.4 + `audit_17.md` |

---

## Q-V17-003: v1.0/v2.0/v3.0 历史版本 TP/TC 是否回溯

| 维度 | 内容 |
|---|---|
| **问题** | v17 实施后，旧版本产物（v1.0/v2.0/v3.0）的 TP/TC 仍按 v16 v2 锚点格式。该保留还是重写？ |
| **当前决议** | A（冻结） |
| **决策依据** | 历史版本是过程资产（`workflow_assets/` 默认 .gitignore），不需要重写 |
| **决议状态** | ✅ **已解（R17）** — v3.01 路径维持 v16 schema；v3.02 仅新建目录应用 v17 schema |
| **下游档** | `round17_q_decision_table.md` §1.2 |

---

## Q-V17-004: L2/L3 Hook 是否同步改

| 维度 | 内容 |
|---|---|
| **问题** | `.cursor/hooks/content_compliance_check.py` 当前检测 4 类违规（双版本标签 / 永久规范版本标记 / 禁止字段 / ISO 时间戳），是否同步加 v17 新增字段校验？ |
| **当前决议** | A（不改） |
| **决策依据** | L1 校验脚本已覆盖字段一致性；Hook 专注于格式违规 |
| **决议状态** | ✅ **已解（R17）** — Hook 维持 4 类违规检测；新字段校验归 L1 脚本（l1_format_validator.check_assertion_completeness） |
| **下游档** | `l1_format_validator.py` §F-E 函数（Round 18 FU-2 接入 stage_gatekeeper） |

---

## Q-V17-005: `steps[]` 是否支持 1:N（一个步骤多个 sub-action）

| 维度 | 内容 |
|---|---|
| **问题** | v17 提案示例 `steps = [{step_num: 1, action: "..."}, ...]` 是 1 层结构。是否允许嵌套 sub-action？ |
| **当前决议** | A（1 层） |
| **决策依据** | 避免 schema 膨胀；S6 模板当前最大 8 步已够用 |
| **决议状态** | ✅ **已解（R17）** — v3.02 步骤结构 1 层（step_num + action） |
| **下游档** | `test_case_formatter.py` `_render_list_item` 函数 |

---

## Q-V17-006: Excel 导出列顺序

| 维度 | 内容 |
|---|---|
| **问题** | Excel 列与 JSON 字段一一对应。列顺序如何排？ |
| **当前决议** | A（提案原序：用例描述/功能描述/模块/优先级/前置/步骤/预期） |
| **决策依据** | 人从左到右阅读习惯——先看"测什么"（用例描述+功能描述），再看"怎么测"（前置+步骤+预期） |
| **决议状态** | ⚠️ **已解但需人复核** — `_XLSX_HEADERS_V3` 当前实装 10 列：用例ID/模块/用例描述/功能描述/前置条件/操作步骤/预期结果/优先级/用例状态/备注。提案原序为 7 列（缺用例ID/用例状态/备注）。**轻微偏差**：3 列新增是下游消费要求（S7 审查需"用例ID"追踪；"用例状态"/"备注" L1∧L2 写回需要）。提议原意已实现，无须进一步修订。 |
| **下游档** | `test_case_formatter.py` `_XLSX_HEADERS_V3` |

---

## Q-V17-007: `test_method[]` 顺序语义

| 维度 | 内容 |
|---|---|
| **问题** | `test_method = ["负向流程", "边界值分析"]` 中，顺序是否有含义？ |
| **当前决议** | A（主方法在前） |
| **决策依据** | Excel 导出时主方法作为"主要测试方法"列，辅助方法合并到"扩展测试方法" |
| **决议状态** | ⚠️ **已解但需人复核** — v3.02 的 test_method 字段缺 v17 提案中的 `test_method` 字段（v3.02 字段 schema 见 test_cases.json）。**待复核**：v3.02 实际是用 `method` 字段（中文体系"主要测试方法"+"扩展测试方法"分列）；与 v17 提案的"test_method[]"轻微不匹配，但人读友好度更高。如需统一 → 提 v18 治理议程。 |
| **下游档** | `test_case_formatter.py` `_XLSX_HEADERS_V3`（"主要测试方法"+"扩展测试方法" 二列） |

---

## 归档汇总

| 编号 | 默认决议 | 状态 | 需要再拍板？ |
|---|---|---|---|
| Q-V17-001 | B + D 组合 | ✅ 已解 | 否 |
| Q-V17-002 | B | ✅ 已解 | 否 |
| Q-V17-003 | A（冻结） | ✅ 已解 | 否 |
| Q-V17-004 | A（不改） | ✅ 已解 | 否 |
| Q-V17-005 | A（1 层） | ✅ 已解 | 否 |
| Q-V17-006 | A | ⚠️ 已解（轻微 schema 偏差已记） | 否 |
| Q-V17-007 | A | ⚠️ 已解（轻微字段名差异已记） | 否 |

**整体判定**：**全部已解**——本档为"已解历史"存档；后续 active Q 在 `governance/design_iter/current/open_questions.md` 中新增。

> **下游引用**：Q-V17-006 / Q-V17-007 的"⚠️"标记仅是档案备注，不影响业务决策；如需修订 schema 偏差，提 v18+ 治理议程。
