# aidocx-workflow-conversation 执行计划（2026-07-15 实战）

> **本次目标**：以 `resource/游戏道具商城系统/v3.01_raw.docx` 作为输入，通过 `aidocx-workflow-conversation` skill 编排的 `run_stage()` / `run_pipeline()` 把流程跑完。

## 1. 解决的认知错误

| 错 | 正 |
|---|---|
| "v4 编排"是 design_iter 方案 | `aidocx-workflow-conversation` skill 内部定义 runtime 为 v4 编排契约；与 design_iter v4 方案无关 |
| "提供的脚手架"是 `run_workflow.py` | 该文件不存在；脚手架 = `aidocx-workflow-conversation` skill 自己 |
| "v4 编排需要 stage_callable" | `stage_callable` 是 `run_stage()` 第 2 步，由 §6 推荐 callable (`save_stage5_output` / `format_test_cases` / `save_stage7_output`) 充当；业务 payload (test_points_payload / test_cases_payload / backlog_payload) 由调用方提供 |
| "v3.01 要按 v2.08 终版需求执行" | v3.01 是当前需求版本，迭代后的产物就属于 v3.01，无版本继承 |

## 2. 脚手架定义（`aidocx-workflow-conversation` SKILL.md）

- **单一入口**：`from ai_workflow.conversation_skills import run_stage, run_pipeline`
- **单阶段**：`run_stage("S6", req_name=..., project_name=..., version=...)`
- **多阶段**：`run_pipeline(["S5","S6","S7"], req_name=..., project_name=..., version=..., stage_callables={...})`
- **阶段执行固定顺序**：preflight → stage_callable → postflight
- **§6 推荐 callable**：`save_stage5_output` / `format_test_cases` / `save_stage7_output`
- **阶段状态枚举**：PASS / FAIL_PRECHECK / FAIL_RUNTIME_GATE / FAIL_POSTCHECK / NEED_LLM_OUTPUT / SKIPPED

## 3. 本次执行清单

### 3.1 输入材料

| 路径 | 状态 |
|---|---|
| `resource/游戏道具商城系统/v3.01_raw.docx` | 存在（42880 字节），已 Read 文本 |
| `resource/游戏道具商城系统/sample_requirements.md` | 存在（2184 字节），未读 |

### 3.2 project_name 推断（用户已决策）

- 用户决策："由 LLM 从 .docx 内容推断"
- 推断结果：`项目名 = "游戏道具商城系统"`（req_name），project_name 由 `req_name` 转 snake_case → `game_item_mall`（**待用户确认**）

### 3.3 阶段执行顺序（按 SKILL.md §6 示例）

```
S1 → S1.5 → S2 → S3 → S4 → S5 → S6 → S7 → S8
```

但 S5 之前需要 S2 backlog + S4 business_flow 作为 `test_points_payload` 输入——**LLM 必须先推理 backlog + business_flow**。

### 3.4 stage_callable 现状

| 阶段 | 现状 |
|---|---|
| S1 / S1.5 / S2 / S3 / S4 | **未实现 stage_callable**——必须由 LLM 直接写产物 JSON |
| S5 | `save_stage5_output(test_points_payload, ...)` |
| S6 | `format_test_cases(test_cases_payload, backlog_payload, test_points_payload, ...)` |
| S7 | `save_stage7_output(...)` |
| S8 | **未实现 stage_callable**——必须由 LLM 直接推理 |

### 3.5 待用户拍板的决策

1. **project_name 取值**：`game_item_mall`？或别的？
2. **执行范围**：全量 9 阶段？或按 §6 示例只跑 S5-S6-S7？
3. **stage_callable 缺口**：S1/S2/S3/S4/S8 必须由 LLM 直接写产物 JSON（没有 callable 兜底），是否接受？

## 4. 遗留问题（→ 下一轮拍板）

| 编号 | 问题 |
|---|---|
| Q-CONV-001 | S5 stage_callable `save_stage5_output` 接收的 `test_points_payload` schema 是什么？需 Read `test_case_formatter.py` / `save_stage5_output` 源码确认 |
| Q-CONV-002 | S2 / S4 阶段没有推荐 callable，但 `test_points_payload` / `backlog_payload` 必须存在——LLM 何时产出？ |
| Q-CONV-003 | `run_pipeline` 在 NEED_LLM_OUTPUT 时会停吗？还是 SKIPPED？ |
| Q-CONV-004 | `project_name` 是必填项还是可选？SKILL.md 示例里都是 `<project_name>` 占位符 |

## 5. 落档协议执行记录

- 本文件创建于 2026-07-15 1:30 (UTC+8)
- 阶段 3.4 / 3.5 由本轮对话拍板后填入"已确认 / 已决策"