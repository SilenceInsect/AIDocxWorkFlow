# v2 遗留问题（next version 待解决）

> **本文件是 v3 的输入**——v3 启动时**必须先答**这些问题。
> **格式约定**：`Q-XXX` 编号 + `状态: [ ] 未决 / [x] 已决`（v2 内部会答完 Q-001~Q-005，移到 resolved_questions.md）

---

## 重要：Q-001 ~ Q-005 已在 v2 PLAN.md 顶部"5 决策必答清单"

**v2 不在本文件重复列 Q-001 ~ Q-005**——v2 PLAN.md §⚠️ 启动必读 已经把它们作为 v2 主体待点头项。

> **本文件只列 v2 完成后仍未决的"次级问题"**（来自 v1 优先级 3 + 优先级 2）。

---

## 来自 v1 "方案迭代目录"自身设计的 4 个未决策（优先级 3）

- [C ] **Q-101**: design_iter 存什么？A / B / C？
  - 候选 A：只存方案文本
  - 候选 B：存"完整 diff 状态"
  - 候选 C：A + B（独立方案 + 自动 diff 视图）
  - **v1 当前默认**：C（已落地方案中——PLAN.md + INDEX.md 双视图）
  - **v2 启动后状态**：⏳ 待 v2 完成后评估——v2 完成后看 INDEX.md 是否满足需求

- [C ] **Q-102**: design_iter 回滚粒度？A / B / 都有？
  - 候选 A：整份方案回滚
  - 候选 B：字段级回滚
  - 候选 C：都有
  - **v1 当前默认**：A（v1 仅实现整份回滚；B 待 v3）
  - **v2 启动后状态**：⏳ 待 v2 完成后评估——v2 实际回滚场景是否需要字段级

- [ A] **Q-103**: design_iter 和现有备份关系？独立 / 包含 v1 / 替代？
  - 候选 A：完全独立
  - 候选 B：包含 v1（迁入备份）
  - 候选 C：替代（删旧备份）
  - **v1 当前默认**：B（已实施；保留 .bak + 软链——v2.1 修复后软链是 v2 起点）

- [C ] **Q-104**: design_iter 回滚方式？手工 / 脚本 / 自动？
  - 候选 A：人工手工
  - 候选 B：脚本一键
  - 候选 C：自动备份 + 脚本回滚
  - **v1 当前默认**：C（已实施；`design_iter.py rollback v0`）

---

## 来自反思的 1 个未决策（优先级 2）

- [ C] **Q-201**: Agent 把"先执行"误读为"先给方案"——v2 启动时如何避免？
  - 根因分析：用户说"先 X"时，Agent 倾向把"先"当成"先给方案再说"
  - 候选 A：在 DNA 自检 Q3 加一条"先 = 优先级，非流程词"
  - 候选 B：beforeSubmitPrompt hook 检测"先"字 + 强提示
  - 候选 C：A + B
  - **v1 当前默认**：C（DNA 已有"先动手再补"反模式 + Q-201 跟踪 v2）
  - **v2 启动后状态**：⏳ 部分缓解——v2 PLAN.md §推进原则 #1 明确"决策先于代码"。v2 完成后看实际效果

---

## v2 完成后的 v3 启动原则

**v3 启动时必读**——但 **v2 完成后** 才启动 v3：

1. **Q-101 ~ Q-104**（design_iter 自身设计）→ 决定 v3 是否扩展 design_iter 能力（如字段级回滚）
2. **Q-201**（反思类）→ 决定 DNA 自检内容是否扩展

**禁止**：
- 跳过 v2 直接评估 v3
- v2 完成前答 Q-101~Q-104 / Q-201（v2 实际效果未观察，评估无依据）

---

## 🚨 v2.08 实战中发现的产出门禁错误（Q-301 必答）

- [ ] **Q-301**: S6 阶段产出门禁执行失败——xlsx 漏生成却标 S6 完成
  - **症状**：v2.08 跑完 S1-S8 全流程后，回头检查 S6 产物时发现 `test_cases.xlsx` 未生成；S6 自检清单未把 xlsx 列为必产门禁，导致 LLM 自评"结构完整性 100%"是错的
  - **根因**：
    - STAGE_S6_TEST_CASES.mdc §输出规范 写了"成功产出（3 个文件）"含 xlsx，但**没有强门禁**（仅描述）
    - aidocx-s6-test-cases/SKILL.md 自检清单 5 项**不包含** xlsx 生成检查
    - S7 审查员 A 的 `id_normalization_rate` / `field_completion_rate` / `module_normalization_rate` 都不校验 xlsx 存在
  - **修复（v2.08 即时补丁）**：
    - ✅ STAGE_S6_TEST_CASES.mdc §输出规范 增加"xlsx 详细规范"+ 强制产出门禁 7 项
    - ✅ aidocx-s6-test-cases/SKILL.md 自检清单增加"3 个文件全部生成"+ "xlsx 3 Sheet 全部存在"
  - **候选决策（v3 待答）**：
    - 候选 A：仅在 S6 加 xlsx 强门禁
    - 候选 B：所有 8 阶段都加"强产出门禁"（每个阶段列产物必产清单，缺一 = 失败）
    - 候选 C：B + 在 S7 审查员 A 加 cross-stage 产物完整性校验
  - **v3 启动后状态**：⏳ 待 v3 决策——参考 v2.08 实战数据决定
  - **影响范围**：
    - 若选 B：8 阶段 × N 产物 = 大量规则更新
    - 若选 C：S7 审查逻辑要扩展（当前不校验 xlsx）

---

> **本文件状态**：v2 启动版 + v2.08 实战补丁（Q-301）。v2 期间**只**跟踪 Q-101~Q-104 + Q-201 + Q-301（5 主决策在 v2 PLAN.md 顶部）。
> **Q-001~Q-005 的 v2 决策状态**由 `v2/decisions.json` 跟踪（D-101~D-105 占位），本文件不重复。

---

## v6.2 闭环记录（2026-07-05）

> **本段是 v6.2 收尾状态**——§六 ~ §九 4 阶段已闭环，全部进入"已决"。
> **v6.2 与 v3 启动议题**（Q-101 ~ Q-104 / Q-201 / Q-301）**无直接关联**——本文件主体 v3 跟踪不动。

### Q-401: AGENTS.md 是否引用 DNA_3Q_CHECK §9.1.1？（v6.3 启动议题）

- [x] **Q-401** —— **状态**：✅ **已决**（v6.3 §10.2 闭环）
- **触发**：v6.2 §9.1.1 加了 self-test 豁免条款，但 **AGENTS.md 没引用**——Agent 启动时不会读到该条款，下次仍可能"无豁免走红线"
- **候选**：
  - 候选 A：AGENTS.md "关键引用"表加一行 → DNA_3Q_CHECK.mdc §9.1.1
  - 候选 B：不动 AGENTS.md，仅在 §9.1.1 文档顶部加红字"⚠️ Agent 必读"
  - 候选 C：A + B（双保险）
- **v6.2 决定**：⏳ 留 v6.3——v6.2 关注点聚焦"红线 + 豁免定义"，**引用传播**是 v6.3 议题
- **影响范围**：
  - 选 A：AGENTS.md 关键引用表 +1 行（约束改动——必先 ask）
  - 选 B：DNA_3Q_CHECK.mdc 顶部加红字（知识改动——直接动）
  - 选 C：2 文件改动，A 在红线下

### Q-402: scan_module_definitions.py Edit 残段风险？（v6.3 启动议题）

- [x] **Q-402** —— **状态**：✅ **已决**（v6.3 §10.3 闭环）
- **触发**：v6.2 §八 给 scan_module_definitions.py 加 self-test 时，**第一次 Edit 失败**（把函数体留成无 def 散段到文件末尾——Python 语法错）——**最终 Write 完整重写才修复**
- **根因**：
  - StrReplace 用 `if __name__ == "__main__"` 块作 anchor，把 self_test 函数定义**留在 `__main__` 之后**——Python 加载模块不执行 `__main__`，`self_test()` 引用在 module-level 不存在
  - Edit 时 old_string 包含 self_test 函数完整内容，匹配成功但**实际效果**是把 def 关键字删除，留下散落函数体
- **候选**：
  - 候选 A：把"先 Read 全文 + Write 重写"列为大文件改动 SOP
  - 候选 B：写 `dna_edit_residue_check.py` 验证 hook 文件 def 关键字与函数体配对
  - 候选 C：A + B
- **v6.2 决定**：⏳ 留 v6.3——v6.2 已修复单点，**模式化预防**是 v6.3 议题
- **影响范围**：
  - 选 A：DESIGN_AND_EXECUTION_STANDARDS.mdc §3 设计准则加"SOP"（约束——必先 ask）
  - 选 B：新加 hook（约束——必先 ask）
  - 选 C：3 文件改动，A 在红线下

### v6.2 闭环汇总

| 阶段 | 议题 | 状态 |
|---|---|---|
| §六 | dna_decision_persistence self-test | ✅ 已闭环 |
| §七 | feedback_logger self-test + cleanup | ✅ 已闭环 |
| §八 | 5 hook self-test（含 docx_hook bug 发现） | ✅ 已闭环 |
| §九 | docx_hook.py bug 修复 + §9.1.1 豁免条款 + 本档 | ✅ 已闭环 |

### v6.3 启动议题（2 个）

1. **Q-401**：AGENTS.md 引用 DNA_3Q_CHECK §9.1.1
2. **Q-402**：Edit 残段风险模式化预防

**v6.3 启动必读**：本文件 §v6.2 + q_decision_table.md §九 + CHANGELOG [v6.2]


> **本文件状态**：v2 启动版 + v2.08 实战补丁（Q-301）+ v6.2 闭环记录（Q-401/Q-402）。
> **v7 启动版段（Q-501~Q-569，69 条）已于 2026-07-09 迁移到 `plans/v7/open_questions.md`**——本文件主体 v3 跟踪不动。
> **Q-001~Q-005 的 v2 决策状态**由 `v2/decisions.json` 跟踪（D-101~D-105 占位），本文件不重复。
> **v7 期间跟踪**：Q-301 / Q-401 / Q-402 维持 v6.x 闭环现状不动。
