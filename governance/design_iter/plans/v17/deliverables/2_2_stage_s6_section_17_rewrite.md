# 子任务 2.2 — STAGE_S6 §1.7 改写

> **本档**：STAGE_S6_TEST_CASES.mdc §1.7 改写记录
> **治理基线**：v17/PLAN.md §2.1 必改约束文件（5 处）
> **v17 阶段定位**：Phase 2 子任务 2.2

---

## 1. 改写范围

| 项 | 值 |
|---|---|
| 文件 | `.cursor/rules/STAGE_S6_TEST_CASES.mdc` |
| 章节 | §1.7 命名继承一致性门禁 |
| 改写前 | "v2 完整版"（锚点继承校验） |
| 改写后 | **字段溯源版**（与 §1.9 同思路） |
| 行号范围 | line 327-403（原），改写后 §1.7.1-§1.7.7 |

---

## 2. 改写内容清单

### 2.1 标题 / 标识变更

| 改前 | 改后 |
|---|---|
| `## §1.7 命名继承一致性门禁（v2 完整版）` | `## §1.7 命名继承一致性门禁（字段溯源版）` |
| `（v16-NAME-001）` | `（NAME-FIELD-001）` |

### 2.2 §1.7.1 核心规范变更

| 维度 | 改前 | 改后 |
|---|---|---|
| 锚点格式 | test_scenario 必须以 `【{OBJ - FP}】` 开头 | test_scenario 纯场景描述，**不带锚点** |
| 名称匹配 | 锚点 OBJ 名 = S2 obj_name / 锚点 FP 名 = S2 fp_desc | **obj_name 字段 == 源 TP.obj_name / fp_name 字段 == 源 TP.fp_name** |
| 锚点取消 | 锚点为强制格式 | **锚点取消**——仅 JSON 字段，不重复进文本 |

### 2.3 §1.7.2 校验项变更

| 改前 | 改后 |
|---|---|
| 检查项：`tc_formal_name_anchor_v2` | 检查项：`tc_field_traceability` |
| test_scenario 锚点格式必须以【OBJ - FP】开头 | test_scenario 文本不带锚点 |
| 锚点继承正确（TC 锚点 = 源 TP title 锚点） | 删除该校验项 |
| OBJ 名称匹配（锚点 OBJ 名 = S2 obj_name） | 字段 obj_name == S2 obj_name |
| FP 名称匹配（锚点 FP 名 = S2 fp_desc） | 字段 fp_name == 源 TP.fp_name |

### 2.4 §1.7.4 标准格式变更

| 改前 | 改后 |
|---|---|
| `test_scenario: 【{OBJ正式名称} - {FP正式名称}】{场景描述}` | `test_scenario: {纯场景一句话描述}（不带锚点）` |

### 2.5 §1.7.6 L1 校验函数变更

| 改前 | 改后 |
|---|---|
| `L1S6Validator.validate_formal_name_v2()` | `L1S6Validator.validate_field_traceability()` |

### 2.6 §1.7.7 产出 Schema 变更

| 字段 | 改前 | 改后 |
|---|---|---|
| obj_name | `"商城首页道具列表"`（与改后一致） | `"商城首页道具列表"` |
| fp_name | `"首页按销量展示前10个热门道具"` | `"首页销量排序展示"`（中性功能名） |
| test_scenario | `"【商城首页道具列表 - 首页按销量展示前10个热门道具】玩家进入商城首页..."` | `"玩家进入商城首页，验证道具列表按销量降序排列"` |
| obj_id | （缺失） | `"UI-ITEM-MALL-01-002-OBJ-01"`（新增） |
| feature_point_ref | （缺失） | `"UI-ITEM-MALL-01-002-OBJ-01-FP-2"`（新增） |

---

## 3. §11 违规扫描结果

```
$ grep -E "\b(v\d+(?:\.\d+)?)\s+(v\d+(?:\.\d+)?)\b" STAGE_S6_TEST_CASES.mdc
（无输出）

$ grep -E "\b(v\d+(?:\.\d+)?)\s*(\+?\s*(?:新增|SSOT|强制))(规则)?" STAGE_S6_TEST_CASES.mdc
（无输出）
```

> **§1.7 范围内 0 处 §11 违规**。

---

## 4. 文档其他位置 v\d+ 残留（Out of Scope）

§1.7 范围外仍有 v\d+ 残留（line 134/138/143/146-148/152/158/160/162/169/181/212），但均属历史元数据注释（如"v14 三级强制"/"v11 §FP 全覆盖门禁"/"v10+ 必填"），**不属于 v17 字段溯源方案治理范围**。按 GOAL.md §2 Out of Scope 不在本次改动范围内。

**判定**：本次仅改 §1.7，其他位置不在治理范围内，保留原样。

---

## 5. 大文件改动 SOP 执行（§3.7）

| 检查项 | 状态 |
|---|---|
| Read 全文 | ✅（line 1-566 全部读完） |
| 文件总行数 | 566 行（> 400 行） |
| 走 StrReplace | ✅ |
| 额外验证（py_compile） | N/A（.mdc 非 Python） |
| 额外验证（grep 自检） | ✅（0 处 §11 违规） |
| 额外验证（行为自检） | N/A（约束文档无 self-test） |

---

## 6. 落档协议

- 本档已落档
- 修改文件数：1（STAGE_S6_TEST_CASES.mdc §1.7）
- 单次响应工具调用：≤ 10
- §11 违规数：0
- 子任务 2.2 状态：✅ 完成