# S5/S6 重产出 — Goal Loop Round 1

> **Goal ID**: `09cdc9e7-fc91-4a88-a391-51af56767806`（重新激活）
> **目标**：按 v17 字段溯源规范（无锚点版）全量重写游戏道具商城 v3.01 的 S5 test_points.json + S6 test_cases.json

---

## §1 用户原始目标

> `/goal-loop 重新产出S5、S6产物`
> **决策**：全量重写 + 不备份

---

## §2 验收标准

### value_criteria（外部价值）

| ID | 内容 | 严重度 |
|---|---|---|
| VC-1 | S5 test_points.json 重写后按 v17 字段溯源规范：`fp_name` 为 LLM 自创中性名（≤20字符，含动词） | BLOCKER |
| VC-2 | S6 test_cases.json 重写后按 v17 规范：`steps[]` 为结构化数组，`test_scenario` 不含【OBJ - FP】锚点 | BLOCKER |
| VC-3 | S5 OBJ 覆盖率 = 1.0（16/16），FP 覆盖率 = 1.0（49/49） | BLOCKER |
| VC-4 | S6 OBJ 覆盖率 = 1.0（16/16），FP 覆盖率 = 1.0（49/49） | BLOCKER |

### process_criteria（内部过程）

| ID | 内容 | 严重度 |
|---|---|---|
| PC-1 | S5 写回 `workflow_assets/游戏道具商城系统/v3.01/「S5 测试点生成」/test_points.json` | MAJOR |
| PC-2 | S6 写回 `workflow_assets/游戏道具商城系统/v3.01/「S6 测试用例生成」/test_cases.json` | MAJOR |
| PC-3 | `tp_count` 保持 87，TC 保持 87 | MAJOR |

---

## §3 执行计划

### Act Round 1

1. 读取 S2 `requirement_objects.json`（16 OBJ / 49 FP）— ✅ 已完成
2. 读取现有 S5/S6 产物，分析字段差异
3. 按 v17 字段溯源规范生成新 S5 test_points.json
   - `fp_name` 改 LLM 自创中性名（≤20字符，含动词）
   - `obj_name` 保持 S2 逐字
   - `title` 纯场景（4-12字）
   - `description` 纯测试逻辑（前置+步骤+预期）
   - 保留 `story_id` / `feature_point_ref` / `obj_id` / `s4_reference`
4. 按 v17 字段溯源规范生成新 S6 test_cases.json
   - `steps[]` 改为结构化数组
   - `test_scenario` 不含【】锚点
   - 保留字段映射：`obj_name` / `fp_name` / `obj_id` / `feature_point_ref`

---

## §4 out_of_scope.md（禁区清单）

### 功能禁区
- 增减 OBJ/FP 数量（保持 16 OBJ / 49 FP）
- 改变 Story 归属（保持现有 Epic/Story 映射）

### 技术禁区
- 使用 Python 脚本批量生成（由 LLM 逐条推理）

### 职责边界禁区
- 不修改 S1/S2 产物（S2 requirement_objects.json 不动）
- 不修改 L1 校验脚本（check_field_completion.py 不动）

---

## §5 落档协议执行记录

- 本档：`governance/design_iter/plans/v17/S5_S6_regen_20260718.md`
- 本轮改动文件：S5 test_points.json + S6 test_cases.json
- 验收标准数：4（VC-1~VC-4）+ 3（PC-1~PC-3）
