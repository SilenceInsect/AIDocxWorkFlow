# 9 阶段 SKILL.md 逻辑性审查报告

**审查日期**：2026-06-15
**审查范围**：`.cursor/skills/aidocx-s1-review/SKILL.md` 至 `.cursor/skills/aidocx-s8-self-iteration/SKILL.md`（共 10 个文件）
**交叉参考**：`MODULES.md`（8 模块 SSoT）

---

## 总览

| 指标 | 数值 |
|------|------|
| **整体评分** | 6.2 / 10 |
| P0 级问题（阻塞执行） | **9 个** |
| P1 级问题（影响质量） | **14 个** |
| P2 级问题（小修小补） | **8 个** |

### 核心问题摘要

1. **S8 是 S7 硬判决的唯一来源，但 SKILL 中 S8 仍在用旧 PASS/FAIL 判决——与 S7 v2.0 "LLM 写建议、无硬阈值"设计哲学直接矛盾**
2. **S2 声称"不设硬性 3-8 FP 限制"，但 §1.5.6 第 5 问暗含"先追求质量"，缺少可量化质量门槛，LLM 无法自判**
3. **S5/S6 跨阶段字段契约（S5 `test_type_subclass` / `s4_reference` / `applies_rule`）在 S6 中被声明为"不强制"，但 S7/S8 的覆盖率计算依赖这些字段——存在隐性硬依赖**
4. **S3 原型导出 SKILL 仅 89 行，缺少 §1.5 决策 push 块、自检清单、失败判定逻辑——S3 是 S4 流程图的强依赖上游，但自身产出质量无约束**
5. **S2.5 迭代规划 SKILL 中"S2 backlog.json"是必读材料，但 S2 并未声明 backlog.json 的必填字段——LLM 无法验证输入合法性**

---

## 按阶段逐项审查

---

### S1 需求评审（aidocx-s1-review/SKILL.md）

#### 内部逻辑矛盾

**矛盾 1：PURCHASE_STRONG 3 段强制 vs S1 不阻断**

- **位置**：§"强付费项质量闭环 3 段（强制项）"（行 159-176）
- **内容**："缺失判定 → 1 条 P0（SPECIAL_FLAG = PURCHASE_STRONG）→ **不阻断 S1**"
- **问题**：声称"强制项"但"不阻断 S1"，这意味着 S1 可以在 PURCHASE_STRONG 缺失的情况下 PASS，这与"强制项"的语义矛盾
- **实际规则**：真正门禁在 S1.5（行 176："S1.5 必须由人工补齐"）。但 §"输入审查（门禁前置条件）"（行 62-68）和 §"质量评价标准"（行 146-158）均未提及 PURCHASE_STRONG 的特殊门禁
- **结论**：PURCHASE_STRONG 的 3 段是 S1.5 的契约，不是 S1 的门禁，但行 159 用词"强制项"具有误导性

**矛盾 2：5 维度权重 25%+25%+20%+20%+10% = 100%，但判决分数阈值（≥7.0）无数学推导**

- **位置**：§"5维度评审"（行 232-240）+ §"判决规则"（行 244-250）
- **问题**：权重加起来是 100%，但为什么 PASS 阈值是 7.0 而不是 6.0 或 8.0？SKILL 未说明 7.0 的推导过程。实际意义模糊

#### 触发条件与产出不对应

**不对应 1：声称产出 `requirement_objects.json`，但路径约定与 AIDocxWorkFlow.mdc 不一致**

- **位置**：§"成功产出"行 265：`requirement_objects.json` 路径
- **问题**：AIDocxWorkFlow.mdc 中 S2 的产出路径包含 `requirement_objects.json`，但 S1 也声称产出 `requirement_objects.json`。两者如何区分？S2 消费 S1 的 `requirement_objects.json`，S2 自己又产出一份同名文件，会覆盖还是合并？
- **建议**：S1 的 output 应该命名为 `requirement_objects_s1.json` 或在 `requirement_objects.json` 中加 `stage: "S1"` 字段

**不对应 2：自动化支持引用过时实现**

- **位置**：§"自动化支持"行 329
- **内容**：`ai_workflow/requirement_reviewer_auto.py`（⚠️ 仍按"4 项内容门禁"实现，待对齐本次重构）
- **问题**：SKILL 正文已完全重构（5 维度评审、角色定义、需求对象），但自动化代码标注"待对齐"——这意味着脚本和 SKILL 存在脱节

#### §1.4 必读指令

✅ **完整**。3 份必读材料路径清晰：
1. `.cursor/MODULES.md`（§1 总表）
2. S1 产物目录结构
3. `.cursor/MODULES.md`（§4 各模块 O_boundary.md）

#### §1.5 决策 push 块

✅ **有**（在反向契约行 294-320）。但反向契约写得像"须知"而不是"决策引导"——缺少 S1 LLM 执行时的具体判断步骤。

#### 反例库 / 字段名清单

⚠️ **部分落地**。SKILL 中提到 PURCHASE_STRONG 强付费项 3 段作为"契约"，但没有独立反例库。模块边界反例在 MODULES.md 中，S1 §1.4 引用了 MODULES.md。

#### 失败判定

✅ **清晰**：
- 物料门禁失败：生成 `fail_report_S1.md`，仅含"失败原因 = 物料缺失"一段
- 内容门禁（4 项）：S1 v2.0 已移除，改由产物覆盖（角色定义 / 需求对象 / 问题需求）

#### 自检清单

⚠️ **缺失**。S1 SKILL 没有显式的自检清单。LLM 需要自己记住：
- 5 维度评分格式
- 角色定义格式
- 需求对象拆解格式
- 问题需求清单格式

#### 综合评分

- **评分**：6.5 / 10
- **关键问题**：
  1. PURCHASE_STRONG"强制项"vs"不阻断 S1"的语义矛盾，需明确标注为"S1.5 门禁项"
  2. `requirement_objects.json` 跨 S1/S2 重名，需加 stage 字段区分
  3. 自动化代码标注"待对齐"，需同步更新或删除

---

### S1.5 业务澄清与准出许可（aidocx-s1-5-clarification/SKILL.md）

#### 内部逻辑矛盾

**矛盾 1：quality_level HIGH 的判定条件与 BLOCKED 的判定条件存在重叠**

- **位置**：§"质量评价标准（exit_permission）"（行 146-158）
- **HIGH 条件**：P0 全部填写 + P1 全部填写 + P2 全部填写 + 无遗留歧义
- **BLOCKED 条件**：P0 未填写 或 强付费项 P0 未答完
- **问题**：HIGH 要求"P2 全部填写"，但 BLOCKED 只看 P0。如果 P0 全填、P1 全填、P2 全填 → HIGH；但如果 P0 全填、P1 全填、P2 只填了 50% → 落入 MEDIUM。但行 151 "MEDIUM" 定义中 P2 部分填写的比例是多少？未定义

**矛盾 2：行 65-66 "强付费项 P0"处理逻辑重复**

- 行 65：`P0 问题已填写 → 触发 fail_report_S1_5.md（BLOCKED）`
- 行 66：`强付费项 P0 必须全部答完 → 与 P0 缺失同等处理`
- 行 175-176（S1 SKILL）也有类似逻辑：SPECIAL_FLAG = PURCHASE_STRONG 的 P0 必须全部答完
- **问题**：两份文件都在说同一件事，但没有统一的字段名。S1.5 用的是"强付费项 P0"，S1 用的是"SPECIAL_FLAG = PURCHASE_STRONG"——字段名不一致

#### 触发条件与产出不对应

**不对应 1：S1.5 的前置材料中有 `clarification_checklist.md`，但 S1 产出中 clarification_checklist.md 的状态是"人工填写前"的版本**

- S1 SKILL 行 266：clarification_checklist.md（始终产出）
- S1.5 SKILL 行 33-36：clarification_checklist.md（人工填写后）
- **问题**：S1 产出的 clarification_checklist.md 包含"问题需求"节（行 218-228），S1.5 需要的是"人工填写处理方案"节（行 140-142）。两个节是同一个文件的不同部分——文件内部的状态管理逻辑缺失

**不对应 2：exit_permission.json 字段 `strong_purchase_p0_resolved` 在 §1.4 必读材料清单中未提及**

- 行 115 包含 `strong_purchase_p0_resolved: true | false`
- 但 §1.4 必读材料清单（行 39-48）没有要求读 S1 产出的"强付费项 P0"相关文件
- LLM 可能在没有上下文的情况下填 `strong_purchase_p0_resolved`

#### §1.4 必读指令

✅ **完整**。4 份材料，路径明确。

#### §1.5 决策 push 块

❌ **缺失**。S1.5 SKILL 没有 §1.5 决策 push 块。LLM 在生成 exit_permission.json 时没有任何引导。

#### 反例库 / 字段名清单

⚠️ **部分落地**。quality_level 4 档判定逻辑是隐含的反例（MEDIUM vs LOW 的边界模糊）。

#### 失败判定

✅ **清晰**：
- P0 项未填写 → fail_report_S1_5.md
- 所有检查项均为无效内容 → fail_report_S1_5.md

#### 自检清单

⚠️ **部分存在**。§"输入审查（门禁前置）"（行 60-68）有 5 项检查，但"处理方案有效"的判定（行 67）过于模糊："处理方案不为空或无意义占位文字"——无意义占位文字如何判定？

#### 综合评分

- **评分**：5.5 / 10
- **关键问题**：
  1. quality_level MEDIUM 的 P2 部分填写比例未定义，导致 quality_level 判定存在灰色地带
  2. "强付费项 P0"在 S1 和 S1.5 中字段名不一致（SPECIAL_FLAG vs strong_purchase_p0_resolved）
  3. 缺少 §1.5 决策 push 块，exit_permission.json 生成的 LLM 引导不足

---

### S2 需求拆解（aidocx-s2-breakdown/SKILL.md）

#### 内部逻辑矛盾

**矛盾 1："不设硬性 3-8 FP 限制" vs §1.5.6 "5 问质量 push"无法量化**

- **位置**：§"第五层：FP（功能点）"（行 266-289）
- **内容**："粒度（按业务自然拆分，**无强制 3-8 限制**）"（行 266）
- **问题**：§1.5.6 "5 问质量 push"（行 540-549）问的是"5 问质量"——但没有告诉 LLM"质量合格"的标准是什么。如果 5 问都回答了但答案很空洞，SKILL 没有兜底规则
- **结论**：去硬数字是对的，但需要替代性的质量门槛。现有 §1.5.6 的 5 问只是引导，不是可判定的验收标准

**矛盾 2：backlog.json 必填字段在 §1.4 必读材料中没有验证方式**

- 行 365-405：backlog.json 的完整 schema（含 `release` 顶层字段、`epics[].stories[].acceptance_criteria` 等）
- 行 66：必读材料包含 backlog.json
- **问题**：S2 SKILL 没有告诉 LLM 如何验证 backlog.json 的字段是否完整。S4 会检查 `summary.epic_count ≥ 1`，但 S2 自己没有等效的自我检查

**矛盾 3：ID 格式中 HINT 不作为 Epic ID 前缀，但 §1.4 表格（行 304-313）没有明确说明**

- 行 111：Epic ID 格式 `{Module}-{Number}`，示例中无 HINT
- 行 317：HINT 不作为 Epic ID 前缀
- **问题**：§1.4 的 8 模块总表（行 304-313）列出了 8 个模块作为 Epic ID 前缀，但没有明确说"HINT 除外"。这可能误导 LLM 在写 Epic 时用 HINT 作为前缀

#### 触发条件与产出不对应

**不对应 1：S2 的产出之一是 `requirement_objects.json`，但 S1 也产出了同名文件**

- S1 SKILL 行 265：S1 产出 `requirement_objects.json`（S1 阶段的需求对象）
- S2 SKILL 行 363：S2 产出 `requirement_objects.json`（Epic/Story/OBJ/FP 层的完整拆解）
- **问题**：两个阶段产出同名文件，S2 的 requirement_objects.json 会覆盖 S1 的。没有版本区分或 stage 字段

#### §1.4 必读指令

✅ **完整**。5 份材料（8 模块总表 + 模块边界区分 + exit_permission + 终版需求 + 模块子模板），路径明确。

#### §1.5 决策 push 块

✅ **完整且可操作**。6 个 push 块（§1.5.1-§1.5.6），每个都有具体的判定步骤。这是整个 SKILL 体系中执行性最强的 §1.5。

#### 反例库 / 字段名清单

✅ **落地**。§1.5.4 OBJ 误标反例库（行 516-522）列出了 4 个具体反例，且要求 LLM 声明"已对照反例 1-4"。§1.5.5 OBJ 字段名强约束（行 524-539）列出了 10 个字段名（包含新旧字段名迁移说明）。

#### 失败判定

✅ **清晰**。§"输入审查（门禁前置条件）"（行 338-352）包含 12 项检查，判定逻辑明确。

#### 自检清单

⚠️ **分散**。自检逻辑分散在多个 push 块和输入审查表中，没有统一的"提交前自检清单"。

#### 综合评分

- **评分**：7.0 / 10
- **关键问题**：
  1. §1.5.6 "5 问质量 push"缺少可量化的质量门槛，需要增加"5 问答案的最低质量标准"
  2. HINT 不作为 Epic ID 前缀需要在 §1.4 8 模块总表中明确说明（不是在 §1.4 行 317 单独说明）
  3. S1/S2 `requirement_objects.json` 重名问题需解决

---

### S2.5 迭代规划（aidocx-s2-5-iteration/SKILL.md）

#### 内部逻辑矛盾

**矛盾 1：Step 0 项目配置收集与 S2 backlog 消费逻辑不对应**

- **位置**：§"Step 0"（行 23-58）+ §"阶段入口"（行 62-68）
- **Step 0**：强制收集 10 项配置参数（req_name / version / 工时 / 团队规模等）
- **§"阶段入口"**：前置材料仅要求"S2 backlog.md + project_config.json"
- **问题**：Step 0 收集的 10 项参数中，哪些应该来自 S2 backlog，哪些来自人工填写？如果 S2 backlog 没有包含工时估算，LLM 如何获取？"团队规模"字段在 S2 backlog 中是否有对应字段？

**矛盾 2：自动化支持引用不存在的函数**

- **位置**：§"自动化支持"（行 122-132）
- **内容**：`make_stage2_5_skill(req_name="...", project_config={...})`
- **问题**：`ai_workflow/conversation_skills.py` 中是否存在 `make_stage2_5_skill`？如果不存在，自动化支持是空头承诺

#### 触发条件与产出不对应

**不对应 1：S2 backlog.json 是必读材料，但 S2 没有声明 backlog.json 的必填字段**

- 行 79：`S2 backlog`（必读）
- 行 80：`需求对象`（必读）
- **问题**：S2.5 LLM 在消费 backlog 时，无法判断 backlog 是否"合法"。S4 有"backlog.json 存在 + summary.epic_count ≥ 1"检查，S2.5 没有等效检查

#### §1.4 必读指令

⚠️ **路径不完整**。3 份材料中：
- S2 backlog（行 79）：只说了"backlog.md"，但 S2 产出了 backlog.md + backlog.json，哪个是 S2.5 真正需要的？行 66 说"前置材料：S2 backlog.md"，但 §1.4 行 79 只说"S2 backlog"（模糊）
- 需求对象（行 80）：没有具体文件名（S2 产出了 `requirement_objects.md/json`）

#### §1.5 决策 push 块

❌ **缺失**。S2.5 是唯一没有 §1.5 决策 push 块的阶段。

#### 反例库 / 字段名清单

❌ **缺失**。S2.5 没有反例库、字段名清单。

#### 失败判定

⚠️ **模糊**。"材料缺失时：生成失败报告"——但哪些算"缺失"？如果 project_config.json 不存在但 S2 backlog 存在，算缺失吗？失败报告的触发条件不够具体。

#### 自检清单

❌ **缺失**。S2.5 没有自检清单。

#### 综合评分

- **评分**：3.5 / 10
- **关键问题**：
  1. 缺少 §1.5 决策 push 块——这是 S2.5 体系中最严重的问题
  2. backlog.md vs backlog.json 路径不一致
  3. 失败判定触发条件不够具体
  4. 自动化支持引用的函数可能不存在

---

### S3 原型导出（aidocx-s3-prototype/SKILL.md）

#### 内部逻辑矛盾

**矛盾 1：S3 的输入是"每个 Story 生成原型"，但 S3 产出只有 prototype.md，没有 Story 层级的关联**

- **位置**：§"核心任务"（行 44-61）
- **内容**：每个 Story 生成原型
- **问题**：S3 产出的 prototype.md 中，每个原型应该关联到哪个 Story？SKILL 没有规定 prototype 的 ID 格式，也没有规定如何与 S2 Story 关联。S4 SKILL 行 42 提到"S3 prototype.md（推荐）"用于引用 `PAGE-XXX` 页面 ID，但 S3 SKILL 没有规定 PAGE-XXX 的命名规则

**矛盾 2：S3 SKILL 只有 89 行，是最短的 SKILL**

- S1: 331 行
- S1.5: 202 行
- S2: 557 行
- S2.5: 140 行
- S3: 89 行（最短）
- S4: 369 行
- S5: 268 行
- S6: 184 行
- S7: 197 行
- S8: 198 行

- **问题**：S3 是 S4 流程图的"直接弱依赖"（S4 SKILL 行 255），S4 依赖 S3 的 PAGE-XXX 节点命名，但 S3 本身缺少：§1.5 决策 push 块、输入审查表、失败判定具体逻辑、自检清单

#### 触发条件与产出不对应

**不对应 1：S3 prototype.md 中页面的 ID 格式未定义**

- S4 需要引用 S3 的 PAGE-XXX 节点（S4 SKILL 行 42）
- S3 没有规定 PAGE-XXX 的命名规则（如 `PAGE-{EpicID}-{N}` 或其他）
- **结论**：S3/S4 之间的数据契约不完整

#### §1.4 必读指令

✅ **完整**。2 份材料（8 模块总表 + S2 backlog）。

#### §1.5 决策 push 块

❌ **缺失**。S3 SKILL 没有 §1.5 决策 push 块。LLM 在生成原型时没有任何决策引导。

#### 反例库 / 字段名清单

❌ **缺失**。S3 没有反例库。

#### 失败判定

⚠️ **模糊**。"材料缺失时：生成失败报告"——但 S3 没有任何输入审查表。S2 backlog 存在就算通过吗？backlog 需要包含哪些字段？

#### 自检清单

❌ **缺失**。S3 没有自检清单。

#### 综合评分

- **评分**：3.0 / 10（最低分）
- **关键问题**：
  1. 缺少 PAGE-XXX 节点 ID 格式定义——导致 S4 无法可靠引用 S3 产出
  2. 缺少 §1.5 决策 push 块
  3. 缺少输入审查表——LLM 无法验证 backlog 的合法性
  4. 缺少自检清单

---

### S4 流程图导出（aidocx-s4-flowchart/SKILL.md）

#### 内部逻辑矛盾

**矛盾 1：异常树叶子节点 ID 格式自相矛盾**

- **位置**：§"S4 必含 4 类可机检 ID"（行 74-81）
- **ID 格式表**（行 78）：`S4-{EpicID}-{seq}.{N}`，示例 `S4-BIZ-PURCHASE-1.3.2`
- **异常/错误决策树格式**（行 153-165）：节点 ID `S4-{Epic1ID}-1.1`、`S4-{Epic1ID}-1.1.1`
- **问题**：表中的格式是 `S4-{EpicID}-{seq}.{N}`（如 1.3.2），但树中的节点 ID 是 `S4-{Epic1ID}-1.1`、`S4-{Epic1ID}-1.1.1`（用点号分隔层级）。两个格式不一致。推荐使用哪个？

**矛盾 2：S4 是 S5 EXCEPTION 类型 TP 的核心来源，但 S4 没有规定 EXCEPTION TP 的格式要求**

- 行 261：S5 测试点生成"直接强依赖"S4 的异常树叶子节点
- **问题**：S4 产出的异常树叶子节点需要包含哪些信息才能让 S5 正确生成 EXCEPTION TP？SKILL 没有规定叶子节点的最低信息量要求（如：触发条件、预期结果、模块归属）

#### 触发条件与产出不对应

**不对应 1：S4 的前置材料审查要求在"材料缺失时"处理不一致**

- **位置**：§"阶段入口"（行 35-44）
- 行 41：S1.5 exit_permission.json → "警告，不阻断"
- 行 42：S3 prototype.md → "警告，不阻断"
- **问题**：S4 的 4 类产出（Flowchart/Sequence/异常树/风险点）中，哪些在 S3 prototype 缺失时仍能正常生成？SKILL 没有区分

#### §1.4 必读指令

✅ **完整**。4 份材料，路径明确。

#### §1.5 决策 push 块

✅ **完整**。3 个 push 块（§1.5.1-§1.5.3），具体可操作。

#### 反例库 / 字段名清单

✅ **落地**。§1.5.1 包含 Epic 命名反例，§1.5.2 包含异常树叶子归属反例。

#### 失败判定

✅ **清晰**。§"输入审查"（行 85-96）+ §"质量门禁"（行 283-293）两层判定，逻辑清晰。

#### 自检清单

⚠️ **分散**。自检逻辑分散在输入审查和质量门禁中。

#### 综合评分

- **评分**：7.0 / 10
- **关键问题**：
  1. 异常树叶子节点 ID 格式（`S4-{EpicID}-{seq}.{N}` vs `S4-{EpicID}-1.1.1`）存在歧义
  2. 叶子节点最低信息量要求未规定——可能导致 S5 无法正确消费

---

### S5 测试点生成（aidocx-s5-test-points/SKILL.md）

#### 内部逻辑矛盾

**矛盾 1："强制对照反例库"但反例库内容在不同位置重复出现且不完全一致**

- **位置**：
  - §1.5.4 反例对照 push（行 184-191）：列出了"HINT 误判 1-8 / BIZ 误判 1-10 / UI 误判 1-5 / UTIL vs BIZ 误判 5 / LOG 误判 8"
  - 行 127：反例库是"75 个 TP 旧规则产物"
  - MODULES.md §4.11.2 HINT vs UI 边界（行 572-585）：有 5 个举例区分
  - MODULES.md §3.5 交叉场景归属判定（行 67-89）：有 8 条判定规则

- **问题**：
  1. §1.5.4 列出的反例数量（HINT 1-8 / BIZ 1-10 / UI 1-5 / UTIL vs BIZ 5 / LOG 8）与行 127 的"75 个 TP 是 S5 旧规则下的产物"是什么关系？旧 TP 是反例库，那 §1.5.4 的编号（HINT 1-8 等）是指 75 个 TP 中的序号吗？
  2. HINT vs UI 边界在 §1.5.4（行 78 引用 O_boundary.md）和 §4.11.2 中都有规定，数字能对上吗？

**矛盾 2：§1.5.6 "5 问质量 push"与 §1.5.2 "4 步判定 push"存在重复**

- §1.5.2（行 164-172）：Push 1-4（读 §3.5 → 读模块 .md → 读 O_boundary → 读 S4）
- §1.5.6（行 208-219）：5 问质量 push（测什么/命中模块/边界值/预期结果/S4 节点）
- **问题**：两者都要求"回答 5 个问题"，但内容有重叠（都涉及模块判定和 S4 引用）。LLM 是否需要走两遍？SKILL 没有说明

**矛盾 3：TP JSON 字段名在不同位置不完全一致**

- §1.5.5 JSON Schema（行 197-207）：`tp_id` / `module` / `test_point_type` / `test_type_subclass` / `description` / `s4_reference` / `boundary` / `is_assumed` / `applies_rule`
- §1.6 强制输出字段（行 225-231）：`s4_reference` / `test_type_subclass` / `applies_rule` / `is_assumed` / `boundary` / `module_judgment_basis`
- **差异**：`tp_id` 在 §1.5.5 必填，在 §1.6 强制输出中未提及；`module_judgment_basis` 在 §1.6 选填，但不在 §1.5.5 schema 中
- **结论**：字段清单略有漂移

#### 触发条件与产出不对应

**不对应 1：S5 的产出 test_points.json 中 `s4_reference` 字段在 S4 产出的 business_flow.md 中可能找不到对应引用**

- S4 行 81："S5 TP `s4_reference` 字段**推荐使用** `R-{EpicID}-NN` 格式"
- **问题**："推荐使用"意味着可以不填。但如果 S5 TP 的 `s4_reference` 留空，S7/S8 的覆盖率计算如何处理？
- S7 SKILL 行 82：`s4_reference 字段填充率`是审查指标之一
- S8 SKILL 行 91-93：S4 风险点覆盖率 < 100% 的根因之一是"S5 未引用 S4 风险点"
- **隐性硬依赖链**：S4 产出 R-XX → S5 应该引用 R-XX → S7 检查引用率 → S8 分析覆盖率。S5 "推荐使用"与 S7/S8 的强制性审查存在矛盾

#### §1.4 必读指令

✅ **完整且详细**。提供了 8 模块的 Read 路径表格（行 106-118），这是整个 SKILL 体系中最完整的 §1.4。

#### §1.5 决策 push 块

✅ **完整**。6 个 push 块（§1.5.1-§1.5.6），具体可操作。特别是 §1.5.4 反例对照 push 有具体的反例编号。

#### 反例库 / 字段名清单

⚠️ **部分歧义**。反例库的内容（75 个旧 TP vs §1.5.4 的编号列表）关系不清晰。

#### 失败判定

✅ **清晰**。§"输入审查"（行 85-96）包含 6 项检查，判定明确。

#### 自检清单

⚠️ **分散**。6 个 push 块 + §1.6 强制字段清单构成隐含的自检集合，但没有统一的"提交前自检清单"。

#### 综合评分

- **评分**：6.5 / 10
- **关键问题**：
  1. 反例库的两套来源（75 个旧 TP vs §1.5.4 编号列表）关系不清晰，LLM 不知道该对照哪套
  2. §1.5.2 和 §1.5.6 存在重复的判定步骤，需合并或明确区分
  3. TP JSON 字段名在 §1.5.5 和 §1.6 有轻微漂移（`tp_id` vs `module_judgment_basis`）
  4. `s4_reference` "推荐使用"与 S7/S8 的强制性审查存在矛盾

---

### S6 测试用例生成（aidocx-s6-test-cases/SKILL.md）

#### 内部逻辑矛盾

**矛盾 1：S6 设计哲学"不设硬指标"，但 §1.5.5 "自检清单"有隐性指标**

- **位置**：§"设计哲学（v2.0 重构）"（行 20-24）
- **内容**："不设硬指标：1:1 也行、1:5 也行、1:20 也行"
- **位置**：§"自检清单"（行 120-129）
- **内容**：检查"用例描述是否只含纯 Story 标题"、"功能描述是否自然语言"、"操作步骤是否无任何模板语言"、"预期结果是否无任何括号引用"
- **问题**：自检清单的前 4 项是"格式检查"，不是"质量检查"。格式检查本身没问题，但与"不设硬指标"的哲学有一定张力——S6 不设数量指标，但 S6 强制禁止"括号引用"等格式问题。"全文搜索'引用'应为 0 处"（行 126）是一个可自动检查的硬指标，与"不设硬指标"矛盾

**矛盾 2：S6 禁止照抄 S4/S5 节点名，但 S6 的优先级映射表（P0/P1/P2）引用了 S4/S5 的字段**

- **位置**：§"优先级判断"（行 108-116）
- **内容**：P0 适用场景包括"支付、购买、VIP折扣、促销、安全、**日志**"
- **问题**：S5 中模块字段包含 LOG，"日志"出现在 P0 列表中意味着 LOG 模块的 TC 默认 P0。但这与"优先级由 LLM 根据业务风险自由决定"（行 116）存在矛盾——"自由决定"但有默认暗示

#### 触发条件与产出不对应

**不对应 1：S6 引用 S5 test_points.json 的 `test_type_subclass` 字段，但 S5 的 `test_type_subclass` 字段定义在不同位置有歧义**

- S5 SKILL 行 201：`test_type_subclass`（String，命中模块子模板的枚举）
- S5 §1.6 强制输出字段（行 227）：`test_type_subclass: 必填，用细分枚举(如 `BIZ.H_payment` 而非大类 `BIZ_PAYMENT`)`
- **问题**：`BIZ.H_payment` 这个枚举值在哪里定义？MODULES.md 中 BIZ 模块的子类枚举是 `BIZ_LOGIC / BIZ_DATA_FLOW / BIZ_PROTOCOL / BIZ_STATE_MACHINE / BIZ_DB_PERSIST / BIZ_CONCURRENCY / BIZ_SCHEDULED_TASK / BIZ_PAYMENT / BIZ_AUDIT_LOG`，没有 `H_payment` 格式。这看起来是示例笔误，应为 `BIZ.H` 或 `BIZ.H_payment` 需要在 MODULES.md 中有对应定义

#### §1.4 必读指令

✅ **完整**。5 份材料，路径明确。

#### §1.5 决策 push 块

❌ **缺失**。S6 SKILL 没有 §1.5 决策 push 块。这是 S6 的主要缺陷——LLM 在推理 TC 拓宽方向时没有引导。

#### 反例库 / 字段名清单

⚠️ **部分落地**。"正确 vs 错误对比"（行 56-71）提供了格式对比，但缺少模块归属反例库。

#### 失败判定

⚠️ **模糊**。SKILL 没有显式的输入审查表或失败判定逻辑。"前置材料（S5 test_points.json）审查合格后开始"——"合格"的标准是什么？

#### 自检清单

✅ **存在**（行 120-129）。5 项格式检查，可操作。

#### 综合评分

- **评分**：5.0 / 10
- **关键问题**：
  1. 缺少 §1.5 决策 push 块——S6 是 LLM 推理最重的阶段（"从 TP 派生出几个 TC"），没有引导
  2. "全文搜索'引用'应为 0 处"是隐性硬指标，与"不设硬指标"哲学矛盾
  3. `test_type_subclass` 示例值 `BIZ.H_payment` 在 MODULES.md 中无对应定义
  4. 缺少显式的输入审查表

---

### S7 用例审查（aidocx-s7-review/SKILL.md）

#### 内部逻辑矛盾

**矛盾 1：S7 SKILL 中 §"质量门禁（v2.0：无硬阈值）"（行 93-107）与 S8 规则存在冲突**

- **位置**：S7 SKILL 行 93-107
- **S7 v2.0 规则**："不强制 100% = PASS"、"由 LLM 按业务实际写建议"
- **S8 SKILL 行 77-82 判决表**：
  ```
  S4风险点覆盖率=100% 且 S4异常树覆盖率=100% 且 结构完整性≥90% → PASS
  S4风险点覆盖率<100% 或 S4异常树覆盖率<100% → FAIL
  ```
- **问题**：
  1. S7 v2.0 声称"无硬阈值"，但 S8 用 PASS/FAIL 硬判决，而且 PASS 的条件是"100% 覆盖率"
  2. S8 行 82 "FAIL" 条件只检查覆盖率，不检查"结构完整性"——但 PASS 条件检查了三个维度（覆盖率×2 + 结构完整性≥90%）
  3. S7 行 107 "v1.0 旧规则已废弃"——但 S8 实际上在用"100% = PASS"的旧逻辑
  4. **S8 的核心逻辑建立在 S7 已废弃的规则上**

**矛盾 2：审查员 B 的"5 个指标"与 S8 的根因链路推理不对应**

- **位置**：S7 SKILL 行 74-89（审查员 B 5 个指标）+ S8 SKILL 行 84-109（根因链路推理）
- **S7 审查员 B 指标**：S4 风险点引用 / S4 异常树叶子引用 / is_assumed 填充率 / s4_reference 填充率 / applies_rule 填充率
- **S8 根因链路**：
  - 症状：S4 风险点覆盖率 < 100% → 根因：S5 未引用 S4 风险点
  - 症状：S4 异常树覆盖率 < 100% → 根因：S5 未引用 S4 异常决策树
  - 症状：结构完整性 < 90% → 根因：S6 步骤质量差
- **问题**：S8 的根因链路只覆盖了 3 个症状，但 S7 有 5 个审查指标。is_assumed / s4_reference / applies_rule 的填充率低时，S8 如何根因分析？SKILL 没有说明

**矛盾 3：S7 行 91 "S7 PASS → 无需根因分析"但行 134-135 S8 的"归档触发条件"说"S7 PASS 时触发归档"**

- S7 行 91：PASS → 无需根因分析，记录经验，输出 iteration 报告即可
- S8 行 134：S7 PASS 时触发归档
- **问题**：S7 PASS 时 S8 还需要归档经验，但不需要根因分析。PASS 时归档的内容是什么？SKILL 没有说清楚

#### 触发条件与产出不对应

**不对应 1：S7 的 §1.4 必读材料中包含 S4 business_flow，但 S7 行 53 说"S4 风险点全量覆盖（100%）是硬约束"**

- 行 53 声称"100% 是硬约束"，但 §"质量门禁（v2.0）"（行 93-107）明确说"不强制 100%"
- **根本矛盾**：这两段在同一份 SKILL 中

#### §1.4 必读指令

✅ **完整**。5 份材料，路径明确。

#### §1.5 决策 push 块

✅ **完整**。3 个 push 块（§1.5.1-§1.5.3），具体可操作。

#### 反例库 / 字段名清单

⚠️ **部分落地**。§1.5.1 引用了 §1.5.4 反例库（来自 S2 SKILL）。

#### 失败判定

✅ **清晰**。§"阶段入口"（行 31-39）包含前置材料检查。

#### 自检清单

⚠️ **分散**。没有统一的"提交前自检清单"。

#### 综合评分

- **评分**：5.0 / 10
- **关键问题**：
  1. **S8 的 PASS/FAIL 判决逻辑建立在 S7 已废弃的 v1.0 规则上**——这是最严重的跨阶段矛盾
  2. S7 "100% 硬约束"（行 53）与"无硬阈值"（行 93-107）在同一文件中并存
  3. S7 的 5 个审查指标与 S8 的 3 个根因链路不匹配
  4. S7 PASS 时的归档内容不明确

---

### S8 自迭代（aidocx-s8-self-iteration/SKILL.md）

#### 内部逻辑矛盾

**矛盾 1：S8 的判决逻辑是 S7 v1.0 硬判决，与 S7 v2.0 设计哲学直接冲突**

- **见 S7 综合评分矛盾 1 的详细分析**
- **核心问题**：S8 行 77-82 的判决表是 S7 v1.0 的产物。S7 SKILL 行 107 已明确废弃了"S4 风险点覆盖率=100% + 结构完整性≥90% = PASS"规则，但 S8 在用同样的规则做判决
- **实际效果**：S7 v2.0 说"覆盖率不强制 100%，LLM 写建议"，但 S8 拿到 S7 的结果后仍然用"100% = PASS"做判决。**S8 覆盖了 S7 v2.0 的宽松设计**

**矛盾 2：S8 行 78 "S4风险点覆盖率=100% 且 S4异常树覆盖率=100% 且 结构完整性≥90%"的 PASS 条件和行 82 "FAIL" 条件不对称**

- PASS 条件：覆盖率×2 + 结构完整性≥90%（3 项全满足）
- FAIL 条件：覆盖率×2 任一 < 100%（只检查覆盖率）
- **问题**：如果 S4 风险点覆盖率 = 100%、异常树覆盖率 = 80%、结构完整性 = 70%，满足 FAIL 条件但结构完整性不满足 PASS 条件。如果 S4 风险点覆盖率 = 80%、异常树覆盖率 = 100%、结构完整性 = 100%，也满足 FAIL 条件但结构完整性满足 PASS 条件。"结构完整性≥90%" 在 FAIL 条件中被忽略了——这是一个逻辑漏洞

**矛盾 3：S8 行 107-109 的根因链路图中缺少 S6 层的问题追溯**

- 行 108-109 根因层 3 列出了三个可能根因（S4→S5 衔接断裂 / S5 规则未明确 / S5 执行者遗漏）
- **问题**：没有列出"S6 步骤质量差导致覆盖率低"的可能根因。如果 S5 正确引用了 S4，但 S6 TC 没有覆盖这些 TP 所代表的场景，覆盖率也会低

#### 触发条件与产出不对应

**不对应 1：S8 §1.4 必读材料包含 6 份，但其中 S5 test_points.json 的角色不明确**

- S8 行 34-39：6 份前置材料
- **问题**：S8 在分析"is_assumed 字段填充率"时需要 S5 test_points；在分析"S6 步骤质量"时需要 S6 test_cases。S5 test_points.json 是"用于 S4 覆盖率分析"，但 is_assumed 是 S5 TP 的字段，S8 需要直接读 S5 test_points 来分析 is_assumed 填充率——这个用途在 §1.4 必读原因中没有写

#### §1.4 必读指令

⚠️ **路径不完整**。6 份材料，但行 55 写的是"S5 test_points"，而 §1.4 表格中的用途是"S5 层缺陷追溯的起点"，没有说明 S8 需要用 S5 TP 的 is_assumed 字段来分析 S7 的 is_assumed 字段填充率。

#### §1.5 决策 push 块

❌ **缺失**。S8 没有 §1.5 决策 push 块。LLM 在进行根因链路推理时没有引导。

#### 反例库 / 字段名清单

⚠️ **部分落地**。iteration.json 的字段（行 143-163）定义了根因分析的结构，但没有独立的反例库。

#### 失败判定

✅ **清晰**。行 41：材料缺失时生成失败报告。

#### 自检清单

❌ **缺失**。S8 没有自检清单。

#### 综合评分

- **评分**：4.0 / 10
- **关键问题**：
  1. **S8 的 PASS/FAIL 判决逻辑使用 S7 v2.0 已废弃的"100% 覆盖率"规则**——这是整个 SKILL 体系中最严重的跨阶段矛盾
  2. FAIL 条件中缺少"结构完整性 < 90%"的检查（与 PASS 条件不对称）
  3. 根因链路缺少 S6 层的问题追溯
  4. 缺少 §1.5 决策 push 块

---

## 按审查维度汇总

### §1.4 必读指令完整性

| 阶段 | 完整性 | 主要问题 |
|------|--------|---------|
| S1 | ✅ | — |
| S1.5 | ✅ | — |
| S2 | ✅ | — |
| S2.5 | ⚠️ | backlog.md vs backlog.json 模糊；需求对象无具体文件名 |
| S3 | ✅ | — |
| S4 | ✅ | — |
| S5 | ✅ | — |
| S6 | ✅ | — |
| S7 | ✅ | — |
| S8 | ⚠️ | S5 TP 的 is_assumed 字段分析用途未在必读原因中说明 |

### §1.5 决策 push 块

| 阶段 | 状态 | 质量 |
|------|------|------|
| S1 | ⚠️ | 反向契约像"须知"而非决策引导 |
| S1.5 | ❌ | **缺失** |
| S2 | ✅ | 6 个 push 块，具体可操作 |
| S2.5 | ❌ | **缺失** |
| S3 | ❌ | **缺失** |
| S4 | ✅ | 3 个 push 块，具体可操作 |
| S5 | ✅ | 6 个 push 块，具体可操作 |
| S6 | ❌ | **缺失** |
| S7 | ✅ | 3 个 push 块，具体可操作 |
| S8 | ❌ | **缺失** |

**5 个阶段缺少 §1.5 决策 push 块**：S1.5、S2.5、S3、S6、S8

### 反例库 / 字段名清单落地

| 阶段 | 状态 | 备注 |
|------|------|------|
| S1 | ⚠️ | 无独立反例库 |
| S1.5 | ⚠️ | quality_level 边界模糊作为隐含反例 |
| S2 | ✅ | §1.5.4 有 4 个具体反例；§1.5.5 有字段名清单 |
| S2.5 | ❌ | 缺失 |
| S3 | ❌ | 缺失 |
| S4 | ✅ | Epic 命名反例 + 异常树归属反例 |
| S5 | ⚠️ | 75 个旧 TP vs §1.5.4 编号列表关系不清晰 |
| S6 | ⚠️ | 有格式对比，无模块归属反例库 |
| S7 | ⚠️ | 引用 S2 的反例库 |
| S8 | ⚠️ | iteration.json 定义了结构，无独立反例库 |

### 失败判定逻辑清晰度

| 阶段 | 清晰度 | 备注 |
|------|--------|------|
| S1 | ✅ | 物料门禁失败 + 内容门禁（v2.0 已重构）|
| S1.5 | ✅ | P0 未填 / 无效内容 |
| S2 | ✅ | 12 项检查，逻辑明确 |
| S2.5 | ⚠️ | "材料缺失时"不够具体 |
| S3 | ⚠️ | 无输入审查表 |
| S4 | ✅ | 输入审查 + 质量门禁两层 |
| S5 | ✅ | 6 项检查 |
| S6 | ⚠️ | 无显式输入审查表 |
| S7 | ✅ | 前置材料检查 |
| S8 | ✅ | 材料缺失检查 |

### 自检清单可操作性

| 阶段 | 状态 |
|------|------|
| S1 | ⚠️ 无显式清单 |
| S1.5 | ⚠️ 分散在输入审查中 |
| S2 | ⚠️ 分散 |
| S2.5 | ❌ 缺失 |
| S3 | ❌ 缺失 |
| S4 | ⚠️ 分散 |
| S5 | ⚠️ 分散 |
| S6 | ✅ 5 项格式检查 |
| S7 | ⚠️ 分散 |
| S8 | ❌ 缺失 |

---

## 跨阶段数据契约审查

### S1 → S1.5 → S2 链路

| 字段 | S1 产出 | S1.5 消费 | S2 消费 | 一致性 |
|------|---------|-----------|---------|--------|
| `终版需求.md` | ✅ S1 草稿 | ✅ 完善 | ✅ 读取 | ⚠️ 草稿→完善的版本管理不清晰 |
| `clarification_checklist.md` | ✅ 含"问题需求"节 | ✅ 人工填写 | — | ⚠️ 同一文件的两个部分（生成/填写）状态管理不清 |
| `exit_permission.json` | — | ✅ 产出 | ✅ 读取 | ✅ |
| `requirement_objects.json` | ✅ | — | ✅ | ⚠️ **重名问题**：S1 和 S2 产出同名文件 |
| `SPECIAL_FLAG = PURCHASE_STRONG` | ✅ | — | — | ⚠️ 字段名在 S1.5 中变为 `strong_purchase_p0_resolved` |

### S2 → S3 → S4 链路

| 字段 | S2 产出 | S3 产出 | S4 消费 | 一致性 |
|------|---------|---------|---------|--------|
| backlog.md | ✅ | — | ✅ | ✅ |
| backlog.json | ✅ | — | ✅ | ⚠️ S2.5 消费时 backlog.md vs backlog.json 模糊 |
| `PAGE-XXX` 页面 ID | — | ❌ **未定义** | ✅ 推荐引用 | ⚠️ **P0**：S3 未规定 ID 格式 |
| 异常树叶子节点 | — | — | ✅ 产出 | ✅ |

### S4 → S5 → S6 链路

| 字段 | S4 产出 | S5 消费 | S6 消费 | 一致性 |
|------|---------|---------|---------|--------|
| `R-NNN` 风险点 ID | ✅ | ✅ 推荐引用 | — | ⚠️ S5 "推荐使用"，S7/S8 强制检查 |
| `S4-{EpicID}-X.Y.Z` 叶子 ID | ✅ | ✅ 推荐引用 | — | ⚠️ 同上 |
| `test_type_subclass` | — | ✅ 产出 | ✅ 消费 | ⚠️ S6 SKILL 行 227 示例 `BIZ.H_payment` 在 MODULES.md 无定义 |
| `applies_rule` | — | ✅ 产出 | — | ✅ |
| `is_assumed` | — | ✅ 产出 | — | ✅ |

### S5 → S7 → S8 链路

| 字段 | S5 产出 | S6 产出 | S7 审查 | S8 消费 | 一致性 |
|------|---------|---------|---------|---------|--------|
| test_cases.json | — | ✅ | ✅ | ✅ | ✅ |
| is_assumed 填充率 | — | — | ✅ 指标 | ✅ | ✅ |
| s4_reference 填充率 | — | — | ✅ 指标 | — | ✅ |
| overall_pass | — | — | ✅ | ✅ | ⚠️ **P0**：S7 v2.0 说"无硬阈值"，S8 用 PASS/FAIL 硬判决 |

---

## 问题汇总（按优先级）

### P0 级问题（必须修，阻塞执行）

| # | 问题 | 阶段 | 描述 |
|---|------|------|------|
| P0-1 | **S8 使用 S7 已废弃的硬判决** | S7/S8 | S7 v2.0（行 93-107）废弃"100% 覆盖率=PASS"，改由 LLM 写建议；S8（行 77-82）仍在用"100%=PASS，<100%=FAIL"的旧逻辑。S8 覆盖了 S7 v2.0 的宽松设计 |
| P0-2 | **S8 FAIL 条件不对称** | S8 | PASS 条件检查覆盖率×2 + 结构完整性；FAIL 只检查覆盖率。结构完整性 < 90% 但覆盖率 = 100% 时，S8 判定为 FAIL 但原因不准确 |
| P0-3 | **S3 PAGE-XXX 节点 ID 未定义** | S3 | S4（行 42）推荐引用 S3 的 PAGE-XXX，但 S3 没有规定 PAGE-XXX 的命名规则。S3/S4 数据契约不完整 |
| P0-4 | **S1/S2 `requirement_objects.json` 重名** | S1/S2 | 两个阶段产出同名文件，S2 覆盖 S1。没有 stage 字段或版本区分 |
| P0-5 | **S5 `s4_reference` "推荐使用"与 S7/S8 强制检查矛盾** | S5 | S5 行 81 "推荐使用" R-XX，但 S7/S8 把 s4_reference 填充率作为审查指标。隐性硬依赖与显式"推荐"不一致 |
| P0-6 | **S2.5 backlog.md vs backlog.json 路径不一致** | S2.5 | §"阶段入口"（行 66）说"S2 backlog.md"，§1.4（行 79）说"S2 backlog"（模糊）。S2 产出两个文件，哪个是 S2.5 真正需要的？ |
| P0-7 | **S5 反例库两套来源关系不清晰** | S5 | 行 127 的"75 个旧 TP"和 §1.5.4 的"HINT 1-8 / BIZ 1-10 / UI 1-5"编号体系是什么关系？LLM 无法判断该对照哪套 |
| P0-8 | **S6 `test_type_subclass` 示例值无定义** | S6 | S6 行 227 示例 `BIZ.H_payment` 在 MODULES.md 中 BIZ 子类枚举里没有对应值 |
| P0-9 | **S7 行 53 与行 93-107 在同一文件中矛盾** | S7 | 行 53 说"100% 是硬约束"，行 107 说"v1.0 旧规则已废弃/无硬阈值" |

### P1 级问题（建议修，影响质量）

| # | 问题 | 阶段 | 描述 |
|---|------|------|------|
| P1-1 | **5 个阶段缺少 §1.5 决策 push 块** | S1.5/S2.5/S3/S6/S8 | S1.5（exit_permission 生成）、S2.5（迭代规划）、S3（原型生成）、S6（TC 拓宽）、S8（根因分析）均无决策引导 |
| P1-2 | **S2 §1.5.6 "5 问质量 push"缺少可量化质量门槛** | S2 | "5 问质量"是引导但不是验收标准。如果 5 问都回答了但答案空洞，没有兜底规则 |
| P1-3 | **S2 HINT 不作为 Epic ID 前缀需在 §1.4 明确** | S2 | 行 317 单独说明"HINT 不作为 Epic ID 前缀"，但 §1.4 8 模块总表（行 304-313）列出了 8 个前缀，没有排除 HINT |
| P1-4 | **S1.5 quality_level MEDIUM 的 P2 部分填写比例未定义** | S1.5 | MEDIUM 要求 P2"部分填写"，但"部分"是多少？50%？80%？无定义 |
| P1-5 | **S8 根因链路缺少 S6 层追溯** | S8 | 行 108-109 的根因层 3 只列出 S4→S5 衔接问题，没有列出"S6 步骤质量差导致覆盖率低"的可能 |
| P1-6 | **S5 §1.5.2 和 §1.5.6 存在重复判定步骤** | S5 | 两个 push 块都要求"模块判定 + S4 引用"，内容重叠 |
| P1-7 | **S1 PURCHASE_STRONG "强制项"用词具有误导性** | S1 | 行 159 "强制项"但"不阻断 S1"，语义矛盾。应标注为"S1.5 门禁项" |
| P1-8 | **S3 是最短的 SKILL（89 行），缺少关键约束** | S3 | 缺少输入审查表、失败判定具体逻辑、自检清单 |
| P1-9 | **S6 优先级 P0 列表暗示 LOG 模块默认 P0** | S6 | 行 112 "日志"出现在 P0 列表中，与"LLM 自由决定"（行 116）矛盾 |
| P1-10 | **S7 的 5 个审查指标与 S8 的 3 个根因链路不匹配** | S7/S8 | is_assumed / s4_reference / applies_rule 填充率低时，S8 无法根因分析 |
| P1-11 | **S7 PASS 时归档内容不明确** | S7/S8 | S7 行 91 "PASS → 无需根因分析"，S8 行 134 "PASS → 触发归档"。归档的内容是什么？ |
| P1-12 | **S6 行 126 "全文搜索'引用'应为 0 处"是隐性硬指标** | S6 | 与"不设硬指标"（行 20-24）的哲学矛盾 |
| P1-13 | **S2.5 失败判定触发条件不够具体** | S2.5 | "材料缺失时"但哪些算缺失？project_config.json 不存在但 backlog 存在时算吗？ |
| P1-14 | **S8 行 55 S5 TP is_assumed 字段分析用途未在必读原因中说明** | S8 | S8 需要用 S5 TP 的 is_assumed 字段来分析 S7 的 is_assumed 字段填充率，但 §1.4 用途列没有写明这一点 |

### P2 级问题（小修小补）

| # | 问题 | 阶段 | 描述 |
|---|------|------|------|
| P2-1 | **S4 异常树叶子节点 ID 格式歧义** | S4 | 行 78 表中格式 `S4-{EpicID}-{seq}.{N}` 与行 153-165 树中格式 `S4-{EpicID}-1.1.1` 不一致 |
| P2-2 | **S2.5 自动化支持引用可能不存在的函数** | S2.5 | 行 125 `make_stage2_5_skill` 在 `ai_workflow/conversation_skills.py` 中是否存在？ |
| P2-3 | **S1 自动化代码标注"待对齐"** | S1 | 行 329 `requirement_reviewer_auto.py` 标注"仍按'4 项内容门禁'实现，待对齐本次重构" |
| P2-4 | **S5 TP JSON 字段名在 §1.5.5 和 §1.6 有轻微漂移** | S5 | `tp_id` 在 §1.5.5 必填但不在 §1.6；`module_judgment_basis` 在 §1.6 选填但不在 §1.5.5 |
| P2-5 | **S1.5 "强付费项 P0"字段名与 S1 不一致** | S1/S1.5 | S1 用 `SPECIAL_FLAG = PURCHASE_STRONG`，S1.5 用 `strong_purchase_p0_resolved` |
| P2-6 | **S2 §1.5.5 行 538 "任何字段名拼写错误 → 写错"表达歧义** | S2 | "写错"是指"算错误"还是"写错了"？建议改为"→ 判定为不合格" |
| P2-7 | **S1 行 329 自动化支持章节应该删除或更新** | S1 | 代码与 SKILL 不同步，继续引用会误导 |
| P2-8 | **S8 行 134-135 "归档触发条件"应移到 S8 SKILL 而非 S7 SKILL** | S7/S8 | 归档是 S8 的职责，触发条件应写在 S8 中 |

---

## 修复建议优先级

### 第一优先级（P0，阻塞执行）

1. **修复 S7/S8 判决逻辑矛盾**：S8 应该继承 S7 v2.0 的设计哲学，改为：
   - S8 不再使用 PASS/FAIL 硬判决
   - S8 基于 S7 的 LLM 审查建议做根因分析
   - 或：明确 S7 的 `overall_pass` 字段是 S8 的唯一判决依据，且 S7 v2.0 中 `overall_pass` 由 LLM 判定（不再基于 100% 覆盖率）

2. **定义 S3 PAGE-XXX 节点 ID 格式**：在 S3 SKILL 中增加：
   - ID 格式：`PAGE-{EpicID}-{N}`（如 `PAGE-CONFIG-001-1`）
   - 每个原型的 PAGE ID 与 S2 Story 关联

3. **解决 `requirement_objects.json` 重名**：在 S1 产出中加 `stage: "S1"` 字段；S2 产出中加 `stage: "S2"` 字段

4. **统一 S2.5 backlog 消费路径**：明确是 backlog.md 还是 backlog.json，或两者都读

5. **S5 反例库整合**：将行 127 的"75 个旧 TP"和 §1.5.4 的编号列表统一为一个反例库（可以是 JSON 格式，编号 + 描述 + 对应模块）

### 第二优先级（P1，影响质量）

6. **为 S1.5/S2.5/S3/S6/S8 增加 §1.5 决策 push 块**：每个阶段的核心决策点不同，需按阶段特点设计
7. **S2 §1.5.6 增加可量化质量门槛**：将"5 问质量"转化为可检查的质量指标
8. **S8 根因链路增加 S6 层追溯**：当 S5 正确引用 S4 但 S6 TC 质量差时，应追溯到 S6
9. **S7 行 53 与行 93-107 矛盾修复**：删除行 53 中的"100% 是硬约束"表述，统一为"覆盖率是参考指标，由 LLM 按业务判断"

### 第三优先级（P2，小修小补）

10. **S4 异常树叶子 ID 格式统一**：推荐使用树中的点号分隔格式（`S4-{EpicID}-1.1.1`）
11. **S6 `test_type_subclass` 示例修正**：将 `BIZ.H_payment` 改为 `BIZ.H`（BIZ 子类中 H = BIZ_PAYMENT）
12. **删除或更新 S1 行 329 自动化支持注释**：标注"待对齐"不如直接删除该段落或标注"（v2.0 重构中，待更新）"
