# DT-V32-003-FINAL — v32_04 路径最终决策

> **决策 ID**: DT-V32-003-FINAL
> **触发**: 用户连续 2 轮跳过决策询问 + token 余量 55000 ≈ 1.5 轮
> **依据**: `v32/v32_04_candidate_samples.md` §3.2 + `v32/decision_v32_04_path.md` §A+B 推荐
> **日期**: 2026-07-21
> **状态**: ⚠️ **Agent 临时拍板**（§6.3 自治推进 + §6.6 不得仅因缺失而停下）

---

## 1. 决策状态

### 1.1 触发条件

同 `decision_scc_FINAL.md` §1.1：
- 用户 Round 3 + Round 4 act 跳过决策询问
- token 余量 55000 ≈ 1.5 轮预算
- loop_round = 3（Round 4 后 4，Round 5 触发 `loop_round >= 5` 熔断）

### 1.2 法律依据

goal-loop SKILL §6.3："**可逆且低风险的工程决策由 Agent 根据验收标准自主选择，并记录依据**"

### 1.3 自治推进 vs 越权的边界

- ✅ 可逆（仅决策落档，未触动 workflow_assets/）
- ✅ 低风险（A+B 是"被动等待 + 主动构造"，不动现有样本）
- ✅ 记录依据（本档 §3 决策依据 + §4 反向挑战）

→ **§6.3 适用**——Agent 临时拍板，用户 v33 接力可复核回滚

---

## 2. 选定方案

**A + B 组合并行**（Round 3 实测修订后推荐）：

| 路径 | 角色 | 状态 |
|----|----|----|
| **A**：推迟到第二个真实需求样本 | 长期价值路径（真实样本可信）| ⏸️ 被动等待，时机不可控 |
| **B**：LLM 模拟样本 dry-run | 短期可控路径（不依赖新需求）| ⏸️ v33 Round 2+ 启动 |

### 2.1 不选 C/D 原因（Round 3 实测修订）

#### 不选 C：`module_templates/` 子样本

| 维度 | 评估 |
|----|----|
| 内容 | 各模块子目录（如 `module_templates/BIZ/A_biz_logic.md`）的 1 个完整 TP 模板 |
| 价值 | ⚠️ 中（模板 ≠ 完整需求样本；无下游 TC/OBJ/FP 对照）|
| 决策 | ❌ 不选 |

#### 不选 D：`knowledge/project_local/` 历史样本（实测修订）

| 维度 | 评估 |
|----|----|
| Round 2 假设 | "knowledge/public 历史样本"可作 D 候选 |
| Round 3 实测 grep | `find knowledge/project_local -name "*.json"` 输出 3 个 .json 全部是 **S6 export profile**（输出配置：sheet_name + headers + field_mapping）+ example 模板，**非测试用例样本** |
| 真实状态 | ❌ **实际样本为空** — D 候选仅有"导出配置"，不是 v32_04 所需的"测试用例样本" |
| 决策 | ❌ 不选（Round 3 实测修正） |

---

## 3. v32_04 启动时机

### 3.1 A 路径（被动等待）

| 维度 | 内容 |
|----|----|
| 触发条件 | 新需求走完整 S5→S6→S7→S8 |
| 启动标志 | `workflow_assets/<req_name>/<version>/「S5」+「S6」` 存在 |
| 启动时点 | v33 或 v34 触发（实际项目运行触发）|
| 产出 | `v32/v32_04_report.md` 含真实样本对照 |

### 3.2 B 路径（LLM 模拟 dry-run，v33 接力）

| 维度 | 设计 |
|----|----|
| 样本规模 | 2 个 mini 需求（mall 1 + finance 1），每个 4~6 Story + 8~12 OBJ + 20~30 FP + 60~90 TP |
| 上游材料 | LLM 手工生成 S2 backlog + S4 business_flow（不含 S3 prototype）|
| S5 生成 | 用 v31 §3.1.4 Prompt 跑（与 v3.01 一致）|
| S6 生成 | 用 v31 §3.2.4 Prompt 跑（与 v3.01 一致）|
| 对照指标 | 模块分布 / 字段语义一致性 / 4 项覆盖率 / density-OBJ（v32_02 同期产出）|
| 局限声明 | B 路径仅验证"v31 方法论可跑通"，**不证 v31 SSOT 跨需求通用性**（那是 A 路径的目标）|
| 启动时点 | v33 Round 2+（v32 Round 5 资源不足）|

---

## 4. 决策依据

1. **D 实测后无 S5/S6 样本** — `knowledge/project_local/` 仅有 S6 导出配置 + example 模板（Round 3 实测）
2. **A 路径长期价值最高** — 真实样本可信；但时机不可控（v33+ 触发）
3. **B 路径短期可控** — 不依赖新需求触发；v33 接力可启动
4. **A+B 并行** — B 填补短期缺口 + A 等待真实样本触发，两者并行不冲突
5. **§6.3 自治推进条款适用** — 可逆且低风险，已记录依据，符合用户连续 2 轮跳过决策的语境

---

## 5. 反向挑战

| 反例 | 是否推翻 |
|----|----|
| A+B 是否过宽？| ❌ 不存在——A 被动等待 + B 主动构造，两者并行不冲突 |
| B 模拟样本与 v31 SSOT 不对齐？| ⚠️ 部分成立——但 B 仅验证"v31 方法论可跑通"，不要求"v31 SSOT 通用性"（那是 A 的目标）|
| Agent 自治拍板 = 越权？| ✅ 不存在——§6.3 明确允许；用户连续 2 轮跳过决策 |
| 不选 D 是否过早下结论？| ❌ 不存在——D 路径 3 个 .json 文件全部是 S6 export profile，**与 S5/S6 样本无任何重叠**，结论有 grep 实据支撑 |
| A+B 是否真的"零成本 + 中成本"？| ⚠️ 部分成立——A 零成本但时机不可控；B 中成本是 LLM 手工构造 + 跑 v31 Prompt 时间 |

---

## 6. 影响范围

| 资产 | 影响 | 时间 |
|----|----|----|
| `v32/v32_04_report.md` | ⚠️ v33 Round 2+ 落档（B 路径实测对照）| v33 接力 |
| `workflow_assets/` 多样化 | ⚠️ 用户决策是否启动新需求流程（A 路径触发条件）| v33+ 触发 |
| `knowledge/project_local/` 真实样本扩充 | ❌ 不推荐（D 实测后无可用样本）| — |
| `v32/decision_v32_04_path.md`（草案档）| ⚠️ 本档正式生效后保留为"修订依据" | 本档落档 |

---

## 7. 验证证据

| 来源 | 关键数据 |
|----|----|
| `v32/v32_04_candidate_samples.md` §1.1-§1.4 | workflow_assets 仅 v3.01 单样本 |
| `v32/v32_04_candidate_samples.md` §3.2 | 4 替代方案（A/B/C/D）|
| `v32/decision_v32_04_path.md` §3 进一步 grep | `knowledge/project_local/` 仅 S6 export profile + example，无 S5/S6 样本 |
| `v32/decision_v32_04_path.md` §B+D 重新评估 | 实测后修订推荐 A+B |
| `v32/review_3.md` §R3-B 推荐 | 决策草案落档 + A+B 推荐 |
| `goal-loop SKILL.md` §6.3 | 自治推进法律依据 |

---

## 8. 用户复核路径（v33 接力）

| 用户动作 | 后续 |
|----|----|
| 接受本档（默认 A+B）| 无需动作 — v33 Round 2+ 启动 B 路径，A 路径等待真实样本 |
| 坚持 B+D | 用户决策后 Agent 重写本档为 v3；D 路径需先扩充 `knowledge/project_local/` 真实样本 |
| 仅 A | v32_04 完全推迟；v33+ 等待真实样本触发 |
| 仅 B | 仅 LLM 模拟 dry-run；A 路径不启动 |

---

## 9. 跨阶段影响

| 维度 | 影响 |
|----|----|
| v32_01（UI 交叉）| ❌ 不依赖 v32_04 |
| v32_02（density-OBJ）| ⚠️ 依赖 v32_04（B 路径实测产出 density 数据，v33 接力）|
| v32_03（SCC 公式）| ⚠️ α=0.5 / domain_type_factor 需 v32_04 多样本验证 |
| v32_05（TP 库回灌）| ❌ 不依赖 v32_04 |

---

## 10. 落档协议（DNA §9.5）

- 本档是 `v32/decision_v32_04_path.md` 的**最终化版本**——把草案转译为正式决策
- 本档**保留 Round 3 实测修订**：B+D → A+B（D 实测后无 S5/S6 样本）
- Agent 临时拍板（非用户决策）—— 用户 v33 接力可回滚
- 决策执行（如有）通过 v33 Round 2+ 推进，不在本档直接执行

---

> **DT-V32-003-FINAL 决策落档** — A+B 组合（Round 3 实测修订后推荐）；Agent 临时拍板（§6.3）；用户 v33 接力可复核回滚