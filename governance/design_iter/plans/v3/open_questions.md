# v3 开放问题（v3 实施期跟踪）

> 本文件跟踪 v3 实施期间新增的开放问题。Q-301~Q-303 已在 `decisions.json` 决策。
> v3 完成后，本文件所有问题转 v4 评估或关闭。

---

## Q-304：marker 校验

- **背景**：L3 hook 检测响应里是否含 "⚠️ DNA 自检未通过" 标记
- **风险**：Agent 滥写（"未通过" 内容空洞）或不写（漏标） → hook 失效
- **候选**：
  - A. marker 必须包含具体违规描述（"未通过：先动手 / 改动无影响范围 / 未做 3 问"）
  - B. marker 必须有引用文件路径
  - C. 不校验 marker，靠用户看到后追问
- **当前默认**：[ A ] marker 必须含 1 个具体违规描述
- **决策状态**：⏳ 待 v3.1 评估

---

## Q-305：临界点 3 次是否合适

- **背景**：D-303 决策"连续 3 次违规才 block"
- **风险**：3 次可能太松（Agent 连续 2 次违规后主动写"未通过"逃避第 3 次）
- **候选**：
  - A. 保持 3 次
  - B. 改 5 次（更松）
  - C. 改 2 次（更紧）
  - D. 配置化（`DNA_VIOLATION_BLOCK_THRESHOLD` 环境变量）
- **当前默认**：[ D ] 配置化
- **决策状态**：⏳ 待 v3.1 评估

---

## Q-306：AGENTS.md 是否加 3 问速查

- **背景**：D-302 决定 L2 用独立 `.mdc`；但 AGENTS.md 是"DNA 总纲"，是否也应该有 3 问入口？
- **候选**：
  - A. 不加（AGENTS.md ≤60 行优先）
  - B. 加 1 行超链接到 L2 mdc
  - C. 加 1 行速查（3 个问号）
- **当前默认**：[ B ] 加 1 行超链接
- **决策状态**：⏳ 待 v3.1 评估

---

## Q-307：hook 性能

- **背景**：`beforeSubmitPrompt` 是 hot path，每次响应都跑
- **风险**：marker 检测 + 状态文件读写 = 单次 < 10ms 还可接受，但 Agent 频繁提交时累计
- **候选**：
  - A. 不优化（< 10ms 可接受）
  - B. 加 5 秒短路（前 5 秒已检测就不重复）
  - C. 异步写日志（违规记录不阻塞响应）
- **当前默认**：[ C ] 异步写日志
- **决策状态**：⏳ 待 v3.1 评估

---

## Q-308：L1 vs L2 内容同步

- **背景**：AGENTS.md L17-20 写 3 问；DNA_3Q_CHECK.mdc 展开成可执行规则
- **风险**：AGENTS.md 改 3 问措辞 → DNA_3Q_CHECK.mdc 也要改
- **候选**：
  - A. 人工同步（靠 commit 流程）
  - B. 加 "双文件一致性 check" hook
  - C. 把 3 问从 AGENTS.md 彻底移走（只留 L2）
- **当前默认**：[ C ] 彻底移走
- **决策状态**：⏳ 待 v3.1 评估

---

## Q-309：违规是否上报给用户

- **背景**：L3 hook 检测到违规 → stderr 输出 `[DNA-WARN]`，Agent 看到但用户不一定看到
- **风险**：用户可能错过违规信号
- **候选**：
  - A. stderr only（用户从 terminal log 看）
  - B. 写一个 `workflow_assets/feedback_logs/dna_violations.jsonl`（让 S8 自迭代消费）
  - C. 同时 stderr + jsonl
- **当前默认**：[ C ] stderr + jsonl
- **决策状态**：⏳ 待 v3.1 评估

---

## v3 实施期新增问题跟踪

> v3 实施期间如发现新问题，追加到本节。

（暂无）

---

## Q-310：决策密度标准（v3.1 决策源）

- **背景**：v3.0 实施期单次改 8 文件 = ≥ 20 决策点；用户反馈"小狗先执行有偏差，人工审查成本高"
- **风险**：决策点过多 → 审查成本高 → 用户需逐项 ask Agent
- **候选**：
  - A. 立阈值（≤ 3 文件/响应）
  - B. 立阈值 + 决策表模板
  - C. 立阈值 + 决策表 + Agent 行为承诺
  - D. 不立阈值（沿用 v3.0 软约束）
- **当前默认**：[ C ] 阈值 + 决策表 + 行为承诺（v3.1 选 C）
- **决策状态**：✅ 已决 → D-304
- **落地**：`DNA_3Q_CHECK.mdc` §7 + `v3/PLAN.md` §6.5

---

## Q-311：阈值 3 文件/响应是否合适

- **背景**：D-304 阈值"单次响应 ≤ 3 文件改动"
- **风险**：3 可能太松（批量改效率低）或太紧（复杂任务拆不细）
- **候选**：
  - A. 保持 3
  - B. 改 5（更松）
  - C. 改 2（更紧）
  - D. 配置化（`DNA_DECISION_DENSITY_THRESHOLD` 环境变量）
- **当前默认**：[ D ] 配置化
- **决策状态**：⏳ 待 v3.2 评估

---

## Q-312：决策表是否要进 commit message

- **背景**：D-304 要求动手前列决策表；改完是否要把决策表写入 commit message？
- **风险**：不写 → 决策表只在响应里 = 不可追溯；写 → commit message 变长
- **候选**：
  - A. 不写（commit 保持简洁）
  - B. 写 1 行"决策表摘要"
  - C. 写完整决策表
- **当前默认**：[ B ] 写 1 行摘要
- **决策状态**：⏳ 待 v3.2 评估

---

## Q-313：决策密度违反是否要触发 L3 hook

- **背景**：v3.0 §0.4 论证 hook 防不住"误读"型违规；决策密度是"明知故犯"型
- **风险**：加 hook = 加复杂度；不加 = 决策密度 0 强制
- **候选**：
  - A. 不加（沿用 v3.0 L3 不动）
  - B. 加 L3 hook 检测"单次响应 > 3 文件改动"
  - C. 加 L3 hook + 走软记录（不 block）
- **当前默认**：[ A ] 不加（v3.0 §0.4 教训 + Agent 自承诺足够）
- **决策状态**：✅ **v3.1 实施翻案**（用户 02:10 拍板）——见 commit 4da9193 `dna_decision_density_check.py`
  - 翻案理由：决策密度是"可数"型（> 3 = 违规）→ hook 能防；区别于"误读"型
  - 实现：sessionEnd 事件 + 单次响应 Write/Edit 计数 ≥ 3 → 临界点 block（exit 1）
  - 默认阈值：3（可由 `DNA_DECISION_DENSITY_THRESHOLD` 环境变量覆盖）

---

## Q-314：决策密度物化声明 vs 实际 .mdc §7 内容的漂移

- **背景**：本档 L105 / L116 / `v3/PLAN.md §6.5.4` / `CHANGELOG.md [v3.1]` 章节均声明"决策密度已物化到 `DNA_3Q_CHECK.mdc §7`"，但**实际**读取 `.cursor/rules/DNA_3Q_CHECK.mdc`：
  - §1 4 问（一致/设计/全局/约束 vs 知识）
  - §7 是"行为承诺"——不含决策密度
  - **决策密度规则实际不存在于任何 `.mdc` 段**
- **真实状态**：决策密度只有 **L3 hook（`dna_decision_density_check.py`，commit 4da9193）** + v3 计划档描述；L2 约束文本从未同步
- **风险**：未来 Agent 引用 "DNA_3Q_CHECK.mdc §7 决策密度" 会引用到错误段（实际是行为承诺）
- **本档动作**：明确登记此漂移，**不擅自改历史档**（避免动 v3 完成判定）
- **决策状态**：⏳ 等待 v6/v7 重新对位 L1/L2/L3 三层

---

## Q-315：Q1 重写 — 用户挑战"5 分钟"硬解读

- **背景**：用户明确指出 Q1 "人能在 5 分钟内直接看懂" 会逼 Agent **缩减篇幅 + 遗漏边界**——属于"为了 5 分钟硬指标而降质"
- **用户重写骨架（直接复述）**：
  1. 不用指代符（"这个 / 它 / 上述" → 改具体名词）
  2. 说人话（不堆术语）
  3. 列执行清单给人工审查（不只写叙述）
  4. 基于已有实现推理和复用，不凭空猜测
  5. 不产出无根据结论——边界说不清就明说"未知"
- **影响范围**：
  - L1：`AGENTS.md` L19-21（重写第 4 条准则）
  - L2：`DNA_3Q_CHECK.mdc` §1（在 4 问末尾追加 Q5 人本可审查细则）
  - 任何 Agent 后续回答（执行清单 > 叙述、指代符 0 容忍）
- **本档动作**：先登记，**不立刻动 AGENTS.md / DNA_3Q_CHECK.mdc**
  - 原因 1：漂移（Q-314）未修之前，对位不清
  - 原因 2：Q1 是 4 准则之一，重写属"最高准则改动"——需 v6 单独方案
- **决策状态**：⏳ 等待 v6 重对位 DNA 时同步处理
- **后续最小改动路径**（用户授权后可动）：
  - 1：`AGENTS.md` L19-21 → 改"5 分钟"为上述 5 条新原则
  - 2：`DNA_3Q_CHECK.mdc` §1 → 4 问 → 5 问 + Q5 引用 AGENTS.md
  - 3：CHANGELOG.md → 加 `[v6 候选] DNA Q1 重写` 章节
