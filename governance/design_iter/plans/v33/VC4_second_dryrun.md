# VC4 — 多项目样本 dry-run（第二轮）

> **VC**: VC4（v33 Round 18）
> **来源**: v32 §3.4 v32_04 立项
> **状态**: 🟡 BLOCKED（等待用户提供 ≥2 个额外样本）

---

## 1. 前置条件

**阻塞项**：需要用户提供 ≥2 个额外样本（来自不同业务领域的真实需求文档）。

**样本要求**：

| 要求 | 说明 |
|------|------|
| 数量 | ≥ 2 个 |
| 领域 | 最好是不同业务领域（如电商、金融、游戏、CRM 等）|
| 格式 | `.docx` / `.doc` / `.md` 原始需求文档 |
| 存放位置 | `resource/` 目录 |

**当前状态**：仅 v3.01（游戏道具商城系统）可用。

---

## 2. 预期改进（基于 Round 10-14 治理）

### 2.1 已完成的治理动作

Round 11-13 已完成以下修复：

| 缺陷 | 治理动作 | 完成轮次 |
|------|---------|---------|
| 规则C（steps ≥ 2 = 0%）| STAGE_S6.mdc + S6 SKILL C1 强制 steps ≥ 2 步 | Round 11-12 |
| density-OBJ（22.2%）| S5 SKILL 新增 per-OBJ 4类型约束章节 | Round 11 |
| R-ID 引用率 0% | S5 SKILL Push4 + Q5 双修订 | Round 13 |

### 2.2 预期结果（第二轮 dry-run）

基于上述治理动作，预期第二轮 dry-run 结果：

| 维度 | v3.01 基线（Round 10）| 治理后预期 | 提升来源 |
|------|---------------------|-----------|---------|
| 规则C（steps ≥ 2）| 0% | **≥ 50%** | S6 SKILL C1 强制 |
| density-OBJ | 22.2% | **≥ 50%** | S5 SKILL 4类型约束 |
| R-ID 引用率 | 0% | **≥ 30%** | S5 SKILL Push4 + Q5 |

> **注意**：预期值为估算。实际结果取决于生成器是否正确执行约束。

---

## 3. 执行计划（样本就绪后）

### 3.1 阶段 1：样本准备（用户）

1. 用户上传 ≥2 个额外样本到 `resource/` 目录
2. 运行 S1 评审 + S2 拆解 + S5 生成 TP
3. 确保 TP 数据可用于 dry-run 分析

### 3.2 阶段 2：dry-run 分析（Agent）

1. 读取新样本的 `test_points.json` 和 `test_cases.json`
2. 运行字段语义一致性检查（规则A/B/C）
3. 计算四项覆盖率（OBJ/FP/S4叶子/R-ID）
4. 计算 density-OBJ 指标
5. 输出 dry-run 报告

### 3.3 阶段 3：结果判定（LLM + 用户）

1. 对比 Round 10 基线与第二轮结果
2. 判定方法论通用性改善程度
3. 识别仍需治理的缺陷

---

## 4. 验收标准

| 标准 | 判定 |
|------|------|
| 规则C（steps ≥ 2）≥ 50% | ✅ PASS |
| density-OBJ ≥ 30% | ✅ PASS |
| R-ID 引用率 ≥ 30% | ✅ PASS |

**全部通过**：VC4 ✅ PASS
**部分通过**：VC4 🟡 PARTIAL
**全部未通过**：VC4 ❌ FAIL

---

## 5. 开放问题

| Q | 状态 |
|---|------|
| Q-801: 额外样本来自哪些业务领域？ | 用户决策 |
| Q-802: 是否需要 S1+S2+S5 全流程跑完才能 dry-run？ | 建议全流程，至少需要 S5 TP 数据 |
| Q-803: 第二轮 dry-run 是否需要与 v3.01 样本对比？ | 是，建议对比分析 |
