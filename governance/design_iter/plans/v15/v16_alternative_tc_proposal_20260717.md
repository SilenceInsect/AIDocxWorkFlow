# TP/TC 方案对比决策表 — v16 锚点方案 vs 用户提交的"终极标准化最优方案"

| 项 | 值 |
|---|---|
| 决策日期 | 2026-07-17 |
| 触发来源 | `~/Downloads/TP-TC 终极标准化最优方案（零冲突、全链路一致、Excel原生适配）.md` |
| 涉及约束 | `.cursor/rules/STAGE_S5_TEST_POINTS.mdc` §1.9 / §1.4；`.cursor/rules/STAGE_S6_TEST_CASES.mdc` §1.7 / §1.4 |
| 决策状态 | 🟡 待用户拍板 |

---

## 1. 背景

用户提供一份"TP-TC 终极标准化最优方案"（v17 候选），主张：
- 取消文本锚点【OBJ - FP】
- `fp_name` 改为 LLM 自创的中性功能名（而非 S2 fp_desc 逐字相等）
- 文本字段（title/description/test_scenario）改为纯场景描述
- Excel 导出完全结构化字段映射

## 2. 冲突清单（10 项）

### 🔴 硬阻断（6 项）

| # | 冲突项 | 方案主张 | 当前 SSOT | 阻断原因 |
|---|---|---|---|---|
| 1 | `fp_name` 字段语义 | LLM 自创 | S5 §1.9.4: 100% 原样 S2 fp_desc | v16 v2 硬门禁 100% 校验 |
| 2 | `title` 锚点 | 不带【】 | S5 §1.9.1: 必须【OBJ - FP】开头 | L1 v2 锚点格式校验 |
| 3 | `description` 锚点 | 自然语言 | S5 §1.9.1: 必须【OBJ - FP】开头 | 同上 |
| 4 | TP 锚点一致性 | 取消锚点 | S5 §1.9.2: 两处锚点完全一致 | L1 v2 第 3 项 |
| 5 | TC test_scenario 锚点 | 不带锚点 | S6 §1.7.1: 必须【OBJ - FP】开头 + 与 TP title 完全一致 | L1 v2 继承性校验 |
| 6 | L1 校验策略 | 只校验字段精准 | v16 v2: 7 项校验（锚点 + 字段 + 逐字比对） | 推翻 L1 整套脚本 |

### 🟡 类型不兼容（4 项）

| # | 冲突项 | 方案 | 当前 |
|---|---|---|---|
| 7 | TC `tp_reference` 字段 | 新增 | 当前无此字段（仅 s5_ref） |
| 8 | `test_method` 类型 | 数组 | 字符串 |
| 9 | `steps` 结构化 | 数组 [{step_num, action}] | 字符串，最大 8 步 |
| 10 | `preconditions` 结构化 | 数组 | 字符串 |

## 3. 方案可取之处（部分采纳空间）

| 优点 | 采纳方式 | 影响范围 |
|---|---|---|
| Excel 结构化字段映射 | ✅ 可独立采纳 | 仅限 `knowledge/project_local/<proj>/s6/export_profiles/` 导出层 |
| `steps[]` 结构化数组 | ⚠️ 仅作**导出格式** | Excel 生成时展开 |
| `test_method[]` 数组 | ⚠️ 仅作**导出格式** | 同上 |
| 取消锚点 | ❌ 不可采纳 | 推翻 v16 v2 + DNA §1 Q1 |

## 4. 推荐路径（决策表）

### 选项 A：完全采纳方案（推 v17）

- 改动范围：S5 §1.9 + S6 §1.7 + L1 脚本 + 87 TP + 87 TC 全部重写 + Excel 导出层
- 收益：字段溯源 + Excel 结构化
- 风险：推翻 v15/v16 决策；v3.01 全部 L1 回归失败；多文件改动违反 §9.1
- **判定**：违反 DNA §1 准则 1（一致性）+ 准则 3（全局考虑）

### 选项 B：部分采纳（推荐 ✅）

- 改动范围：仅 Excel 导出层（`knowledge/project_local/<proj>/s6/export_profiles/test_cases.export.json`）做结构化映射
- 收益：Excel 导出 100% 稳定 + 保留 L1 100% 通过
- 风险：0
- **判定**：满足 §1 一致性 + §9.1 ≤3 文件

### 选项 C：现状不动

- 改动范围：0
- 风险：Excel 导出仍依赖文本截取（仅导出层问题，非产物问题）

## 5. 决策落档

- 落地文件：本档
- 约束文件改动：0（按用户要求）
- 待批行动：
  - [ ] 用户确认采纳选项 A/B/C
  - [ ] 选项 A → 需走 governance/design_iter/plans/v17/ 走完整方案迭代
  - [ ] 选项 B → 在 `knowledge/project_local/游戏道具商城/s6/export_profiles/` 加结构化导出 schema
  - [ ] 选项 C → 关单

## 落档协议执行记录（§9.5）

- 修改文件数：0（仅落档分析文档）
- 单次响应工具调用数：≤ 10
- 决策点：3 项选项已显式列出（A/B/C）
- 改动前已 Read 上游产物：用户提交的 .md 完整读完