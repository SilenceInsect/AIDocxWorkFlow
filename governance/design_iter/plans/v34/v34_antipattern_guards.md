# v34 — 臆造防御机制（草稿）

> **本文件状态**：🚧 **DRAFT** — 不进入 goal-loop Act 阶段。
>
> **触发原因**：参考 `link_module_chain_map.md` §7 自我纠正日志（**Agent 3 轮被否**）—— 必须有显式防御机制。
>
> **核心策略**：**用户原话强引用 + 错误映射清单 + Round 校准日志**——三道防线。

---

## 1. v34 §7 历史教训（自我纠正日志回顾）

| 轮次 | Agent 臆造 | 用户纠正 | 防御启示 |
|---|---|---|---|
| **Round 1** | "关联 = 测试可追溯 4 层链 TC→TP→OBJ→原需求" | 9:23 否 — 关联是功能 ↔ 功能（跳转/从属/数据传递/触发链）| **没有用户原话依据的"概念扩展"必被否** |
| **Round 2** | "回归 = 子功能模块静态范围扩展" | 9:26 否 — 改为变更视角（触发→波及→回归验证）| **没有用户原话依据的"实施方式扩展"必被否** |
| **Round 3** | （本轮合规拍板）按用户多层副本实例落地变更视角 | ✅ 通过 | **用户原话实例化才安全** |

**3 轮校准的核心规律**：
1. Round 1 / 2 都是 Agent 把"概念" / "实施方式"做主动外推
2. **Agent 外推 = 臆造 = 必被否**
3. **必须等用户给原话实例，才落档**

---

## 2. 三道防线（本方案）

### 2.1 第一道防线：**用户原话强引用**

> **强制**：本方案（`v34_8role_contracts.md` + `v34_S1_S2_integration_points.md`）每条契约 / 每个接入点都附**用户原话引用**（附在段末"用户原话 #N"）。

**审计检查清单**：
- [ ] 每条契约段都有"用户原话 #N"标注
- [ ] 每条原话都来自用户 2026-07-22 给过的 7 条原话（不是 Agent 自创）
- [ ] 每条原话引用都用 `>` 引用块 + 时间戳（保留可追溯）

**违反处理**：
- 没有原话引用的契约 = **强制删除**（不留为"待补"）
- 原话引用错误（引申 / 改写）= **强制回退**

### 2.2 第二道防线：**错误映射清单**

> **参考 v34 `link_module_chain_map.md` §3 错误映射**

| ❌ 错误映射 | ❓ 触发原因 | ✅ 正确做法 |
|---|---|---|
| ❌ 8 角色 = "评审加一栏 25% 权重" | 把"8 角色并行任务"窄化为"通用评审维度加权" | 8 角色 = **11 项行为契约子系统**（角色创造 + 行为创造 + 行为约束 + 行为推进 + 行为计划 + 行为执行规范 + 方法论 + 行为结果审计 + 反思 + 迭代 + 经验沉淀 + 模板维护）|
| ❌ 8 角色 = "S1+S2 阶段工具" | 把 8 角色限于 S1/S2 两个阶段 | 8 角色 = **全生命周期子系统**（S1 预审 + S2 拆解 + S3-S6 校验 + S8 沉淀）|
| ❌ 8 角色 = "Agent 自主深度思考" | 让 Agent 自己决定思考深度 / 思考产出 | 8 角色 = **基于用户原话 + 模块模板的约束角色**，思考产物形态受 §1.2 约束 |
| ❌ 8 角色可自创 / 自合 / 跨模块自由 | 越界创造"第 9 角色" / 合并"CONFIG+UI" / 自由跨界 | 8 角色 SSOT（`.cursor/MODULES.md` §4.X）—— **不允许 Agent 修改** |
| ❌ OBJ 新增 `template_refs` / `requirement_diff` 字段破坏 9 字段 SSOT | 把新字段塞进 OBJ 9 字段内 | 9 字段 SSOT 不动 —— 新增字段挂在 OBJ **顶层**（与 9 字段平级）|
| ❌ `module_gap_analysis.md` 替代 `clarification_checklist.md` | 把"8 模块覆盖预审"看作 P0 清单本身 | `module_gap_analysis.md` 是 **S1 阶段的预审产物** → **喂给 S1.5 的人工澄清输入**——`clarification_checklist.md` 仍是 S1.5 的产出 |

### 2.3 第三道防线：**Round 校准日志 + 用户拍板机制**

> **强制**：本方案不直接进入 goal-loop Act 阶段。**先 Draft，等用户对 §5 10 项 Q 逐项拍板后才进入 Act**。

**Round 校准日志模板**（每次拍板后填）：

| 轮次 | 时间 | Agent 提议 | 用户拍板 | 拍板依据 |
|---|---|---|---|---|
| Round 1 | 2026-07-22 | 8 角色 = "评审加一栏 25% 权重" | ❌ 否 → 改为 11 项行为契约 | 用户原话 #7 |
| Round 2 | （待） | （待） | （待） | （待）|

---

## 3. Agent 不该做的事（黑名单）

> **参考 DESIGN_AND_EXECUTION_STANDARDS.mdc §5.3 全局违规认定**

| 违规类型 | 描述 | 本方案具体场景 |
|---|---|---|
| 跨模块直接调用 | 未通过标准化接口 | 8 角色跨界思考（如 BIZ 角色自行决定 LINK 字段） |
| 忽略失败报告 | 未生成 `fail_report_*` | 8 角色产物缺漏但不报 |
| 版本号混乱 | 未遵循版本规范 | v34 子方案破坏现有 v33 流程 |
| 路径硬编码 | 未使用 `Path` 对象 | 8 角色模板路径硬编码 |
| **臆造扩展** | **未基于用户原话的扩展** | **Agent 自创"8 角色创造第 9 个角色"** |
| **直接入库模板** | **Agent 未经审核写公共知识库** | **8 角色经验沉淀直接修改 `module_templates/<MODULE>/X_*.md`** |
| **自决矛盾** | **不汇报用户自行解决冲突** | **策划需求和通用经验矛盾时 Agent 自决采纳哪一方** |
| **自我循环迭代** | **无外部信号驱动自迭代** | **8 角色"反思 → 迭代"无用户触发** |

---

## 4. 用户拍板前的"安全区"清单

> **草稿阶段（未拍板前），以下行为被严格禁止**：

| 行为 | 是否允许 | 原因 |
|---|---|---|
| 改 `STAGE_S1_REVIEW.mdc` | ❌ 禁止 | 用户未拍板 §5 10 项 Q |
| 改 `STAGE_S2_BREAKDOWN.mdc` | ❌ 禁止 | 同上 |
| 改 `module_templates/<MODULE>/X_*.md` | ❌ 禁止 | SSOT（DESIGN_AND_EXECUTION_STANDARDS.mdc §0.1）|
| 改 `.cursor/MODULES.md` | ❌ 禁止 | SSOT |
| 创建 `module_gap_analysis.md` 实文件 | ❌ 禁止 | 仅方案层（v34_*.md），不动产物 |
| 创建 `template_refs` 字段在 S2 产物 | ❌ 禁止 | 仅方案层 |
| goal-loop Act 阶段执行 | ❌ 禁止 | 等用户拍板 |
| v34_*.md 文件互相引用 / 互相依赖 | ✅ 允许 | 方案内部 |
| 起草 `v34_<stage>_plan_<role>.md` 模板 | ✅ 允许 | 草稿阶段可准备模板 |
| 起草 `v34_<stage>_audit_<role>.md` 模板 | ✅ 允许 | 同上 |
| 起草 `v34_<stage>_reflection_<role>.md` 模板 | ✅ 允许 | 同上 |
| 创建 `module_templates/<MODULE>/_experience.md` 占位 | ❌ 禁止 | 等 Q-003 拍板 |
| 创建 `goal_parallel_executor.py` 真实代码 | ❌ 禁止 | 等 Q-002 拍板 |

---

## 5. 落地流程（用户拍板后）

```
┌────────────────────────────────────────────────────────────┐
│  Step 1：用户对 §5 10 项 Q 逐项拍板                            │
│                                                              │
│  Step 2：根据拍板结果，修订 3 个 v34_*.md 草稿文件              │
│    - v34_8role_subsystem_PLAN.md（更新 §5 改动清单）           │
│    - v34_8role_contracts.md（更新对应 Q 选项）                │
│    - v34_S1_S2_integration_points.md（更新对应 Q 选项）       │
│                                                              │
│  Step 3：用户对修订版拍板（VC6 PASS）                          │
│                                                              │
│  Step 4：进入 goal-loop Act 阶段（Round 1）                   │
│    - 改 STAGE_S1_REVIEW.mdc（§1.2.A）                       │
│    - 改 STAGE_S2_BREAKDOWN.mdc（§2.2.A）                    │
│    - 落档 audit_1.md + review_1.md                          │
│                                                              │
│  Step 5：goal-loop Round 2+                                  │
│    - 跑真实样本（如 v3.02）                                    │
│    - 校准 8 角色行为契约                                       │
│    - 优化 template_refs + requirement_diff 字段              │
│                                                              │
│  Step 6：v34 子方案 achieved → 归档                           │
└────────────────────────────────────────────────────────────┘
```

---

## 6. 与 v33 / v34 既有工作的关系

| 既有工作 | 本方案是否复用 | 备注 |
|---|---|---|
| `goal_parallel_executor.py` | ⚠️ 待 Q-002 拍板 | 真并行的实现 |
| v33 §10 五段式 goal-loop | ✅ 复用 | §2.2 / §2.8 / §2.9 / §2.10 = goal-loop 5 段 |
| v33 §11 audit + review 模式 | ✅ 复用 | `audit_<round>.md` + `review_<round>.md` |
| v34 `link_module_chain_map.md` §7 自我纠正日志范式 | ✅ 复用 | 本文件 §1 + §2.3 直接借鉴 |
| v34 LINK 双职责（R5 LINK 角色承担） | ⚠️ 待 Q-006 拍板 | 合并 vs 分离 |
| v33 Round summary 模板 | ✅ 复用 | 校准日志记录格式 |

---

**最后修改**：2026-07-22 00:45 草稿
**goal-loop 轮次**：未启动（**等用户对 §5 10 项 Q 拍板**）

---

## 附：与 v33 `v33_Round2_summary.md` §1.1 拍板模式对齐

参考 v33 Round 2 拍板流程：
- 4 项 FINAL 决策（DT-V32-001/002/003/004）—— 用户逐项拍板
- VC6 PASS — 4 项全部用户确认接受

本方案的拍板目标 = **§5 10 项 Q 全部用户逐项拍板** + VC6 PASS。