# v34 — S1 预审接入点 + S2 拆解接入点（草稿）

> **本文件状态**：🚧 **DRAFT** — 不进入 goal-loop Act 阶段。
>
> **严格基于用户原话**：所有接入点都附用户原话引用。
>
> **三阶段边界**：见 `AGENTS.md` §三阶段核心边界（SSOT）。**S1.5 是人工决策阶段**，本方案不动 S1.5。

---

## 1. S1 接入点（8 角色预审）

> **用户原话 #1**："S1 的需求拆解，需要有一个拆解的角色，角色按规则进行拆解"
>
> **用户原话 #2**："S1 需要做一个多角色并行任务，8 个模块对应 8 个角色"
>
> **用户原话 #4**："补齐策划需求没有的，明确策划需求有的，找到策划需求和通用经验矛盾的"
>
> **用户原话 #5**："8 模块的模版，对审查也是有大的启发和导向作用的"

### 1.1 当前 S1 痛点（基于 `STAGE_S1_REVIEW.mdc` 4 维度）

|| 维度 | 权重 | 当前做法 | 痛点 |
|---|---|---|---|---|
| 质量闭环完整性 | 30% | 强付费项 3 段识别（发奖/排行/订单/兑换/VIP/战令）| ❌ 关键字识别，没有按模块深度展开 |
| 功能覆盖度 | 25% | 每个功能 F 有测试场景 | ❌ 没有按模块视角检查"每个模块的覆盖度" |
| 可测试性 | 25% | GM 命令 / 测试脚本 | ⚠️ 部分按模块（如 GM 命令涉及 BIZ/AUX）|
| 一致性 | 20% | 角色 / 功能 / 验收标准 | ❌ 没按 8 模块交叉检查 |

**核心痛点**：S1 当前是"通用化评审"——**没用 8 模块视角预审**。结果：**S1 阶段漏掉的模块视角到 S2 才暴露，导致 S2 返工**。

### 1.2 S1 8 角色预审接入点（草稿）

> **核心改动**：新增 5 维度 **"8 模块覆盖预审"**（用户原话 #5："对审查有大的启发和导向作用"）。

#### A. 评审维度调整（5 维度）

| # | 维度 | 权重 | 评审要点 | 用户原话 |
|---|---|---|---|---|
| 1 | **质量闭环完整性** | 20% | 强付费项 3 段 | — |
| 2 | **功能覆盖度** | 20% | 每个功能 F 有测试场景 | — |
| 3 | **可测试性** | 15% | GM 命令 / 测试脚本 | — |
| 4 | **一致性** | 15% | 角色 / 功能 / 验收标准 | — |
| **5** ⭐ 新增 | **8 模块覆盖预审** | **30%** | **每个功能 F 用 8 视角审：哪些模块覆盖到 / 哪些模块漏了 / 哪里和通用经验矛盾** | **用户原话 #4："补齐策划需求没有的，明确策划需求有的，找到策划需求和通用经验矛盾的"** |

> **总权重 100%**：从原 4 维度（30/25/25/20）调整为 5 维度（20/20/15/15/30），**新增维度权重最大**——因为这是 S1 阶段最大的漏点。

#### B. S1 8 角色并行预审（v33 §10 goal-loop 模式）

> **用户原话 #2**："S1 需要做一个多角色并行任务"

```
┌────────────────────────────────────────────────────────────┐
│  S1 启动：8 角色并行深度阅读原始需求文档                          │
│                                                              │
│  R1 CONFIG-专家 ─→ 读 module_templates/CONFIG/*            │
│  R2 UI-专家     ─→ 读 module_templates/UI/*                │
│  R3 BIZ-专家    ─→ 读 module_templates/BIZ/*               │
│  R4 AUX-专家    ─→ 读 module_templates/AUX/*               │
│  R5 LINK-专家   ─→ 读 module_templates/LINK/*              │
│  R6 SPECIAL-专家 ─→ 读 module_templates/SPECIAL/*          │
│  R7 LOG-专家    ─→ 读 module_templates/LOG/*               │
│  R8 HINT-专家   ─→ 读 module_templates/HINT/*              │
│                                                              │
│  ↓ 每角色输出 1 份"模块预审报告"                                 │
│  ↓ 8 份报告汇聚到 → clarification_checklist.md                │
└────────────────────────────────────────────────────────────┘
```

#### C. S1 新增产出：`module_gap_analysis.md`

> **用户原话 #4**："补齐策划需求没有的，明确策划需求有的，找到策划需求和通用经验矛盾的"

| 功能 F | CONFIG | UI | BIZ | AUX | LINK | SPECIAL | LOG | HINT | 漏模块 / 矛盾 |
|---|---|---|---|---|---|---|---|---|---|
| F3 购买流程 | ✅ 提到价格表 | ✅ 商城页 | ✅ 扣款 | ❌ 没提本地缓存 | ❌ 没提跨服 | ⚠️ 防刷未细化 | ❌ 没提资产流水 | ✅ 飘字 | **漏 4 块** |
| F5 VIP 商城 | ✅ 提到 VIP 配置 | ✅ | ✅ | — | ⚠️ 没提跨服 VIP 权益 | — | ❌ 没提 VIP 状态变更埋点 | ✅ | **漏 1 矛盾 1** |
| ... | ... | ... | ... | ... | ... | ... | ... | ... | ... |

**这份 `module_gap_analysis.md` 直接喂给 S1.5 的 P0 列表**——人工填澄清时，看哪些模块漏了要决策"补 / 不补"。

#### D. S1 现有 6 产出不变（兼容性 SSOT）

| # | 文件 | 是否动 |
|---|---|---|
| 1 | `gm_commands.md` | ❌ 不动 |
| 2 | `test_coverage.md` | ❌ 不动 |
| 3 | `planning_acceptance.md` | ❌ 不动 |
| 4 | `quality_loop_report.md` | ❌ 不动 |
| 5 | `role_definitions.md` | ❌ 不动 |
| 6 | `clarification_checklist.md` | ⚠️ 增 "8 模块覆盖 P0" 段（基于 `module_gap_analysis.md`）|
| **7** ⭐ 新增 | **`module_gap_analysis.md`** | ✅ 新增（8 模块覆盖矩阵）|

#### E. S1 失败报告补充

`fail_report_S1.md` 增加"8 模块覆盖预审失败"维度（如某功能 F 漏 5 个以上模块但策划拒绝补 → 触发三步自问）。

### 1.3 S1 → S1.5 衔接

> **AGENTS.md 三阶段边界**：S1.5 是"人工决策阶段"——本方案**不改变 S1.5 的人工定位**。

```
S1 输出 7 文件（含 module_gap_analysis.md 新增）
 ↓
S1.5 读取 module_gap_analysis.md 的"漏模块"清单
 ↓
人工决策：补 / 不补 / 灰度 / 弃用
 ↓
exit_permission.json 增加 "module_gap_decisions": [...]
 ↓
S2 读取 exit_permission + module_gap_analysis → 启动 8 角色拆解
```

---

## 2. S2 接入点（8 角色拆解）

> **用户原话 #2**："8 个模块对应 8 个角色，每个角色按自己的模块，对文档进行深度阅读和思考，找到属于自己的需求内容"
>
> **用户原话 #3**："需求对象常见的背景、状态、边界等等，都可以在模块的 A～XXX 的模板说明里面找到启发"
>
> **用户原话 #4**："补齐策划需求没有的，明确策划需求有的，找到策划需求和通用经验矛盾的"

### 2.1 当前 S2 痛点（基于 `STAGE_S2_BREAKDOWN.mdc` 800 行）

**核心痛点**：S2 已有"按模块拆 OBJ"机制（5 条拆分判定规则 §第三层 293-299），但：
- **OBJ 拆解靠 LLM 自觉读模板**（你说"我看了 8 模块以后"——LLM 不一定看了）
- **5 字段派生链只到 FP，不追溯到模板**
- **"策划需求 vs 通用经验矛盾"无处归档**

### 2.2 S2 8 角色拆解接入点（草稿）

#### A. S2 OBJ 9 字段保留 + 新增 2 字段（兼容性 SSOT）

> **STAGE_S2_BREAKDOWN.mdc §第四层 9 字段是 SSOT，不动**。**新增 2 字段挂在 OBJ 顶层**（不在 9 字段内）：

| 新字段 | 类型 | 内容 | 用户原话 | 校验 |
|---|---|---|---|---|
| `template_refs` | `[]` | 该 OBJ 引用了哪些 `<MODULE>/<X>_*.md` 哪一段 | 用户原话 #3："可以在模块的 A～XXX 的模板说明里面找到启发" | `l1_s6.py` 增非空校验（路径必须存在）|
| `requirement_diff` | `{}` | 策划需求 vs 通用经验对比（3 栏：业务定制 / 通用补全 / 矛盾）| 用户原话 #4："补齐策划需求没有的，明确策划需求有的，找到策划需求和通用经验矛盾的" | `consistency_check.py` 增结构校验 |

#### B. `template_refs` 字段结构

```json
{
  "obj_id": "OBJ-001",
  "obj_name": "商城首页布局与控件",
  "belong_module": "UI",
  // 9 字段保留（SSOT）
  "scene": "...",
  "input": "...",
  "normal_flow": "...",
  "exception_flow": "...",
  "data_change": "...",
  "output_display": "...",
  "verify_method": "...",
  // ... feature_points[], 派生链
  
  // 新增字段（v34 草稿）
  "template_refs": [
    {
      "path": "knowledge/public/module_templates/UI/A_control_basic.md",
      "section": "## 典型场景",
      "used_for": "FP-001-01 热门道具展示"
    },
    {
      "path": "knowledge/public/module_templates/UI/E_animation.md",
      "section": "## 边界陷阱",
      "used_for": "FP-001-03 滚动动画边界"
    }
  ]
}
```

#### C. `requirement_diff` 字段结构

```json
{
  "obj_id": "OBJ-001",
  "obj_name": "商城首页布局与控件",
  "requirement_diff": {
    "策划独有（明确有的）": [
      "首页布局：左导航 + 右商品列表 + 顶部 Banner"
    ],
    "策划没写（通用经验补）": [
      "首页加载失败兜底（无网络/接口超时）",
      "商品列表为空时的空状态 UI",
      "首次访问引导浮层（HINT）"
    ],
    "策划与通用经验矛盾": [
      {
        "场景": "商品点击区域",
        "策划需求": "整张卡片可点击",
        "通用经验": "按钮 + 卡片分离（防误触）",
        "建议": "采纳策划（已确认产品偏好）"
      }
    ]
  }
}
```

#### D. S2 8 角色并行拆解（v33 §10 goal-loop 模式）

```
┌────────────────────────────────────────────────────────────┐
│  S2 启动：8 角色并行拆解                                      │
│                                                              │
│  R1 CONFIG-专家 ─→ 拆 CONFIG OBJ（配置表 / 字段校验）          │
│  R2 UI-专家     ─→ 拆 UI OBJ（页面 / 控件）                  │
│  R3 BIZ-专家    ─→ 拆 BIZ OBJ（业务流 / 状态机）              │
│  R4 AUX-专家    ─→ 拆 AUX OBJ（底层 / 缓存 / 离线）           │
│  R5 LINK-专家   ─→ 拆 LINK OBJ（跨服务 + 变更回归）          │
│  R6 SPECIAL-专家 ─→ 拆 SPECIAL OBJ（异常 / 风控 / 边界）      │
│  R7 LOG-专家    ─→ 拆 LOG OBJ（埋点 / 审计 / 监控）           │
│  R8 HINT-专家   ─→ 拆 HINT OBJ（飘字 / 弹窗 / 红点）         │
│                                                              │
│  每角色读：                                                  │
│    - 终版需求.md + exit_permission.json + module_gap_analysis.md │
│    - 自己模块的 <X>_*.md（学习来源）                          │
│                                                              │
│  ↓ 每角色输出 N 个 OBJ（每个 OBJ 含 template_refs + requirement_diff）│
│  ↓ 8 角色产物合并 → requirement_objects.json                  │
└────────────────────────────────────────────────────────────┘
```

#### E. S2 现有产物不变（兼容性 SSOT）

| # | 文件 | 是否动 |
|---|---|---|
| 1 | `backlog.md` / `backlog.json` | ❌ 不动（SSOT）|
| 2 | `requirement_objects.md` / `requirement_objects.json` | ⚠️ OBJ 顶层增 2 字段（`template_refs` + `requirement_diff`）|
| 3 | `fail_report_S2.md` | ❌ 不动（SSOT）|

#### F. 与 v34 §3.4 缺口的关系

`v34/link_module_chain_map.md` §3.4 列出 LINK 模块 5 项缺口：
- ❌ S2 backlog OBJ/FP 模板没有 `regression_scope` 字段
- ❌ S5 TP `test_point_type` 枚举没有 `REGRESSION`
- ❌ S6 TC 没有"回归型 TC"标准模板
- ❌ v3.01 商城系统 S2 OBJ 未标 `regression_scope`
- ❌ 模块模板未明确"回归 = 通用能力验证"

**本方案与 v34 LINK 缺口的关系**：
- R5 LINK 角色承担"`regression_scope` 字段填写"职责
- 字段位置：挂在 OBJ 顶层（与 `template_refs` / `requirement_diff` 同级）
- 与 `template_refs` 不冲突：`template_refs` 是"读了哪些模板"，`regression_scope` 是"波及哪些通用能力"

> **待用户拍板（Q-006）**：R5 LINK 角色是否同时承担 `regression_scope` 字段填写（**合并职责**）vs R5 只承担关联职责、`regression_scope` 由 R3 BIZ 角色承担（**职责分离**）

---

## 3. 与其他阶段的衔接（不动）

| 阶段 | 本方案是否动 | 备注 |
|---|---|---|
| S1.5 | ❌ 不动 | AGENTS.md 三阶段边界 SSOT（人工决策阶段）|
| S2.5 | ❌ 不动 | 迭代规划，与 8 角色拆解无关 |
| S3 | ⚠️ 后续轮次评估 | S3 prototype 是否需要 R2 UI / R5 LINK 角色输出 page-flow 校验 |
| S4 | ⚠️ 后续轮次评估 | S4 business_flow 是否需要 R5 LINK 角色输出触发链校验 |
| S5 | ⚠️ 后续轮次评估 | S5 TP 是否需要 8 角色"模板强制遍历产物"（`prompts/test_point_gen.md` 已"按需"软约束，v34 Round X 升级）|
| S6 | ⚠️ 后续轮次评估 | S6 TC 是否需要 8 角色"回归型 TC 校验" |
| S7 | ⚠️ 后续轮次评估 | S7 审查员是否需要按 8 视角交叉 |
| S8 | ⚠️ 后续轮次评估 | S8 自迭代 / 经验沉淀是 8 角色反思 + 经验沉淀的天然落点 |

**v34 子方案本轮范围 = S1 + S2 接入点**。S3-S8 接入点留待 v35 / v36 评估。

---

## 4. §9.1 ≤ 5 文件改动清单（接入点落地）

| # | 文件 | 改动类型 | 备注 |
|---|---|---|---|
| 1 | `STAGE_S1_REVIEW.mdc` §新评审维度 | 4 维度 → 5 维度（新增"8 模块覆盖预审" 30%） | 见 §1.2.A |
| 2 | `STAGE_S1_REVIEW.mdc` §S1 产出清单 | 6 产出 → 7 产出（新增 `module_gap_analysis.md`）| 见 §1.2.D |
| 3 | `STAGE_S2_BREAKDOWN.mdc` §OBJ 顶层 | 新增 `template_refs[]` + `requirement_diff{}` 2 字段（9 字段不变）| 见 §2.2.A |
| 4 | `STAGE_S2_BREAKDOWN.mdc` §输入审查 | 增 `module_gap_analysis.md` 作为必读材料 | 见 §2.2.D |
| 5 | `STAGE_S2_BREAKDOWN.mdc` §与 S1 衔接 | 增"8 模块覆盖预审 → S2 拆解"段落 | 见 §1.3 |

> **≤ 5 红线**：5 项改动是上限。**任何超出 5 项的改动必须拆 v35**。

---

## 5. 待用户拍板清单（汇总）

| Q | 维度 | 选项 |
|---|---|---|
| **Q-001** | 角色身份深度（BIZ 专家具体身份） | A：服务端业务逻辑专家 / B：业务架构师 / C：DBA / D：混合 |
| **Q-002** | 并行执行方式 | A：`goal_parallel_executor.py` 真并行 / B：LLM 上下文分块思考 / C：串行 |
| **Q-003** | 经验沉淀落点 | A：`P_game_specific.md` / B：新增 `_experience.md` / C：单独目录 |
| **Q-004** | 反思周期 | A：每阶段 / B：每 Story / C：每 OBJ |
| **Q-005** | 模板维护权限 | A：候选制（Agent 写候选，人工审核）/ B：提案制（Agent 直接提案）|
| **Q-006** | R5 LINK 角色职责 | A：合并职责（关联 + 回归）/ B：分离职责（关联归 R5，回归归 R3）|
| **Q-007** | v3.01 产物是否回填 | A：不回填（SSOT 原则）/ B：v3.02 起新版本才填 / C：v3.01 也填 |
| **Q-008** | `module_gap_analysis.md` 是否也输出到 S2 物料门禁 | A：是 / B：否（仅供 S1.5 用）|
| **Q-009** | `requirement_diff` 是否纳入 S6 字段校验 | A：是（`l1_s6.py` 强校验）/ B：否（弱校验）/ C：只校验存在性，不校验内容 |
| **Q-010** | 8 角色并行是否真并发（同一时间 8 个 prompt）| A：是（成本高）/ B：否（同 LLM 顺序思考 8 次，产物合并）/ C：混合（关键模块真并行）|

---

**最后修改**：2026-07-22 00:40 草稿
**goal-loop 轮次**：未启动（**等用户拍板 §5 10 项 Q**）