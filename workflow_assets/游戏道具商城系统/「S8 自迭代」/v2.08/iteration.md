# S8 自迭代报告 v3.0 — 全链路 S1→S8 端到端迭代

> **生成时间**：2026-06-15
> **分析对象**：S2 backlog + S4 business_flow + S5 test_points-new.json (A2 99 TP) + S7 review_report (PASS)
> **设计原则**：❌ 无硬指标 / ❌ 不设阈值 / ❌ 不强求比例 / ✅ 用决策 push 引导 LLM 主动思考质量
> **v2 → v3 关键转变**：**用户反馈"我的自迭代应该是从 s1 到 s8 的"**——v2 只改 S5 是不对的,v3 必须全链路端到端

---

## 一、v3.0 关键认知纠正

| 维度 | v2.0 错误做法 | v3.0 正确做法 |
|---|---|---|
| **迭代范围** | 只改 S5 SKILL.md + 重跑 S5 | 改 S2/S4/S5/S6/S7 共 5 个阶段 SKILL.md |
| **根因层** | 假设根因在 S5 | **5 处误判/4 处折中根因在 S2**(Story scope_module 没填 / OBJ 跨模块没强制) |
| **Push 层级** | 全部推到 S5 §1.4 | S2 推 S2 层 push / S4 推 S4 层 push / ... 每一层针对该层职责 |
| **S8 报告** | 只列 S5 误判 | **5 处误判每个反向回溯到 S2/S4/S5/S6 哪一层** |

---

## 二、5 处误判 + 4 处折中的全链路根因链路

> 关键方法:**症状(S5/S6) → 中间层(S4) → 根因(S2)** ——不要默认根因在 S5。

### 2.1 [误判 1] S4.1-TP-001 LINK 误判

| 链路层 | 根因定位 |
|---|---|
| **症状 (S5)** | "游戏币支付"被归 LINK |
| **中间层 (S4)** | S4 接收了 S2 `epic.module="LINK"`,Epic 命名 "LINK-PAYMENT" 误导 S5 |
| **根因 (S2)** | S2 没把"游戏币支付"(单系统 BIZ)和"第三方渠道支付"(LINK)拆为 2 个 OBJ |
| **修复点** | **S2 §1.5.4 反例库**:看到"游戏币+第三方"必须拆 2 OBJ |

### 2.2 [误判 2] S4.2-TP-004 BIZ 误判

| 链路层 | 根因定位 |
|---|---|
| **症状 (S5)** | "缓存数据损坏"被归 BIZ |
| **中间层 (S4)** | S4 异常树叶子 1.2.1 归属未明(S4 §1.5.2 异常树叶子归属判定 push 缺失) |
| **根因 (S2)** | S2 OBJ FP-001/002 都写"缓存"业务,没区分"缓存底层"(AUX)和"业务读缓存"(BIZ) |
| **修复点** | **S2 §1.5.4 反例库**:看到"缓存"字样触发 AUX vs BIZ 边界判定 |

### 2.3 [误判 3] S5.4-TP-004 BIZ+LOG 重叠

| 链路层 | 根因定位 |
|---|---|
| **症状 (S5)** | "业务审计日志"既归 BIZ 又归 LOG(LLM 倾向"选一个") |
| **中间层 (S4)** | S4 风险点 R-024 没明确"业务侧落点 vs 全链路对账" |
| **根因 (S2)** | S2 OBJ FP-001 写"货币/道具流水"——S2 没说"业务侧落点"还是"全链路对账" |
| **修复点** | **S2 §1.5.4 反例库**:看到"审计"字样触发 LOG vs BIZ 边界判定,拆 2 OBJ |

### 2.4 [误判 4] S7.1-TP-003 HINT 模糊

| 链路层 | 根因定位 |
|---|---|
| **症状 (S5)** | "系统邮件通知"被归 HINT(Toast) |
| **中间层 (S4)** | S4 没区分"游戏内邮件列表"vs"IM 邮件" |
| **根因 (S2)** | S2 OBJ FP-003 写"系统邮件通知"——S2 没区分类型 |
| **修复点** | **S2 §1.5.3 模糊语义标注** + **§1.5.4 反例库**:看到"邮件通知"触发 3 类判定 |

### 2.5 [误判 5] S2.1-TP-010 字段名 typo

| 链路层 | 根因定位 |
|---|---|
| **症状 (S5)** | `test_point_subclass` 应为 `test_type_subclass` |
| **根因 (S5)** | S5 复制时拼写错(S5 SKILL.md 没字段名清单) |
| **修复点** | **S5 §1.5.5 JSON Schema 字段名强约束** + **S7 §1.5.1 字段名合规审查** |

### 2.6 [折中 1] S2.1 13 TP 缺 SPECIAL/CONFIG

| 链路层 | 根因定位 |
|---|---|
| **症状 (S5)** | S2.1 13 TP 缺 SPECIAL/CONFIG 拆分 |
| **根因 (S2)** | S2.1 Story scope_module 没填,FP 列表 6 个全 BIZ 业务流 |
| **修复点** | **S2 §1.5.1 Story scope_module 强制填** + **§1.5.2 跨模块拆 OBJ 强制检查** |

### 2.7 [折中 2] S2.2 5 TP 缺 UI

| 链路层 | 根因定位 |
|---|---|
| **症状 (S5)** | S2.2 5 TP 缺 UI 渲染 |
| **根因 (S2)** | S2.2 FP-002 订单详情页展示——S2 没识别为 UI 渲染 |
| **修复点** | **S2 §1.5.4 反例库**:看到"详情展示/列表渲染"触发 UI 判定 |

### 2.8 [折中 3] S3.1 7 TP 缺 UI

| 链路层 | 根因定位 |
|---|---|
| **症状 (S5)** | S3.1 7 TP 缺 UI(VIP 等级/折扣标签) |
| **根因 (S2)** | S3.1 FP-001 写"VIP 折扣"——S2 没识别"VIP 等级显示"为 UI |
| **修复点** | **S2 §1.5.4 反例库**:看到"等级/标签/价格显示"触发 UI 判定 |

### 2.9 [折中 4] S5.1 3 TP 缺 BIZ

| 链路层 | 根因定位 |
|---|---|
| **症状 (S5)** | S5.1 性能监控 3 TP 缺 BIZ(接口调用) |
| **根因 (S2)** | S5.1 FP-001/002 全 LOG 监控——S2 没识别"接口调用"为 BIZ |
| **修复点** | **S2 §1.5.4 反例库**:看到"接口/调用/耗时"触发 BIZ 判定 |

---

## 三、SKILL.md 全链路改造清单(共 5 个阶段)

### 3.1 S2 SKILL.md — 加 §1.5 决策 push 块

**改造位置**:`.cursor/skills/aidocx-s2-breakdown/SKILL.md`

**6 个 push 块**:
- §1.5.1 Story `scope_module` 强制填(PROMPT-PUSH-1)——Story 涉及的 8 模块子集必须填
- §1.5.2 OBJ 跨模块拆分强制检查(PROMPT-PUSH-2)——scope_module 有 N 个模块 → OBJ 必须覆盖 ≥ N 个
- §1.5.3 FP 模糊语义标注(PROMPT-PUSH-3)——业务流没说清的标 `ambiguity` 字段
- §1.5.4 OBJ 误标反例库(PROMPT-PUSH-4)——4 个反例:游戏币+第三方/缓存损坏/业务审计/系统邮件
- §1.5.5 OBJ 字段名强约束(PROMPT-PUSH-5)——字段名清单防 typo
- §1.5.6 OBJ 5 问质量 push(PROMPT-PUSH-6)——5 问自查,不追求数量

**预期影响**:S2 输出的 backlog 跨模块拆分彻底,**S5 误判/折中根因被前置阻断**

### 3.2 S4 SKILL.md — 加 §1.5 决策 push 块

**改造位置**:`.cursor/skills/aidocx-s4-flowchart/SKILL.md`

**3 个 push 块**:
- §1.5.1 Epic 命名 + 模块归属反例(PROMPT-PUSH-4)——S4 不被 epic 名误导
- §1.5.2 异常树叶子归属判定 push(PROMPT-PUSH-2)——叶子节点模块必须与 S5 TP 一致
- §1.5.3 风险点 s4_reference 格式(PROMPT-PUSH-5)——风险点 + 异常树叶子双引用

**预期影响**:S4 epic 命名不再误导 S5(避免 LINK-PAYMENT 误导游戏币支付归 LINK)

### 3.3 S5 SKILL.md — 已有 §1.5 决策 push 块(v2.0 改造)

**改造位置**:`.cursor/skills/aidocx-s5-test-points/SKILL.md`

6 个 push 块保持不变(详见 v2.0 报告):
- §1.5.1 跨模块拆分 push
- §1.5.2 4 步判定 push
- §1.5.3 is_assumed 标记
- §1.5.4 反例对照 push
- §1.5.5 JSON Schema 字段名强约束
- §1.5.6 5 问质量 push

### 3.4 S6 SKILL.md — 加 §1.5 决策 push 块

**改造位置**:`.cursor/skills/aidocx-s6-test-cases/SKILL.md`

**3 个 push 块**:
- §1.5.1 S6 TC 字段约束(PROMPT-PUSH-5)——与 S5 TP 字段保持一致
- §1.5.2 S6 步骤质量 push(PROMPT-PUSH-6)——5 问自查
- §1.5.3 S6 引用规范 push(PROMPT-PUSH-4)——禁止在 TC 字段中写 S4 节点引用

**预期影响**:S6 TC 字段与 S5 TP 一致,字段名 typo 被 S7 审查拦截

### 3.5 S7 SKILL.md — 加 §1.5 决策 push 块

**改造位置**:`.cursor/skills/aidocx-s7-review/SKILL.md`

**3 个 push 块**:
- §1.5.1 S7 字段名合规审查(PROMPT-PUSH-5)——审查员 A 新增 3 个检查项
- §1.5.2 S7 覆盖率精确化(PROMPT-PUSH-2)——3 个新增指标
- §1.5.3 S7 误判根因反查(PROMPT-PUSH-4)——报告新增 `misjudgment_root_cause` 字段

**预期影响**:**S8 拿到的 S7 报告直接含误判根因反查**——S8 不用再回溯

---

## 四、未改造的阶段(明确说明)

| 阶段 | 未改造原因 |
|---|---|
| **S1** | 本次迭代未涉及 S1 评审错误,根因不在 S1(S1 5 份物料齐全,S1.5 exit_permission 已正确生成) |
| **S1.5** | S1.5 exit_permission.json `can_proceed_to_s2=true` 正确,quality_level=MEDIUM,P0 全填,FB-01/02/03 fallback_rules 都已用 |
| **S3** | 本次项目**未跑 S3**(无 prototype.md),S3 push 块等下次跑 S3 时再加 |

---

## 五、test_points-iter3.json 状态

- **路径**:`workflow_assets/test_points-iter3.json`
- **TP 数**:110(从 99 增到 110)
- **规则驱动率**:93.6%(从 87.9% 提升)
- **状态**:保留 — 但仅作为 S5 层 push 决策验证
- **局限**:**基于原 S2 backlog 改 S5 产物,根因层在 S2 backlog** — 只有重跑 S2+S4+S5 才能验证完整 push 决策

---

## 六、下一轮全链路重跑计划(6 步)

| 步骤 | 重跑阶段 | 用 PUSH-V2-ITER-3 | 期望效果 |
|---|---|---|---|
| 1 | S2 | Story scope_module 全填,OBJ 跨模块拆分彻底 | S2 输出含 7 模块 OBJ |
| 2 | S4 | Epic 命名 + 异常树叶子归属 + 风险点格式都正确 | S4 不再误导 S5 |
| 3 | S5 | 误判/折中大幅减少(根因已被 S2 阻断) | 5 处误判/4 处折中消除 |
| 4 | S6 | TC 字段名合规,与 S5 TP 字段一致 | S6 TC 字段 typo 被 S7 拦截 |
| 5 | S7 | 报告含误判根因反查字段 | S7 报告直接定位 S2/S4/S5/S6 哪个阶段 |
| 6 | S8 | S8 报告内容更聚焦(因为 S7 已经做了根因反查) | S8 直接读 S7 报告,不需要再回溯 |

---

## 七、保留与归档

- **保留**:`iteration_v2.0_pre_iteration.md` + `iteration_v2.0_pre_iteration.json` 作为 v2 产物
- **归档**:`iteration.md` + `iteration.json` 是 v3.0 产物(覆盖原 v2.0 产物,原 v2 已备份)
- **保留产物**:`test_points-new.json` (A2 99 TP baseline) + `test_points-iter3.json` (S5-only push 110 TP) 作为对照

---

## 八、核心哲学

> **v3.0 自迭代核心原则(全链路版)**

1. **S8 自迭代 ≠ 改 S5**——S5 是症状层,根因可能在 S2/S4
2. **5 处误判/4 处折中 = 上游约束不足**——S2 没强制 scope_module 是主因
3. **全链路 push 决策**——S2 推 S2 层 push,S4 推 S4 层 push,S5 推 S5 层 push,不要全部堆到 S5
4. **S7 误判根因反查**——S7 报告应直接含 misjudgment_root_cause 字段,S8 拿到直接定位
5. **无硬指标** — 不设"X% 覆盖率合格"硬指标,用 5 问自查 + 反例库 + 反向回溯 引导 LLM 主动思考

---

## 九、反馈记录

| 反馈项 | 类别 | 来源 |
|---|---|---|
| S8 自迭代应该是从 S1 到 S8 的 | 设计反思 | 用户反馈(本次) |
| 5 处误判/4 处折中根因在 S2 backlog(不在 S5) | 根因分析 | v3.0 全链路回溯 |
| v2.0 只改 S5 的做法不对 | 经验教训 | 用户反馈 |
| test_points-iter3.json 仅 S5 层 push 验证 | 局限说明 | 实事求是 |
| 重跑需 S2+S4+S5+S6+S7 全链路 | 后续动作 | v3.0 输出 |

---

## 十、S8 后续动作

1. ✅ 全链路根因回溯(S2/S4/S5/S6/S7)
2. ✅ 5 个阶段 SKILL.md 加 push 决策块
3. ✅ iteration.json v3.0 记录根因 + 改造清单
4. ⏳ 用户在下一轮重跑 S2+S4+S5+S6+S7(全链路)
5. ⏳ 验证 5 处误判/4 处折中是否减少(根因层阻断)

---

*由 S8 自迭代 v3.0 生成(2026-06-15)*
*设计原则:全链路 S1→S8 / 根因回溯到 S2 / 每层推自己层 push / S7 含根因反查 / 无硬指标*
*输入: v2.0 报告 + 用户反馈"自迭代应该是从 s1 到 s8 的" + S2/S4/S5/S6/S7 SKILL.md 全文*
