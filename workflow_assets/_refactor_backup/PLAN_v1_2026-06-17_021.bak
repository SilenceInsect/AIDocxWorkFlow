# AIDocxWorkFlow 规则体系重构方案（备份）

> **状态**：Phase 1 方案完成，**等待用户对 5 个关键决策的确认**。
> **备份时间**：2026-06-17 02:03
> **下一步**：5 个决策点头后开 Phase 2（写代码）。
> **设计原则（最高准则）**：**"人更好理解和索引"**——所有结构反推自此。

---

## 〇、一句话方案

**把"约束"和"知识"物理分离**：
- 约束（Agent 必读）留在 `.mdc` + `SKILL.md`
- 知识（人查阅）迁出到 `.cursor/knowledge/` + `MODULES.md`
- 两份**通过 `RULES_INDEX` 双向引用**
- **一致性由 `gen_consistency.py` 守护**——不再靠人记得改

---

## 一、问题诊断（项目自陈的 5 个一致性问题）

| # | 问题 | 项目自陈证据 |
|---|---|---|
| **P1** | **SSOT 多头声明，无质量保障** | `AIDOCxWorkFlow.mdc` 说 "DESIGN §2.3 是 SSOT"；`DESIGN §2.3` 说"本表是 SSOT"；`STAGE_S*.mdc` 写"强制读 DESIGN §2.3"——**但 DESIGN §2.3 自身写 S7 覆盖率 ≥ 0.85 / S7 结构 ≥ 0.90 这种硬阈值，而 v2.0 重构已经废除硬阈值**——SSOT 自身不一致 |
| **P2** | **知识当约束读** | DESIGN §3（DRY/KISS/YAGNI/50 行函数要拆）是设计原则，**给知识库看的**——但 `alwaysApply: true` 让 Agent 当约束读。Agent 不会因为"DRY 是好原则"就把 Python 函数拆短，**这是噪声** |
| **P3** | **"事实已废"没同步到 SSOT** | CHANGELOG L18-21 "v2.0 重构废除 18 种方法 / TP:TC 加权 / 硬指标 0.85 0.90"——但 `DESIGN §2.3` 还在写 0.85/0.90；`DESIGN §4.3` 还在写 `S4_ANOMALY_COVERAGE=1.0` 硬指标。**事实和 SSOT 漂移** |
| **P4** | **重复声明** | `STAGE_S5_TEST_POINTS.mdc` 和 `aidocx-s5-test-points/SKILL.md` 内容**重叠 70%**（必读材料清单、决策 push 块、5 问质量 push、JSON Schema）——**改一忘一是必然** |
| **P5** | **"我改 X 影响 Y" 不可查** | 人想查 "S5_MIN_TP_PER_STORY=6 改 8 影响谁"——得人脑遍历 9 份 STAGE_S*；想查 "STAGE_S5 §1.4 哪些被 STAGE_S7 引用"——同样得遍历。**没有索引** |

**P1-P3 是 v2.0 重构本身的副作用**——CHANGELOG L18-21 自己也承认"v1.0 加的所有硬指标全部废弃"，**但废弃动作只改了下游 SKILL.md，没改上游 SSOT**。

---

## 二、方案架构

### 2.1 顶层分层

```
.cursor/
├── rules/                       ← 约束层（Agent 必读）
│   ├── AIDocxWorkFlow.mdc       ← 项目入口（总是读）
│   ├── DESIGN_AND_EXECUTION_STANDARDS.mdc  ← 跨阶段决策 + 行为约束（重定位后只留约束）
│   ├── CODEGRAPH_SYNC.mdc       ← 代码索引
│   └── STAGE_S*.mdc × 10        ← 阶段级约束（输入门禁 + 必读清单 + push 块）
├── skills/                      ← 约束层执行入口（Agent 必读）
│   └── aidocx-*/SKILL.md × 12   ← 阶段技能（强约束：必读材料 / 必走 push）
├── MODULES.md                   ← 知识层（业务定义：模块 / 边界 / 模板）
├── HANDOFF.md                   ← 知识层（项目导览）
│
├── knowledge/                   ← [新增] 知识层（**迁出自 DESIGN §3**）
│   ├── design_principles.md     ← DRY/KISS/YAGNI/命名规范（**只给人看**）
│   ├── history.md               ← 历次重构原因（CHANGELOG 人读版）
│   └── why_*.md                 ← "为什么 X 是 Y"——决策背景
│
├── RULES_INDEX.md               ← [新增] 双视图索引（人+Agent 都能查）
├── RULES_INDEX.json             ← [新增] 机器可读引用图
│
└── gen_*.py                     ← [新增] 一致性守护者脚本
    ├── gen_rules_index.py       ← 扫所有 .mdc + SKILL.md → 生成 RULES_INDEX
    └── gen_consistency_check.py ← 扫"SSOT 声明 vs 实际值" vs "硬指标是否已废"
```

### 2.2 DESIGN_AND_EXECUTION_STANDARDS.mdc 的"重定位"

| 章节 | 当前位置 | 重定位后位置 | 性质 |
|---|---|---|---|
| **§1 模块定义** | DESIGN §1.1-1.4 | **删除** | 知识——`MODULES.md` 已是 SSOT |
| **§2.1 通用执行流程** | DESIGN §2.1 | **保留** | 约束 |
| **§2.2 输入审查规则** | DESIGN §2.2 | **保留** | 约束 |
| **§2.3 质量门禁速查表** | DESIGN §2.3 | **重写**——只列"哪个阶段读哪个 SSOT"，不写硬阈值 | 约束（入口） |
| **§2.4 失败报告规范** | DESIGN §2.4 | **保留** | 约束 |
| **§2.5 版本号规则** | DESIGN §2.5 | **保留** | 约束 |
| **§2.6 目录结构** | DESIGN §2.6 | **保留** | 约束 |
| **§3 设计准则（DRY/KISS/命名/错误处理/日志/测试）** | DESIGN §3.1-3.6 | **迁出到** `.cursor/knowledge/design_principles.md` | **知识**——人看，Agent 不读 |
| **§4.1 枚举定义** | DESIGN §4.1 | **保留但加 `as_of: 2026-06-15` 字段** | 约束 |
| **§4.2 数据结构** | DESIGN §4.2 | **保留但加 `as_of` 字段** | 约束 |
| **§4.3 阈值常量** | DESIGN §4.3 | **大改**——只保留 **当前生效的** + **已废的移到 `.cursor/knowledge/history.md`** | 约束（**关键修复 P3**） |
| **§4.4-4.6 命名/目录/字段映射** | DESIGN §4.4-4.6 | **保留** | 约束 |
| **§5 跨阶段判决** | DESIGN §5 | **保留** | 约束 |

**关键：DESIGN §2.3 改写示例**——

| 旧 | 新 |
|---|---|
| 速查表 + 硬阈值 7.0/0.90/0.85/0.90 | 速查表 + 引用"看各 STAGE_S* 实际门禁（脚本只列事实，LLM 按业务写建议——v2.0 起无硬阈值）" |

### 2.3 STAGE_S* 与 SKILL.md 的"单写源"

**当前**：每阶段有两份文件（.mdc + SKILL.md）70% 重叠

**重构**（遵循 MODULES.md §11 已有的"单写源"原则）：
- **`STAGE_S*.mdc` 只写"约束"**：必读材料清单、强制 push 块、输入门禁、字段约束
- **`SKILL.md` 只写"执行入口"**：阶段一句话定位、自动化支持、参考文档
- **详细字段 / 拆解格式 / 决策表**——只在 SKILL.md，**STAGE_S*.mdc 引用不重复**

**这一改能彻底解决 P4**——之前改 70% 重叠内容会漂移，改完是"约束 vs 入口"两份各管一段。

### 2.4 规则模板（业务问题 + 决策依据 + 影响范围 + 调整记录）

把 DESIGN §4.3 重写为这个模板：

```markdown
### 规则 R-003：S5 每 Story 至少 6 个 TP

> **业务问题**：每 Story 测试点数量太少 → S6 拓宽空间不足 → S7 漏测概率 ↑
> **决策依据**：基于 4 类测试覆盖（正/反/边/异），6 个是业务经验值（CHANGELOG v1.0 → v2.0 反复调整过）
> **可调范围**：4-10（按业务风险）
> **当前值**：≥ 6（指导值，非硬阈值——v2.0 起无硬阈值，STAGE_S5 §1.5.6 5 问质量 push 替代）
> **影响范围**：
>   - 引用方：`STAGE_S5_TEST_POINTS.mdc §核心原则`、`aidocx-s5-test-points/SKILL.md §1.1`
>   - 检查方：`gen_consistency_check.py --check-rule R-003`
> **调整记录**：
>   - 2026-06-15 v1.0：初版"≥ 6" 指导值
>   - 2026-06-15 v2.0：v2.0 重构废除硬阈值，改为"指导值 + LLM 按业务灵活调整"
```

人看完能：
- **理解**——知道为什么是 6
- **审查**——能判断"这个值对不对"
- **索引**——`RULES_INDEX.md` 按"业务问题"反查
- **迭代**——改值前看"影响范围"知道改谁

### 2.5 双视图索引

**人视图（`RULES_INDEX.md`）——按"业务问题"组织**：

```markdown
# 规则索引（按业务问题）

## 问题：S5 测试点数量怎么定？
→ R-003 S5 ≥ 6 TP/Story（DESIGN §4.3 规则 R-003）
→ 决策依据：4 类覆盖法

## 问题：S7 用例审查的判定标准？
→ R-007 S7 v2.0 无硬阈值（DESIGN §2.3 + STAGE_S7 §核心原则）
→ 决策依据：v2.0 重构废除（CHANGELOG 2026-06-15）

## 问题：改了 S5_MIN_TP_PER_STORY 影响谁？
→ 影响：STAGE_S5 / STAGE_S7 / aidocx-s5-test-points SKILL
→ 详见：RULES_INDEX.json
```

**Agent 视图（`RULES_INDEX.json`）——按"规则 ID 引用方"组织**：

```json
{
  "rules": {
    "R-003": {
      "title": "S5 ≥ 6 TP/Story",
      "ssot_file": ".cursor/rules/DESIGN_AND_EXECUTION_STANDARDS.mdc#R-003",
      "value": "≥ 6 (指导值)",
      "effective_from": "v1.0",
      "deprecated_from": null,
      "as_of": "2026-06-15",
      "referenced_by": [
        ".cursor/rules/STAGE_S5_TEST_POINTS.mdc",
        ".cursor/skills/aidocx-s5-test-points/SKILL.md"
      ],
      "consistency_checks": [
        "检查 R-003 value 字段在所有 referenced_by 文档中一致"
      ]
    },
    "R-007": {
      "title": "S7 v2.0 无硬阈值",
      "as_of": "2026-06-15",
      "deprecation_history": [
        {"version": "v1.0", "value": "≥ 0.85 覆盖率"},
        {"version": "v2.0", "value": "无硬阈值，LLM 按业务写建议", "reason": "v2.0 重构废除"}
      ]
    }
  }
}
```

### 2.6 `gen_*.py` 一致性守护者

**`gen_rules_index.py`**（生成式——不"生成事实"，只"提取已经写过的事实"）：
- 扫所有 `.mdc` + `SKILL.md` 顶部 frontmatter
- 扫 R-XXX 规则段落
- 扫**所有 `@ref` 标记**（R-003 / MODULES §4.5 / STAGE_S5 §1.4）
- 输出 `RULES_INDEX.md` + `RULES_INDEX.json`

**`gen_consistency_check.py`**（**核心修复 P3**）：
```python
# 关键检查（伪代码）
checks = [
    check_ssot_self_consistency,         # SSOT 文档内部自洽
    check_referenced_rules_exist,        # 所有 @ref R-XXX 必须能解析到 RULES_INDEX
    check_deprecated_values_not_active,  # 硬阈值 0.85/0.90 不能出现在 v2.0+ SSOT
    check_dup_rules_in_stage_files,      # STAGE_S5 和 SKILL 不能有矛盾的值
    check_module_ssot,                   # 任何"8 模块"必须引用 MODULES.md
    check_orphan_refs,                   # R-XXX 没人引用 → 候选删除
]
```

**关键设计**：
- **不生成事实**（不扫描代码）——只检查"已经写下的规则之间的相互引用"
- **失败时给诊断信息**（"DESIGN §4.3 写 R-003=6，但 STAGE_S5 §1.6 写 R-003=8"）
- **不阻断**——人看完后再决定修哪边

### 2.7 CHANGELOG 改造——加入"规则变更"通道

CHANGELOG 现在只记"功能/重构"，不记"规则变更"——

```markdown
## [v2.1] - 2026-06-17 — 规则体系重构

### 规则变更（Rules）
- **R-003 业务问题标注**：S5 ≥ 6 TP/Story 决策依据补充
- **R-007 标记 deprecated**：v1.0 S7 ≥ 0.85 硬阈值已废（v2.0 改"无硬阈值"）
- **DESIGN §2.3 重写**：从"硬阈值速查"改为"引用方导航"

### 文件迁移
- DESIGN §3 设计准则 → `.cursor/knowledge/design_principles.md`
- 8 模块表 → 已删除冗余（保留 MODULES.md 唯一）

### 新增文件
- `RULES_INDEX.md` + `RULES_INDEX.json`
- `gen_rules_index.py` + `gen_consistency_check.py`
```

---

## 三、方案的自洽性（4 个关键问题都能回答）

### Q1：知识 vs 约束如何物理分离？

- **约束层** = `.mdc` + `SKILL.md` + `MODULES.md`（业务定义）—— Agent 必读
- **知识层** = `.cursor/knowledge/*.md`—— 人查阅，Agent **不读**
- **物理标记**：knowledge/ 下所有文件 `description` 标 `alwaysApply: false`（即使有人误设也不强读）

### Q2：如何让"硬指标已废"自动同步？

- `RULES_INDEX.json` 每个 R-XXX 有 `deprecation_history` 字段
- `gen_consistency_check.py --rule R-007` 报错"v1.0 阈值 0.85 不应出现在 v2.0+ 文档中"
- 误改 DESIGN §2.3 的人会被脚本拦下

### Q3：人怎么查"我改 X 影响 Y"？

- `RULES_INDEX.md` 按"业务问题"反查（"我要查 S5 TP 数量"）
- `RULES_INDEX.json` 按"规则 ID"反查影响范围（"R-003 影响 STAGE_S5 + SKILL"）

### Q4：Agent 怎么知道"哪些必读、哪些参考"？

- **约束层文件**都有 `alwaysApply: true` 或在 SKILL.md `description` 声明必读
- **知识层文件**全部 `alwaysApply: false`
- **RULES_INDEX.json 标记每个规则的 `mandatory_for_agent: true/false`**

---

## 四、方案"必然好"的论证

> **为什么这套结构能避免当前混乱再次发生？**

| 反模式 | 方案的预防机制 |
|---|---|
| **SSOT 自己不一致** | `gen_consistency_check.check_ssot_self_consistency` 检查 §2.3 和 §4.3 不互相矛盾 |
| **"事实已废"没同步** | `deprecation_history` + 一致性检查：v1.0 阈值不能在 v2.0 文档出现 |
| **STAGE_S* 和 SKILL.md 重复漂移** | 重构后 .mdc 写"约束"、SKILL 写"入口"——物理隔离，**不可能重复** |
| **"我改 X 影响 Y" 不可查** | `RULES_INDEX.json` `referenced_by` 字段直接列出影响 |
| **知识当约束读** | 知识迁出 `knowledge/` + 物理 `alwaysApply: false` |
| **人改阈值没意识到改了多份** | `gen_consistency_check` 报告"全 N 处引用了 R-003，列出位置" |

**根本机制**：
- **单写源**（改 1 处影响所有引用方）——MODULES.md §11 原则
- **事实可追溯**（`as_of` + `deprecation_history`）——知识库原则
- **机器可校验**（gen 脚本守住漂移）——工程化原则
- **人可审查**（双视图索引 + 业务问题反查）——最高准则

---

## 五、迁移路径（不变更语义）

**这一步才是"必然好"的关键**——4 步可逆迁移：

| 步 | 动作 | 风险 |
|---|---|---|
| **1. 建索引（不动既有文件）** | 写 `gen_rules_index.py` → 扫所有 R-XXX → 生成 `RULES_INDEX.md/json` | **零**——只读 |
| **2. 迁知识（DESIGN §3 → knowledge/）** | 剪贴 DESIGN §3 到 `knowledge/design_principles.md` + DESIGN 加 `> §3 已迁出` | **低**——纯剪贴 |
| **3. 修 SSOT（DESIGN §2.3 + §4.3）** | 硬阈值改"v2.0 起无硬阈值，引用各 STAGE_S* 实际门禁" + 加 `as_of` 字段 | **中**——影响引用方 |
| **4. 跑 gen_consistency_check** | 任何引用方未同步 → 报告 + 提示 | **零**——只诊断 |

**每步都可独立回滚**——这是为什么"必然好"。

---

## 六、待用户确认的 5 个关键决策

1. **`gen` 脚本定位**——"一致性守护者"（不生成事实）——**对**？
2. **DESIGN §2.3 / §4.3 重写**——硬阈值改"v2.0 起无硬阈值"——**对**？
3. **STAGE_S* vs SKILL.md 物理分离**——.mdc 写约束、SKILL 写入口——**对**？
4. **`RULES_INDEX.md` + `RULES_INDEX.json` 双视图**——人查 MD、Agent 查 JSON——**对**？
5. **DESIGN §3 知识层迁出**——到 `knowledge/`——**对**？

**这 5 件事点头后**，开 Phase 2 写代码。

---

## 七、识别到的可立即应用的规则清单（备份）

下列规则在备份当下（2026-06-15 v2.0）**已生效**——RULES_INDEX v0.1 草拟清单：

| 规则 ID | 标题 | 当前值 | as_of | 引用方 |
|---|---|---|---|---|
| R-001 | S1 评审评分门禁 | ≥ 7.0/10.0（指导） | 2026-06-15 | STAGE_S1 §判决规则、DESIGN §4.3 |
| R-002 | S1.5 P0 必填门禁 | P0 项 100% 填写 | 2026-06-15 | STAGE_S1.5、STAGE_S2 §输入审查 |
| R-003 | S5 每 Story 至少 6 TP | ≥ 6（指导值，v2.0 起非硬阈值） | 2026-06-15 | STAGE_S5 §核心原则、aidocx-s5-test-points SKILL |
| R-004 | S6 必填字段完整性 | 100% | 2026-06-15 | STAGE_S6 §输出规范 |
| R-005 | 8 模块 SSOT | MODULES.md（项目级唯一） | 2026-06-15 | 所有 SKILL.md、所有 STAGE_*.mdc、ai_workflow/*.py |
| R-006 | S2 Epic 拆解精度 | ≥ 90%（v1.0 阈值，v2.0 重构未明文废除） | 2026-06-15 | STAGE_S2 §输入审查 |
| R-007 | S7 审查判定 | v2.0 起**无硬阈值**（脚本只列事实，LLM 按业务写建议） | 2026-06-15 v2.0 废弃 v1.0 ≥ 0.85 | STAGE_S7 §核心原则、aidocx-s7-review SKILL |
| R-008 | S8 自迭代 | 输出"判定"非"建议" | 2026-06-15 | STAGE_S8 §核心原则 |
| R-009 | 模块边界严格隔离 | AUX vs BIZ vs LINK vs SPECIAL 四者无重叠 | 2026-06-15 v1.11 | MODULES §4.10.1 |
| R-010 | 单写源原则 | "同样的内容只能在一个地方维护" | 2026-06-15 v1.3 | MODULES §11 |
| R-011 | JSON Schema 字段名强约束 | 详见各阶段 SKILL.md | 2026-06-15 | STAGE_S5 §1.6、STAGE_S6 §输出规范 |
| R-012 | S2.5 默认跳过 | `AIDOCX_INCLUDE_S2_5` 未设置 = 跳过 | 2026-06-15 | AIDocxWorkFlow.mdc 编排开关 |
| R-013 | 失败报告命名 | `fail_report_S*.md` | 2026-06-15 | DESIGN §2.4、§4.4 |
| R-014 | is_assumed 强制要求 | 必填 `true`/`false`；业务常识假设需 `requires_human_review: true` | 2026-06-15 | STAGE_S5 §is_assumed 标记 |

> **P1 修复关键**：R-007 在 RULES_INDEX.json 标记 `deprecated_history`，**v1.0 阈值 ≥ 0.85 不能出现在 v2.0+ 文档中**——`gen_consistency_check` 守护。

---

## 八、Phase 2 计划（5 个决策点头后启动）

| 阶段 | 产出 | 风险 |
|---|---|---|
| **P2.1** | `gen_rules_index.py`（扫所有 .mdc + SKILL.md → 输出 RULES_INDEX.md/json） | 零（只读） |
| **P2.2** | `gen_consistency_check.py`（6 个检查 + 诊断报告） | 零（只读） |
| **P2.3** | 创建 `.cursor/knowledge/` 目录 + 迁 DESIGN §3 + 迁 history.md | 低（剪贴） |
| **P2.4** | 重写 DESIGN §2.3 + §4.3 为"规则模板 R-XXX"格式 | 中（影响引用方） |
| **P2.5** | 跑 gen_consistency_check，生成诊断报告 | 零（只读） |
| **P2.6** | 按诊断报告逐项修复引用方 | 视诊断结果 |
| **P2.7** | STAGE_S* vs SKILL.md 物理分离（如有需要） | 中（结构调整） |

---

## 九、对话外的可回溯锚点

- **方案备份**：`workflow_assets/_refactor_backup/RULES_REFACTOR_PLAN_v1_2026-06-17.md`（本文件）
- **CHANGELOG**：本备份完成后追加一条 `[v2.1] [PLAN] 规则体系重构方案备份（待 5 决策确认）`
- **可继续对话的开场白**："继续 Phase 2 决策 / 讨论 R-XXX 规则清单 / 修改 §2.3 重写方案"

---

*由 AIDocxWorkFlow Phase 1 自迭代生成*
*最高准则：人更好理解和索引*
