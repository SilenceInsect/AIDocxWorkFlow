# CONFIG 模块测试点模板（概览）

> **模块代码**：`CONFIG`
> **来源**：`.cursor/MODULES.md` §4 + `.cursor/MODULES.md` §1 总表
> **作用**：S5 生成 CONFIG 模块测试点时，按 story 实际涉及子类**按需加载**对应子模板。
>
> **完整覆盖范围**（一句话）：
> 游戏数据表结构与字段合法性校验、枚举/资源/ID/时间格式约束、同表/全局配置数值一致性、
> 跨表 ID 关联依赖校验、循环依赖拦截；客户端 & 服务端配置解析、复杂嵌套结构解析、超大表加载性能；
> 概率/成长/上限数值逻辑校验；配置增量/全量热更新、在线/离线热更、异常回滚兜底；
> 新旧版本前后兼容、赛季/灰度/渠道专属配置隔离；Excel 导出打包发布流程校验；
> 服务端全局常量、定时任务、GM 参数配置校验；非法配置容错告警、配置变更日志追溯。

---

## 子类索引

| 字母 | 子类名             | 子类代码（v1.2 枚举）              | 模板                                                                | 对应细化段 |
| ---- | ------------------ | ----------------------------------- | ------------------------------------------------------------------- | ---------- |
| A    | 字段合法性         | `FIELD_LEGALITY`                    | [A_field_legality.md](./CONFIG/A_field_legality.md)                  | 段 1（基础结构校验）|
| B    | 同表 / 全局一致性  | `FIELD_INTRA_DEP`                   | [B_consistency.md](./CONFIG/B_consistency.md)                        | 段 2（配置一致性）|
| C    | 跨表依赖           | `FIELD_CROSS_DEP`                   | [C_cross_dep.md](./CONFIG/C_cross_dep.md)                            | 段 3（跨表依赖）|
| D    | 热更新             | `RELOAD_4_MODE`                     | [D_hot_reload.md](./CONFIG/D_hot_reload.md)                          | 段 4（热更）|
| E    | 解析与加载         | `PARSE_LOAD`                        | [E_parse_load.md](./CONFIG/E_parse_load.md)                          | 段 5(1)（解析）|
| F    | 版本兼容与灰度     | `VERSION_COMPAT`                    | [F_version_compat.md](./CONFIG/F_version_compat.md)                  | 段 5(2)（版本兼容）|
| G    | 数值逻辑校验       | `VALUE_LOGIC`                       | [G_value_logic.md](./CONFIG/G_value_logic.md)                        | 段 5(3)（数值逻辑）|
| H    | 导出/导入/发布     | `EXPORT_PUBLISH`                    | [H_export_publish.md](./CONFIG/H_export_publish.md)                  | 段 5(4)（导出发布）|
| I    | 服务端专属         | `SERVER_CONFIG`                     | [I_server_specific.md](./CONFIG/I_server_specific.md)                | 段 5(5)（服务端）|
| J    | 边界区分           | —（判定规则，非测试类型）          | [J_boundary.md](./CONFIG/J_boundary.md)                              | 段 4（边界区分）|
| K    | 游戏项目补充       | —（游戏专项，非通用测试类型）      | [K_game_specific.md](./CONFIG/K_game_specific.md)                    | 段 5（游戏补充）|

> **结构说明**：
> - A-D 对应原 §4 矩阵的 4 个 v1.1 枚举（向后兼容）
> - E-I 是 v1.2 新增的 5 个枚举，对应段 5 五大类
> - J/K 是判定规则和游戏专项，S5 不直接生成 J/K 类型的 TP，但用 J/K 作为"边界判定参考"和"游戏项目额外检查项"

---

## 加载规则（S5 prompt 使用方式）

1. **检测** `epic.module == "CONFIG"` → 必读本概览
2. **按 story 内容** 识别涉及的子类（如"活动过期"→ 涉及 A 字段合法性 + G 数值逻辑 + K 游戏专项）
3. **按需加载** 对应子模板
4. **交叉参考** `J_boundary.md` 防止误标 CONFIG 标签（实际是 UI / BIZ / LINK / LOG）

---

## 边界总览

| 归 CONFIG                          | 不归 CONFIG（归其他模块）              |
| ---------------------------------- | -------------------------------------- |
| 配置文件本身的字段/枚举/资源/ID 校验 | 加载后 UI 展示（按钮颜色变了）→ UI      |
| 配置导出/解析/热更/版本兼容         | 玩家操作触发的业务逻辑 → BIZ           |
| 同表/跨表数值/ID 约束/循环依赖      | 后端接口读取配置做业务计算 → BIZ / LINK|
| 数值公式/概率/上限/平衡校验         | 埋点/日志（配置变更的 audit log）→ LOG  |
| 非法配置容错告警                    | 性能监控（加载耗时上报）→ LOG           |

> 完整边界规则见 [`J_boundary.md`](./CONFIG/J_boundary.md)

---

## 关键词快速映射（S5 自动检测）

| 关键词 / 上下文                          | 子类       |
| ---------------------------------------- | ---------- |
| 字段、类型、范围、枚举、必填、ID 格式     | A 字段合法性 |
| 一致、统一、平衡、概率、百分比、公式      | B 一致性 / G 数值逻辑 |
| 引用、依赖、外键、ID 关联、循环依赖       | C 跨表依赖 |
| 热更、reload、热加载、增量、全量          | D 热更新 |
| 解析、加载、JSON、性能、超大表、嵌套      | E 解析与加载 |
| 兼容、版本、灰度、渠道、白名单            | F 版本兼容 |
| 概率、公式、上限、堆叠、产出消耗          | G 数值逻辑 |
| 导出、Excel、CSV、发布、审核、打包        | H 导出发布 |
| 全局常量、定时任务、GM、分布式服、权限    | I 服务端 |
| 本地化、多区服、活动过期、礼包限购、防沉迷 | K 游戏专项 |

---

## 进度

- v1.0 (2026-06-15)：CONFIG 模块 11 子模板全部到位
