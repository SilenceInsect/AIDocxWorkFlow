# LINK 模块测试点模板（概览）

> **模块代码**：`LINK`
> **来源**：`.cursor/MODULES.md` §1 总表 + §4.9 LINK 细分索引 + 用户最终版细化定义
> **作用**：S5 生成 LINK 模块测试点时，按 story 实际涉及子类**按需加载**对应子模板。
>
> **完整覆盖范围**（一句话）：
> 内部业务上下游联动、多活动并行相交 / 重复开赛季重置逻辑、跨系统数据联动约束；跨分布式服务数据同步、跨服互通业务与时序一致性；多端同账号数据实时对齐、多端登录冲突约束；第三方渠道登录 / 支付 / 上报业务对接、外部开放 API 交互与回调幂等处理；跨模块通用资源 / 限购互通约束；业务异步消息队列上下游透传；灰度 / 多服环境数据隔离与同步；游戏数据对外平台透出、客服 / 对账数据同步（**与 AUX 底层传输框架隔离，仅覆盖业务互通规则**）。

---

## 子类索引

| 字母 | 子类名 | 子类代码（v1.2 枚举） | 模板 | 对应细化段 |
|------|--------|----------------------|------|-----------|
| A | 内部业务关联 | `INTERNAL_BIZ_LINKAGE` | [A_internal_biz_linkage.md](./LINK/A_internal_biz_linkage.md) | §三-1 内部业务关联 |
| B | 跨服务/分布式关联 | `CROSS_SERVER_SYNC` | [B_cross_server_sync.md](./LINK/B_cross_server_sync.md) | §三-2 跨服务/分布式关联 |
| C | 多端一致性关联 | `MULTI_CLIENT_SYNC` | [C_multi_client_sync.md](./LINK/C_multi_client_sync.md) | §三-3 多端一致性关联 |
| D | 外部第三方关联 | `EXTERNAL_THIRD_PARTY` | [D_external_third_party.md](./LINK/D_external_third_party.md) | §三-4 外部第三方关联 |
| E | 跨模块资源互通 | `CROSS_MODULE_RESOURCE` | [E_cross_module_resource.md](./LINK/E_cross_module_resource.md) | §三-5-③ 跨模块资源互通约束 |
| F | 对外数据透出 | `OUTBOUND_DATA` | [F_outbound_data.md](./LINK/F_outbound_data.md) | §三-5-④ 对外数据透出关联 |
| O | 边界区分 | —（判定规则，非测试类型） | [O_boundary.md](./LINK/O_boundary.md) | §四 区分边界 |
| P | 游戏专项 | —（游戏项目专属） | [P_game_specific.md](./LINK/P_game_specific.md) | 游戏项目补充 |

> **结构说明**：
> - A-D 对应原 §三 4 个核心类别（内部业务/跨服务/多端/外部第三方）
> - E-F 对应用户新增 4 大类中的 2 个独立成子类（跨模块资源互通 + 对外数据透出）
> - 用户新增 4 大类中的另外 2 个（跨环境配置 + 异步消息）已分别融入 B（跨服业务中"灰度/多服环境"子项）和 D（外部第三方中"异步消息队列"子项）——避免子类过细
> - O/P 是判定规则和游戏专项，S5 不直接生成 O/P 类型的 TP，但用 O 作为"边界判定参考"、用 P 作为"游戏项目额外检查项"

---

## 加载规则（S5 prompt 使用方式）

1. **检测** `epic.module == "LINK"` → 必读本概览
2. **按 story 内容** 识别涉及的子类（如"跨服组队"→ 涉及 B 跨服务 + E 资源互通；"第三方支付回调"→ 涉及 D 外部第三方 + F 对外透出）
3. **按需加载** 对应子模板
4. **交叉参考** `O_boundary.md` 防止误标 LINK 标签（实际是 AUX 底层 / BIZ 单系统 / SPECIAL 异常拦截）

---

## 边界总览（AUX vs LINK 核心区分）

| 归 LINK | 不归 LINK（归其他模块）|
|---------|----------------------|
| 内部多系统业务联动规则（上游解锁下游、模块互通数据流转）| 底层网络传输、SDK 底层封装、消息队列底层框架 → **AUX** |
| 多活动并行相交、活动复开/重置业务规则 | 单系统独立业务流程（单独商城购买、单独任务领取）→ **BIZ** |
| 跨分布式服务数据同步、跨服业务与时序一致性 | 第三方接口恶意篡改、跨服数据作弊拦截 → **SPECIAL** |
| 多端同账号数据实时对齐、多端登录冲突约束 | 客户端本地缓存实现、缓存命中与淘汰 → **AUX** |
| 第三方渠道登录/支付/上报业务对接、回调幂等 | 支付 SDK 底层加密、TCP 重连、HTTP 工具 → **AUX** |
| 平台外部开放 API 交互（运营后台、客服工单、防沉迷）| 资源下载、断点续传、整包增量热更 → **AUX** |
| 跨模块通用资源/限购互通约束（通用货币/道具全系统流通）| 业务资源加载、引用计数、卸载 → **AUX** |
| 业务异步消息队列上下游透传（充值完成推送活动服）| 业务流程内部状态机、业务异常处理 → **BIZ** |
| 灰度/多服环境数据隔离与同步 | 灰度流量调度、防刷限流、外部接口限流 → **SPECIAL** |
| 游戏数据对外平台透出（排行榜/客服/对账）、脱敏 | UI 弹窗、飘字、红点 → **HINT** |
| 跨服异常时的业务兜底（外部接口超时/重试/幂等）| 业务行为日志、审计追踪、监控埋点 → **LOG** |

> **核心规则**（来自用户最终版）：
> - **AUX 是"水管"**——底层通用框架、工具、SDK 封装、传输基础设施（只提供"能力底座"，不含业务联动逻辑）
> - **LINK 是"水管里流通什么业务数据、两端业务怎么对齐"**——业务层面的关联、互通、外部对接逻辑
> - **二者无重叠**——任何重叠项归 AUX（基础设施）/ LINK（业务规则），无第三选项

> 完整边界规则 + 误判案例 + 决策树见 [`O_boundary.md`](./LINK/O_boundary.md)

---

## 关键词快速映射（S5 自动检测）

| 关键词 / 上下文 | 子类 |
|----------------|------|
| 上下游、依赖、解锁、前置、后置、模块互通、活动复开、活动重置、关联、联动 | A 内部业务关联 |
| 跨服、跨服务、分布式、时序、一致性、时序校验、断点续同步、灰度服、跨服拍卖、跨服竞技 | B 跨服务/分布式 |
| 多端、顶号、登录冲突、PC、移动端、模拟器、本地缓存、差值刷新、客户端服务端 | C 多端一致性 |
| 第三方、渠道、SDK、微信、支付宝、登录态、账号绑定、回调、对账、支付、上报、防沉迷、运营后台、客服、API、幂等 | D 外部第三方 |
| 通用货币、通用道具、跨系统限购、全服上限、共享积分、资源互通 | E 跨模块资源互通 |
| 对外透出、排行榜、客服后台、对账平台、脱敏、字段对齐、实时同步、定时同步 | F 对外数据透出 |
| 异步消息、消息队列、上下游推送、消息丢失、重复消息 | D 外部第三方（子项）/ B 跨服务（子项） |
| 测试服、正式服、灰度服、数据隔离、白名单 | B 跨服务/分布式（子项） |

---

## 进度

- v1.8 (2026-06-15)：LINK 模块 **8 文件**全部到位（6 子模板 + O 边界 + P 游戏专项）；AUX vs LINK 边界规则在 8 文件中持续交叉引用
