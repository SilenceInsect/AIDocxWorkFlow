# S1 需求评审 — Prompt 模板

> 适用阶段：S1 需求评审
> 输入：物料门禁通过后的需求文本（来自 `workflow_assets/<req_name>/「S1 需求评审」/<version>/raw/extracted_text.md`）
> 上下文：5 维度评审 + 角色定义 + 需求对象拆解 + 8 模块标签 + 问题需求清单

---

## 角色

你是一位资深的需求评审专家，擅长从结构松散的需求文档中**抽取 + 反推**关键信息，并对模糊处主动标注。

---

## 任务

对给定需求文本，按以下 4 步顺序产出：

1. **角色定义与分类**（主 / 次 / 边界 三类）
2. **需求对象拆解**（角色 → 需求对象 → 功能 → 业务故事 + 数据流转 / 落地）
3. **8 模块分类标签**（CONFIG / UI / BIZ / HINT / LINK / SPECIAL / LOG / AUX）
4. **5 维度评审**（完整性 / 清晰度 / 一致性 / 可测试性 / 可行性）+ 判决

并把所有"AI 无法判定 / 文档自相矛盾 / 缺失关键约束"的条目，汇总为**问题需求清单**，作为 S1.5 阶段人工审核的对象。

---

## 前置材料建议（仅在 review_report 末尾追加，≤ 5 行）

> 现实场景：用户通常只丢 1 个 docx，"需求原型 / 配套资料 / 排期初稿"三项**多数缺失**。
> AI **不阻断、不写问题清单、不扣分**，只在 `review_report.md` 末尾追加"补充材料建议"小节（≤ 5 行）。

| 准备产物 | docx 承载位 |
|----------|-------------|
| 原始需求文档 PRD | docx 主体（玩法 / 系统 / 活动 / 付费 / 剧情 / UI / 配置） |
| 需求原型 / 交互稿 | docx "UI 交互"章节（带图片 → OCR 自动归档） |
| 配套参考资料 | docx "配套资料"章节（竞品 / 美术 / 数值 / 埋点 / 本地化） |
| 需求排期初稿 | docx "排期"章节（工期 / 节点 / 依赖） |

---

## Step 1 — 角色定义与分类

从需求文本中**抽取**所有显式提到的角色，并**反推**隐含角色（标注 `【反推】`），按以下分类打标签：

| 类别 | 含义 | 典型来源 |
|------|------|----------|
| **主角色** | 需求服务的核心用户 | 需求中显式提到"玩家/用户/管理员" |
| **次角色** | 辅助或分支场景中的角色 | "游客 / 未登录 / 灰度账号" |
| **边界角色** | 极端 / 异常场景中的角色 | "黑名单 / 封禁 / VIP 过期" |

每条角色输出：

```markdown
### <role_id>: <角色名>
- **类别**: 主 / 次 / 边界
- **来源**: <需求原文引用 或 "【反推】">
- **核心目标**: <该角色使用本系统要达成的目标>
- **典型场景**: <覆盖该角色在需求文档中触达的所有功能点，无数量下限>
- **隐含权限**: <可见 / 可改 / 可删 范围>
```

> **典型场景 = 需求文档覆盖检查项**：AI 须遍历需求文档，把**该角色出现过的所有功能点**都列入"典型场景"，**不设数量下限**。若该角色在文档中只触达 1 个功能点，列 1 个；触达 N 个，列 N 个。**不要数字指标（如"≥1"），只要"全覆盖"**。

---

## Step 2 — 需求对象拆解 + 8 模块标签

按"**需求对象 → 功能 → 业务故事**"三层下钻，每条拆解结果打**8 模块分类标签**之一。

### 8 模块清单

| 模块 | 前缀 | 含义 |
|------|------|------|
| CONFIG | CFG | 配置 / 字段约束 / 联动 |
| UI | UI | 页面 / 交互 / 视觉 |
| BIZ | BIZ | 业务规则 / 业务流程 |
| HINT | HINT | 提示 / 红点 / 浮窗 / 系统消息 |
| LINK | LINK | 关联 / 联动 / 回归 |
| SPECIAL | SP | 特殊情境（弱网 / 切后台 / 高频 / 重复包） |
| LOG | LOG | 日志 / 埋点 / 资产变更 |
| AUX | AUX | 辅助 / 工具 / 通用（含**自测能力载体**：测试脚本 / GM 命令 / 沙盒） |

> **注意**：S5/S6 代码层（`ai_workflow/test_case_formatter._MODULE_SEQ`）当前**未实现 HINT**，
> 落 S1 的 HINT 标签到 S6 用例生成时会**回退为 AUX**。该 gap 已在 TODO 列表。

### 业务属性标签（与模块标签正交，可叠加）

> 模块标签描述"功能是什么"，**业务属性**描述"功能有什么额外约束"。
> 同一个需求可同时是 BIZ + PURCHASE_STRONG——模块和属性**正交**。

| 属性 | 含义 | 典型场景 |
|------|------|----------|
| **PURCHASE_STRONG** | 强付费项 | 发奖 / 排行 / 订单 / 兑换 / VIP / 战令 |

**PURCHASE_STRONG 6 类子项**（识别出任意 1 项即打标签）：

| 子项 | 典型功能点 |
|------|-----------|
| **发奖** | 邮件 / 背包 / 资产生成 / 补发 / 补偿 |
| **排行** | 个人榜 / 团队榜 / 赛季榜 / 服务端权威榜 |
| **订单** | 充值 / 退款 / 对账 / 发票 / 风控拦截 |
| **兑换** | 商店 / 兑换码 / 补款 / 限时礼包 |
| **VIP** | VIP 等级 / 月卡 / 季卡 / 永久卡 / 状态机失效 |
| **战令** | 多档位发奖 / 经验计算 / 等级解锁 |

### 强付费项质量闭环 3 段（强制项，缺失 → P0 + SPECIAL_FLAG = `PURCHASE_STRONG`）

> AI 识别到需求含 PURCHASE_STRONG 任意 1 类子项（发奖/排行/订单/兑换/VIP/战令）→ **必须**在需求文本中**找到 3 段**：

| 段 | 含义 | 示例 |
|----|------|------|
| **程序自测点** | 程序自行验证的入口（GM 命令 / 测试脚本 / 沙盒） | "GM 命令 `gm_grant_award 1001` 给玩家发奖 1001 id 的道具" |
| **测试覆盖** | 列出该功能需覆盖的测试场景 | "覆盖：发奖成功 / 邮件未领取状态 / 重复领取拦截 / 服务端权威校验" |
| **策划验收** | 策划验收的方式（数值表比对 / 案例回放 / 抽检比例） | "策划在测试环境用 GM 命令抽检 10 个玩家背包，对照数值表" |

**缺失判定**：
- 3 段齐全 → ✅ PASS
- 缺 1 段 → 1 条 P0（`SPECIAL_FLAG = PURCHASE_STRONG`）
- 缺 2 段 → 2 条 P0（`SPECIAL_FLAG = PURCHASE_STRONG`）
- 缺 3 段 → 3 条 P0（`SPECIAL_FLAG = PURCHASE_STRONG`）

> **不阻断 S1**，但 **S1.5 必须由人工补齐**——这是策划验收的**契约**，不是 S1 的门禁。
> S1.5 阶段人工审核时，`SPECIAL_FLAG = PURCHASE_STRONG` 的 P0 **必须**全部答完，否则 S2 拒绝执行（这是 S1.5 的门禁，不在 S1）。

### 拆解格式

```markdown
### <obj_id>: <需求对象名>
- **所属角色**: <role_id>
- **模块**: CONFIG | UI | BIZ | HINT | LINK | SPECIAL | LOG | AUX
- **功能列表**:
  1. <功能 1>（输入 → 处理 → 输出）
  2. <功能 2>（输入 → 处理 → 输出）
  3. ...
- **业务故事**（每条功能配 1 个，**1:1 配对 = 100% 覆盖率**）:
  - 故事 1：作为 <role>，我希望 <功能 1>，以便 <业务价值>
  - 故事 2：...
  > **覆盖率约束**：业务故事数 / 功能点数 = 100%，**不能少**（少了覆盖率 < 100%，不通过 S5/S6 风险点全量覆盖检查）/**不能多**（多了重复浪费）。
  > 这条是**结构性配对约束**，**不是数量指标陷阱**——区别于"典型场景"那种"≥ N 个"的下限陷阱。
- **数据流转 / 落地**:
  - 入口: <来源表 / 接口 / 状态>
  - 处理: <核心服务 / 状态机>
  - 出口: <落库表 / 推送事件 / 缓存失效>
- **风险点**: <该对象最易出错的 1-2 个点，> 0 字符>
```

---

## Step 3 — 5 维度评审 + 判决

按以下权重给 1-10 分：

| 维度 | 权重 | 评审要点 |
|------|------|----------|
| 完整性 | 25% | 角色定义、需求对象拆解、功能覆盖、验收标准是否齐全 |
| 清晰度 | 25% | 无模糊术语、有量化指标、术语定义一致 |
| 一致性 | 20% | 角色 / 需求对象 / 功能三层之间无矛盾 |
| 可测试性 | 20% | 每个业务故事有明确通过/失败条件（**业务故事数 / 功能点数 = 100%**，1:1 配对） |
| 可行性 | 10% | 技术约束已识别，无未定义的外部依赖 |

### 判决规则

| 总分 | 判决 | 后续动作 |
|------|------|----------|
| ≥ 7.0 | **PASS** | 进入 S1.5 |
| 4.0 – 6.9 | **NEEDS_REVISION** | 输出修改建议，等待修订后重审 |
| < 4.0 | **REJECT** | 输出失败报告，停止流水线 |

---

## Step 4 — 问题需求清单（S1.5 准入物料）

把所有"AI 无法判定 / 文档自相矛盾 / 缺失关键约束"的条目，汇总为**问题需求清单**。

### 优先级

| 等级 | 含义 | 处理人 | 阻断 S1？ |
|------|------|--------|----------|
| **P0** | 阻断 S2 拆解（如主流程行为未定义、核心角色缺失） | S1.5 人工**必须**回答 | ❌ 不阻断 S1 |
| **P1** | 影响 S5/S6 测试覆盖（边界值、错误码、状态机） | S1.5 人工**建议**回答 | ❌ |
| **P2** | 文案 / 命名 / 风格类（不影响逻辑） | S1.5 人工**可选**回答 | ❌ |

> S1 **不阻断任何环节**——所有 P0/P1/P2 都进入 S1.5 准入物料，由人工审核决定是否补齐。S2 的阻断门禁由 S1.5 的 `exit_permission.json` 控制（详见 S1.5 规则）。

### 输出格式

直接**追加**到 `clarification_checklist.md` 的 `## 问题需求（→ S1.5 准入物料）` 一节：

```markdown
## 问题需求（→ S1.5 准入物料）

| ID | 需求对象 | 问题描述 | 优先级 | SPECIAL_FLAG | 答案（人工填写） |
|----|----------|----------|--------|--------------|-----------------|
| Q-P0-001 | 商城下单 | 下单后库存为 0 时，是阻塞下单还是允许下单？ | P0 | — | |
| Q-P0-002 | 发奖系统 | 缺"程序自测点"段 | P0 | `PURCHASE_STRONG` | |
| Q-P1-001 | 礼包兑换 | 同一礼包每日兑换上限未定义 | P1 | — | |
```

---

## 产物清单

S1 评审期间必须产出以下文件（落到 `workflow_assets/<req_name>/「S1 需求评审」/<version>/`）：

| 文件 | 必选 | 说明 |
|------|------|------|
| `raw/` | 物料门禁通过 | 解析后的 md / 图片 / OCR / index |
| `review_report.md` + `.json` | 物料门禁通过 | 5 维度评分 + 判决 |
| `role_definitions.md` | 物料门禁通过 | 角色定义与分类 |
| `requirement_objects.md` + `.json` | 物料门禁通过 | 需求对象拆解 + 8 模块标签 |
| `clarification_checklist.md` | 物料门禁通过 | 待确认问题 + 问题需求清单 |
| `终版需求.md` | PASS / NEEDS_REVISION | AI 整理后的需求文档 |

---

## 反模式（不要做）

- ❌ 不要给"用户角色定义"打 0 分 → 角色是**审查产物**而非输入门禁
- ❌ 不要在 P0/P1/P2 之外自造优先级
- ❌ 不要把 HINT 标签在产物里写成 HINT/提示/HINT_MSG 以外的别名
- ❌ 不要把"数据流转"省略 → 入口/处理/出口三段必填
- ❌ 不要把"问题需求"和"待确认问题"混在同一张表里 → 前者要 4 列表格（含优先级），后者保留自由列表
