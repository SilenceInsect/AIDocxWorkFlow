# v1 遗留问题（next version 待解决）

> **本文件是 v2 的输入**——v2 启动时**必须先答**这些问题。
> **格式约定**：`Q-XXX` 编号 + `状态: [ ] 未决 / [x] 已决`

---

## 来自方案本身的 5 个未决策（核心）

- [ B ] **Q-001**: gen 脚本定位 = 一致性守护者（不生成事实）？
  - 候选 A：守护者（不写文件，只检查一致性）
  - 候选 B：生成器（继续现在模式）
  - **影响**：决定是否需要 RULES_INDEX.md / RULES_INDEX.json 的自动生成

- [ A ] **Q-002**: DESIGN §2.3 / §4.3 重写 = 硬阈值改"v2.0 起无硬阈值"？
  - 候选 A：全删硬阈值（v2.0 起），同时要方案给合理的推理标准做质量保底
  - 候选 B：保留 S1 / S5 / S6 硬阈值，删其它
  - 候选 C：保留所有硬阈值
  - **影响**：决定 S1 / S5 / S6 SKILL.md 是否需要重写

- [ A ] **Q-003**: STAGE_S* vs SKILL.md 物理分离 = .mdc 写约束、SKILL 写入口？
  - 候选 A：物理分离（已设计，未做），因为需要单独调用执行阶段
  - 候选 B：合并（同源）
  - **影响**：决定 9 个 STAGE_S* 文档是否拆分

- [ A] **Q-004**: 双视图索引 = RULES_INDEX.md（人）+ RULES_INDEX.json（Agent）？
  - 候选 A：双视图
  - 候选 B：单视图（只 .md / 只 .json）
  - **影响**：决定 `gen_rules_index.py` 是否写

- [ A] **Q-005**: DESIGN §3 知识层迁出 = 到 `knowledge/`？
  - 候选 A：迁出（建立新目录）
  - 候选 B：留在 DESIGN 文件内
  - **影响**：决定 `knowledge/` 目录是否建立 + 多大

---

## 来自"方案迭代目录"自身设计的 4 个未决策

- [C ] **Q-101**: 存什么？A / B / C？
  - 候选 A：只存方案文本
  - 候选 B：存"完整 diff 状态"
  - 候选 C：A + B（独立方案 + 自动 diff 视图）
  - **当前默认**：C（已落地方案中）

- [C ] **Q-102**: 回滚粒度？A / B / 都有？
  - 候选 A：整份方案回滚
  - 候选 B：字段级回滚
  - 候选 C：都有
  - **当前默认**：A（v1 仅实现整份回滚；B 待 v2）

- [ A] **Q-103**: 和现有备份关系？独立 / 包含 v1 / 替代？
  - 候选 A：完全独立
  - 候选 B：包含 v1（迁入备份）
  - 候选 C：替代（删旧备份）
  - **当前默认**：B（已实施；保留 .bak + 软链）

- [C ] **Q-104**: 回滚方式？手工 / 脚本 / 自动？
  - 候选 A：人工手工
  - 候选 B：脚本一键
  - 候选 C：自动备份 + 脚本回滚
  - **当前默认**：C（已实施；`design_iter.py rollback v0`）

---

## 来自反思的 1 个未决策

- [ C] **Q-201**: Agent 把"先执行"误读为"先给方案"——v2 启动时如何避免？
  - 根因分析：用户说"先 X"时，Agent 倾向把"先"当成"先给方案再说"
  - 候选 A：在 DNA 自检 Q3 加一条"先 = 优先级，非流程词"
  - 候选 B：beforeSubmitPrompt hook 检测"先"字 + 强提示
  - 候选 C：A + B
  - **当前默认**：C（DNA 已有"先动手再补"反模式 + Q-201 跟踪 v2）

---

## v2 启动原则

**优先级排序**（先答哪个）：

1. **Q-001 ~ Q-005**（5 核心决策）→ 决定 v2 主体方向
2. **Q-201**（反思类）→ 决定 DNA 自检内容是否扩展
3. **Q-101 ~ Q-104**（目录设计）→ 决定 v2 是否扩展 design_iter 能力

**禁止**：
- 跳过 Q-001 ~ Q-005 直接写 v2
- 一次答完所有问题（每次只答 1-2 个，迭代收敛）

---

> **本文件状态**：v1 起点的"未决问题清单"——**v2 启动时**是必读输入。
