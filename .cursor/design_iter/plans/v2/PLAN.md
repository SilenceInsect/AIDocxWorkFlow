# AIDocxWorkFlow 方案 v2：5 决策回答 + 4 步迁移实施

> **本方案 = v1 设计的执行版**。
> v1（398 行）已论证"为什么这样改"；v2 只回答"改成什么 + 怎么改"。
> **核心动作**：逐条答 Q-001~Q-005 → 执行 v1 §五 4 步迁移 → 重新跑 `gen_consistency_check` 验证。

---

## ⚠️ 启动必读：5 个核心决策必答清单

> **这 5 个决策 v1 已列出候选答案（默认 = 候选 A），但默认≠已决。**
> **Agent 不替你答——你点头后，Agent 才开始动 v2 主体工作。**

### Q-001：gen 脚本定位

- **候选 A**：守护者（不写文件，只检查一致性）—— v1 默认
- **候选 B**：生成器（继续现在模式——既检查又生成）
- **影响**：决定是否需要 `RULES_INDEX.md` / `RULES_INDEX.json` 的自动生成
- **当前默认答案**：[ A ] 守护者（v1 倾向）
- **决策状态**：⏳ **待用户点头**

### Q-002：DESIGN §2.3 / §4.3 重写

- **候选 A**：全删硬阈值（v2.0 起），同时要方案给合理的推理标准做质量保底 —— v1 默认
- **候选 B**：保留 S1 / S5 / S6 硬阈值，删其它
- **候选 C**：保留所有硬阈值
- **影响**：决定 S1 / S5 / S6 SKILL.md 是否需要重写
- **当前默认答案**：[ A ] 全删（v1 倾向）
- **决策状态**：⏳ **待用户点头**

### Q-003：STAGE_S* vs SKILL.md 物理分离

- **候选 A**：物理分离（已设计，未做），因为需要单独调用执行阶段 —— v1 默认
- **候选 B**：合并（同源）
- **影响**：决定 9 个 STAGE_S* 文档是否拆分
- **当前默认答案**：[ A ] 物理分离（v1 倾向）
- **决策状态**：⏳ **待用户点头**

### Q-004：双视图索引

- **候选 A**：双视图（RULES_INDEX.md 人读 + RULES_INDEX.json Agent 读）—— v1 默认
- **候选 B**：单视图（只 .md / 只 .json）
- **影响**：决定 `gen_rules_index.py` 是否写
- **当前默认答案**：[ A ] 双视图（v1 倾向）
- **决策状态**：⏳ **待用户点头**

### Q-005：DESIGN §3 知识层迁出

- **候选 A**：迁出（建立 `.cursor/knowledge/`）—— v1 默认
- **候选 B**：留在 DESIGN 文件内
- **影响**：决定 `knowledge/` 目录是否建立 + 多大
- **当前默认答案**：[ A ] 迁出（v1 倾向）
- **决策状态**：⏳ **待用户点头**

> **Agent 推进规则**：5 决策全部点头后，Agent 把每个决策写入 v2 `decisions.json`（D-101 ~ D-105），并把 open_questions.md 中对应行标记为 `- [x]`。任何 Q-XXX 答"否"或"换候选"，Agent **必须**先告诉用户"这会改 v1 哪些前提"再继续。

---

## v2 必备 3 栏

### 1. 本次 v2 解决的问题（来自 v1 open_questions.md Q-001~Q-005）

| 决策 | v1 默认 | 待点头 | 落地动作（点头后） |
|---|---|---|---|
| Q-001 gen 脚本定位 | 守护者 | ⏳ | 写 `gen_consistency_check.py`（不写 RULES_INDEX 生成器——v1 §2.6 设计） |
| Q-002 DESIGN §2.3 / §4.3 重写 | 全删硬阈值 | ⏳ | 重写 §2.3 为"引用方导航"；§4.3 改为 R-XXX 规则模板 |
| Q-003 STAGE_S* vs SKILL.md 物理分离 | 物理分离 | ⏳ | 9 份 STAGE_S*.mdc 拆出 SKILL 引用内容 |
| Q-004 双视图索引 | 双视图 | ⏳ | 同 Q-001（与 RULES_INDEX.json 写不写挂钩） |
| Q-005 DESIGN §3 知识层迁出 | 迁出 | ⏳ | 创建 `.cursor/knowledge/`，剪贴 DESIGN §3 |

> **v2 主体进度 = 这 5 行 × 点头状态**——用户每点头 1 个，Agent 推进 1 步；不解锁下一决策。

### 2. 本次 v2 新增内容

- **v2 启动框架**（本文件）——3 栏 + 5 决策清单 + 推进规则
- **v2 决策清单** `decisions.json`——D-101~D-105 占位（点头后填充）
- **v2 反思 Q-201 决策**——v2 启动原则已部分回答（"5 决策先答"），待 v2 完成后验证效果
- **v2 目录设计**——Q-101~Q-104 移入 v2 open_questions.md（v2 启动后新评估）
- **修复 v2.1 软链问题**——已在 v2.1.1 完成（CHANGELOG 已记）

### 3. 本次 v2 仍遗留的问题（→ v3）

**来自 v1 open_questions 优先级 3 的 4 个 + 优先级 2 的 1 个**：

- Q-101：design_iter 存什么？A / B / C？（候选 A 默认已实施；B 待评估）
- Q-102：design_iter 回滚粒度？A / B / 都有？（候选 A 默认已实施；B 待 v3）
- Q-103：design_iter 和现有备份关系？（v1 D-003 已答：B 包含 v1）
- Q-104：design_iter 回滚方式？（v1 D-001 已答：C 自动 + 脚本）
- Q-201：Agent "先执行"误读防——v2 启动原则已部分缓解，待 v2 完成后观察

> **v2 不强行答 Q-101~Q-104 / Q-201**——v2 完成后看 v2 实际效果再决定 v3 是否需要扩展 design_iter 能力。

---

## v2 推进原则（Agent 必读）

1. **决策先于代码**：5 决策未点头 → 不动 STAGE_S* / SKILL.md / DESIGN 文件。
2. **一次答一个**：用户点头 1 个 → 推进该决策的 1 个落地动作 → 等用户下一个点头。
3. **决策同步记账**：每个点头后，**立即**更新 3 处：
   - `v2/PLAN.md` §1 表格的"待点头"列
   - `v2/open_questions.md` 对应 Q-XXX 标 `- [x]`
   - `v2/decisions.json` 加 D-101~D-105（用户点头哪个加哪个）
4. **决策否决要先告知影响**：用户说"Q-001 我选 B"——Agent 必须先列"选 B 会改 v1 §2.6 + §八 P2.1 哪些设计"，等用户确认再动。
5. **v2 不重新设计**：v2 不重复 v1 §一~§四 的"为什么这样改"论证——只看 v1 论证后实施。

---

## v2 执行路径（点头后的 4 步迁移，引自 v1 §五）

| 步 | 动作 | 风险 | 关键检查 |
|---|---|---|---|
| **1. 建索引** | 写 `gen_rules_index.py` → 扫所有 R-XXX → 生成 `RULES_INDEX.md/json` | 零 | 只读，不动既有文件 |
| **2. 迁知识** | 剪贴 DESIGN §3 到 `.cursor/knowledge/design_principles.md` + DESIGN 加 `> §3 已迁出` | 低 | 纯剪贴，可回滚 |
| **3. 修 SSOT** | DESIGN §2.3 + §4.3 改写为"引用方导航 + R-XXX 规则模板" | 中 | 影响引用方——必须先跑 `gen_consistency_check` 识别引用方 |
| **4. 跑检查** | 跑 `gen_consistency_check.py` 6 项检查 → 生成诊断报告 | 零 | 只诊断，Agent 不自动修 |

> **每步独立可回滚**——v2 任何一个推进后想撤回，对应 `git revert` 即可（v2.1 已用软链 + INDEX，**不要**整体替换 PLAN.md）。

---

## v2 完成判定

- [ ] Q-001~Q-005 全部点头
- [ ] D-101~D-105 全部写入 `v2/decisions.json`
- [ ] 4 步迁移全部完成
- [ ] `gen_consistency_check` 6 项检查全过（或未过的有显式 `skip_reason`）
- [ ] CHANGELOG 加 `[v2.2] — 4 步迁移完成`
- [ ] v2 `open_questions.md` 重写（Q-101~Q-104 / Q-201 留给 v3 评估）

**v2 完成 → 启动 v3 评估**（v3 = 评估 Q-101~Q-104 + Q-201 + 任何 v2 跑出来的新问题）。

---

## 详细引用

| 内容 | 路径 |
|---|---|
| v1 完整设计（为什么这样改） | `.cursor/design_iter/plans/v1/PLAN.md`（v1 §一~§四） |
| v1 4 步迁移路径 | v1 PLAN §五（PLANS 行 323-334） |
| v1 5 个待点头决策 | v1 PLAN §六（PLANS 行 338-344） |
| v1 14 项规则清单 | v1 PLAN §七（PLANS 行 350-369） |
| v1 Phase 2 任务分解 | v1 PLAN §八（PLANS 行 375-385） |
| 项目根铁律 | `AGENTS.md` |

---

*由 AIDocxWorkFlow v2.1 迭代生成*
*最高准则：人更好理解和索引*
