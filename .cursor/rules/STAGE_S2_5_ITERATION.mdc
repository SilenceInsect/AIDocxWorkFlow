---
description: AIDocxWorkFlow Stage 2.5 — 迭代规划（独立阶段）
alwaysApply: false
---

# AIDocxWorkFlow S2.5 — 迭代规划

> **独立阶段**：可以单独调用。上游材料（S2 backlog）审查合格后开始，失败写失败报告。

---

## Step 0 — 项目配置收集（执行前必须完成）

> **强制前置**：S2.5 在正式开始迭代规划前，必须先收集并确认项目配置参数。配置不全不得进入 7 步规划。

### 必须收集的配置参数

|| 参数 | 说明 | 示例 |
|------|------|------|------|
| 项目名（req_name） | 需求名称 | 游戏道具商城系统 |
| 版本名（version） | 版本标识 | v1.0 |
| 排期开始日期 | 迭代第一天 | 2025-07-01 |
| 排期截止日期 | 迭代最后一天 | 2025-07-12 |
| 策划预估工时（h） | 产品策划阶段估算工时 | 40h |
| 前端预估工时（h） | 前端开发估算工时 | 80h |
| 后端预估工时（h） | 后端开发估算工时 | 120h |
| 测试预估工时（h） | 测试估算工时 | 60h |
| 团队规模 | 各角色人数 | 前端2人·后端3人·测试2人 |
| 迭代总工时（周） | 任务管线估算总时长 | 1.5周（即10.5天≈52h） |

### 快速迭代参考基准

当前项目迭代节奏极快（1.5 周全流程），各角色可用工时估算：

```
迭代周期 1.5 周 ≈ 7.5 工作日 ≈ 60h/人
- 前端 2 人：共 120h
- 后端 3 人：共 180h
- 测试 2 人：共 120h
- 合计：420h
```

### 配置收集方式

执行 S2.5 时，若检测到项目配置不完整，**必须主动询问用户**，不得自行假设默认值后跳过。可通过以下方式获取：

1. **主动提示**：列出所有待填写字段，逐项询问
2. **用户提供文档**：用户粘贴或上传项目排期表
3. **从 S2 backlog 推断**：部分字段可从 backlog.json 的 summary 中获取估算值（Epic总数、工时等）

### 配置保存

收集完成后，将配置保存为 `project_config.json` 并写入输出目录：

```json
{
  "req_name": "游戏道具商城系统",
  "version": "v1.0",
  "schedule": {
    "start_date": "2025-07-01",
    "end_date": "2025-07-12"
  },
  "estimates": {
    "planning_hours": 40,
    "frontend_hours": 80,
    "backend_hours": 120,
    "qa_hours": 60
  },
  "team_size": {
    "frontend": 2,
    "backend": 3,
    "qa": 2
  },
  "iterations": {
    "total_weeks": 1.5,
    "total_hours_per_person": 60
  }
}
```

---

## 阶段入口

**触发方式**：`/aidocx-s2-5-iteration` 或粘贴 S2 backlog

**前置材料（必须全部满足）**：

| 材料 | 来源 | 审查要求 |
|------|------|----------|
| S2 backlog.md | `workflow_assets/<req_name>/「S2 需求拆解」/<version>/backlog.md` | 存在且含 Epic/Story/Task |
| S2 requirement_objects.md | `workflow_assets/<req_name>/「S2 需求拆解」/<version>/requirement_objects.md` | 存在且含需求对象 + 功能点（工时估算依据） |
| **项目配置** | `workflow_assets/<req_name>/「S2.5 迭代规划」/<version>/project_config.json` | **存在且所有字段已填写** |

**材料缺失时**：生成失败报告，停止 S2.5。

---

## 核心任务

将 S2 拆解好的任务转化为团队可执行的迭代计划，包含 **7 步规划**。

### 7 步执行规划

| Step | 名称 | 操作 | 核心目标 |
|------|------|------|----------|
| 1 | 迭代负载平衡 | 计算每人总工时 vs 迭代周期，识别过载/富余 | 避免忙的忙死、闲的闲死 |
| 2 | 一对一任务确认 | 与每个任务负责人确认：能按时完成吗？依赖项对方能交付吗？ | 避免拍脑袋接任务，到时候做不完 |
| 3 | 迭代启动会（全员对齐） | 同步：迭代目标 / 每人任务&截止 / 里程碑 / 风险点 / **需求冻结** | 对齐所有人预期，需求锁定 |
| 4 | 任务录入与配置 | 录入 Jira/禅道：状态/负责人/截止/依赖关系/风险/里程碑 | 所有人在工具里看进度 |
| 5 | 资源锁定与申请 | 申请/锁定：压测服务器、测试环境、特殊账号、云资源 | 提前申请避免要用时发现被占用 |
| 6 | 启动前置并行工作 | 测试：提前写用例；开发：技术预研；产品：补文档 | 串行改并行，压缩迭代周期 |
| 7 | 进入执行与跟进 | 每日站会 / 任务状态跟进 / 风险调整 | 持续跟进，及时调整 |

### 需求冻结原则

**启动会上必须宣告：本次迭代需求已锁定。** 除线上 P0 紧急问题外，中途不得新增需求。

### 小需求简化方案

单个小功能修改不需要走全流程：
- 不用开启动会，只和相关人员同步任务与排期
- 不用复杂负载平衡，只确认负责人能做
- 直接启动前置工作 + 执行

---

## 输入审查（门禁前置条件）

| 检查项 | 要求 | 缺失时 |
|--------|------|--------|
| project_config.json 存在 | 必须（所有字段已填写） | 失败报告（提示补充配置） |
| backlog.md 存在 | 必须 | 失败报告 |
| 有 Task 拆解 | 建议有 | 自动生成简化版 |
| Epic 优先级已分配 | 必须 | 失败报告 |
| 关键里程碑已设定 | 必须（开发完成、转测、回归、上线） | 失败报告 |

> **门禁原则**：S2.5 的门禁是**关键里程碑完整性**，不是「排期与资源规划 ≥ 80%」这类拍脑袋的评估分数。迭代计划必须包含：开发完成 → 转测 → 回归 → 上线 四个里程碑，且每个里程碑有明确日期。

---

## 输出规范

### 成功产出

| 文件 | 路径 |
|------|------|
| 项目配置 | `workflow_assets/<req_name>/「S2.5 迭代规划」/<version>/project_config.json` |
| 迭代规划报告 | `workflow_assets/<req_name>/「S2.5 迭代规划」/<version>/iteration_plan.md` |
| 迭代规划数据 | `workflow_assets/<req_name>/「S2.5 迭代规划」/<version>/iteration_plan.json` |

```markdown
# 迭代规划报告

**版本**: v1.0
**迭代周期**: YYYY-MM-DD ~ YYYY-MM-DD
**团队规模**: 前端 N人 · 后端 N人 · 测试 N人

---

## 1. 项目配置

| 参数 | 数值 |
|------|------|
| 项目名 | ... |
| 版本名 | v1.0 |
| 排期开始 | YYYY-MM-DD |
| 排期截止 | YYYY-MM-DD |
| 策划预估工时 | N h |
| 前端预估工时 | N h |
| 后端预估工时 | N h |
| 测试预估工时 | N h |
| 迭代总工时 | N 周 |

## 2. 迭代目标

- 目标1
- 目标2

## 3. 负载平衡

| 成员 | 角色 | 任务数 | 总工时(h) | 迭代可用(h) | 状态 |
|------|------|--------|-----------|-------------|------|
| 张三 | 前端 | 5 | 40 | 40 | 正常 |

## 4. 里程碑

| 里程碑 | 日期 | 说明 |
|--------|------|------|
| 开发完成 | MM-DD | ... |
| 转测 | MM-DD | ... |
| 回归完成 | MM-DD | ... |
| 上线 | MM-DD | ... |

## 5. 资源锁定

| 资源 | 负责人 | 状态 |
|------|--------|------|
| 压测服务器 | 李四 | 已申请 |
| 测试环境 | 王五 | 已锁定 |

## 6. 风险点

| 风险 | 等级 | 应对措施 | 状态 |
|------|------|----------|------|
| ... | HIGH | ... | OPEN |

## 7. 任务分配

| Epic | Story | Task | 负责人 | 工时 | 截止 |
|------|-------|------|--------|------|------|
| UI-001 | UI-001-001 | TASK-001 | 张三 | 8h | MM-DD |
```

同时生成 JSON：`iteration_plan.json`

### 失败报告（S2.5 迭代规划失败）

`workflow_assets/<req_name>/「S2.5 迭代规划」/<version>/fail_report_S2_5.md`

```markdown
# 迭代规划失败报告

**阶段**: S2.5 迭代规划
**日期**: YYYY-MM-DD

## 失败原因

...

## 后续动作

[ ] 补充 backlog 后重新提交 S2.5
```

---

## 与其他阶段的衔接

| 上游 | 下游 |
|------|------|
| S2 需求拆解 | S3 原型导出、S5 测试点生成（并行） |

S2.5 与 S3/S4 可并行执行，加速迭代。

---

## 代码入口

```python
from ai_workflow.conversation_skills import save_iteration_plan
save_iteration_plan(version, backlog, raw_output, parsed, req_name)
```

---

## 关联文件

- Prompt 模板：`ai_workflow/prompts/iteration_planning.md`
- 保存逻辑：`ai_workflow/conversation_skills.py` → `save_iteration_plan()`
- 输出目录：`workflow_assets/<req_name>/「S2.5 迭代规划」/<version>/`
