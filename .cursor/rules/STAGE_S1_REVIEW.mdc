---
description: AIDocxWorkFlow Stage 1 — 需求评审（独立阶段）
alwaysApply: false
---

# AIDocxWorkFlow S1 — 需求评审

> **独立阶段**：可以单独调用。丢入材料 → 审查材料 → 合格后开始评审产出，失败写失败报告。

---

## 阶段入口

**触发方式**：`/aidocx-s1-review` + 需求文件（.docx / .md / .txt），**不接受纯文本粘贴**

**输入材料**：
- 原始需求文件（.docx / .md / .txt）
- .docx 由 `ai_workflow/stage_s1_input/run_s1_pipeline()` 自动转 md + 图片归档 + OCR，产物落 `raw/`
- 无文件 / 不可解析 / 解析后正文 < 50 字 → **物料门禁失败**，生成 `fail_report_S1.md` 停止

---

## 物料门禁（前置条件，阻断）

| 检查项 | 要求 | 缺失时 |
|--------|------|--------|
| 需求文件 | 文件路径 / 附件存在 | 直接生成 `fail_report_S1.md`，**不进**评审 |
| 文档可解析 | `.docx` / `.md` / `.txt` 解析成功 | 同上 |
| 正文非空 | 解析后正文 ≥ 50 字 | 同上 |

> **本阶段不验证"内容是否齐全"**。角色 / 功能 / 指标 / 验收的完整性由**审查产物**补齐，不阻断 S1。

### 内容门禁 → 审查产物 / 评审维度 的覆盖关系

> 原"内容门禁"4 项的诉求，已被下表**完全覆盖**。本阶段不再单设"输入审查"环节：

| 原内容门禁项 | 归宿 |
|-------------|------|
| 用户角色定义 | → 审查产物 `role_definitions.md`（主 / 次 / 边界） |
| 功能描述 | → 审查产物 `requirement_objects.md`（功能列表） |
| 量化指标 | → 5 维度评审"清晰度"维度 |
| 验收标准 | → 审查产物 `requirement_objects.md`（业务故事通过/失败条件） |

---

## 前置材料建议（仅提示，不阻断）

> 仅在 `review_report.md` 末尾的"补充材料建议"小节出现，**不进入**物料门禁 / 问题清单 / fail_report / 5 维度评分扣分。

| 准备产物 | 现实状态 | docx 承载位 |
|----------|----------|-------------|
| **原始需求文档 PRD** | ✅ 必有 | docx 主体（玩法 / 系统 / 活动 / 付费 / 剧情 / UI / 配置） |
| **需求原型 / 交互稿** | ⚠️ 多数缺失 | docx "UI 交互"章节（带图片 → OCR 自动归档） |
| **配套参考资料** | ⚠️ 多数缺失 | docx "配套资料"章节（竞品 / 美术 / 数值 / 埋点 / 本地化） |
| **需求排期初稿** | ⚠️ 多数缺失 | docx "排期"章节（工期 / 节点 / 依赖） |

**AI 行为约束**：✅ 允许在 review_report 追加"补充材料建议"小节（≤ 5 行） / ❌ 不写问题清单 / ❌ 不触发 fail / ❌ 不扣分。

---

## 审查产物（评审期间生成）

| 产物 | 路径 | 说明 |
|------|------|------|
| 角色定义 | `role_definitions.md` | 从需求中抽取 + 反推，主 / 次 / 边界三类；"典型场景"**覆盖该角色在需求文档中触达的所有功能点**（不设数量下限） |
| 需求对象拆解 | `requirement_objects.md` + `.json` | 角色 → 需求对象 → 功能 → 业务故事（**1:1 配对 = 100% 覆盖率**） + 8 模块标签 |
| 问题需求清单 | 追加到 `clarification_checklist.md` | P0/P1/P2 三级，作为 S1.5 准入物料 |

### 8 模块标签（与 S5/S6 共用）

CONFIG / UI / BIZ / HINT / LINK / SPECIAL / LOG / AUX

> **AUX = 自测能力载体**（测试脚本 / GM 命令 / 沙盒 / 调试入口），不参与业务功能验收。
> 完整模块定义见 `.cursor/MODULES.md`（项目级唯一真相源）。

### 业务属性标签（与模块标签正交，可叠加）

> 模块标签描述"功能是什么"，**业务属性**描述"功能有什么额外约束"。
> 同一个需求可同时是 BIZ + PURCHASE_STRONG——模块和属性**正交**。

| 属性 | 含义 | 典型场景 |
|------|------|----------|
| **PURCHASE_STRONG** | 强付费项 | 发奖 / 排行 / 订单 / 兑换 / VIP / 战令（见下表） |

**PURCHASE_STRONG 6 类子项**（识别出任意 1 项即打标签）：

| 子项 | 典型功能点 |
|------|-----------|
| **发奖** | 邮件 / 背包 / 资产生成 / 补发 / 补偿 |
| **排行** | 个人榜 / 团队榜 / 赛季榜 / 服务端权威榜 |
| **订单** | 充值 / 退款 / 对账 / 发票 / 风控拦截 |
| **兑换** | 商店 / 兑换码 / 补款 / 限时礼包 |
| **VIP** | VIP 等级 / 月卡 / 季卡 / 永久卡 / 状态机失效 |
| **战令** | 多档位发奖 / 经验计算 / 等级解锁 |

### 问题需求清单格式

> P0/P1/P2 **都不阻断 S1**——S1 不发 fail_report；进入 S1.5 后由人工审核决定。
> `SPECIAL_FLAG` 列高亮**强付费项的 P0**，S1.5 审核时一眼可见。

| ID | 需求对象 | 问题描述 | 优先级 | SPECIAL_FLAG | 答案（人工填写） |
|----|----------|----------|--------|--------------|-----------------|
| Q-P0-001 | 发奖系统 | 缺"程序自测点"段 | P0 | `PURCHASE_STRONG` | |
| Q-P1-001 | 任务链 | "完成" 条件模糊 | P1 | — | |
| Q-P2-001 | 邮件系统 | 描述略简但可猜 | P2 | — | |

> 强付费项的 P0 缺失 = 缺"程序自测点 / 测试覆盖 / 策划验收"3 段中的任意 1 段或全部。

### 强付费项质量闭环 3 段（强制项，缺失 → P0 + SPECIAL_FLAG = `PURCHASE_STRONG`）

**识别规则**：AI 识别到需求含 PURCHASE_STRONG 任意 1 类子项（发奖/排行/订单/兑换/VIP/战令）→ **必须**在需求文本中**找到 3 段**：

| 段 | 含义 | 示例 |
|----|------|------|
| **程序自测点** | 程序自行验证的入口（GM 命令 / 测试脚本 / 沙盒） | "GM 命令 `gm_grant_award 1001` 给玩家发奖 1001 id 的道具" |
| **测试覆盖** | 列出该功能需覆盖的测试场景 | "覆盖：发奖成功 / 邮件未领取状态 / 重复领取拦截 / 服务端权威校验" |
| **策划验收** | 策划验收的方式（数值表比对 / 案例回放 / 抽检比例） | "策划在测试环境用 GM 命令抽检 10 个玩家背包，对照数值表" |

**缺失判定**：
- 3 段齐全 → ✅ PASS
- 缺 1 段 → 1 条 P0（SPECIAL_FLAG = `PURCHASE_STRONG`）
- 缺 2 段 → 2 条 P0（SPECIAL_FLAG = `PURCHASE_STRONG`）
- 缺 3 段 → 3 条 P0（SPECIAL_FLAG = `PURCHASE_STRONG`）

> **不阻断 S1**，但 **S1.5 必须由人工补齐**——这是策划验收的**契约**，不是 S1 的门禁。
> S1.5 阶段人工审核时，`SPECIAL_FLAG = PURCHASE_STRONG` 的 P0 **必须**全部答完，否则 S2 拒绝执行（这是 S1.5 的门禁，不在 S1）。

---

## 核心任务

对需求文档进行 **5维度评审**，给出评分和判决。

### 评分维度（每项 1-10 分）

| 维度 | 权重 | 评审要点 |
|------|------|----------|
| 完整性 | 25% | 角色定义、需求对象拆解、功能覆盖、验收标准是否齐全 |
| 清晰度 | 25% | 无模糊术语、有量化指标、术语定义一致 |
| 一致性 | 20% | 角色 / 需求对象 / 功能三层之间无矛盾 |
| 可测试性 | 20% | 每个业务故事有明确通过/失败条件（**业务故事数 / 功能点数 = 100%**，1:1 配对） |
| 可行性 | 10% | 技术约束已识别，无未定义的外部依赖 |

### 判决规则

> **🚨 强制阅读指令（MANDATORY）**：本节是 S1 阶段**评分判决**的细化定义（**非全阶段门禁**）。
> - **全阶段质量门禁 SSOT**：[`DESIGN_AND_EXECUTION_STANDARDS.mdc` §2.3](./DESIGN_AND_EXECUTION_STANDARDS.mdc#23-质量门禁quality-gate)
> - **5 维度评分权重、ACCEPTANCE 标准、判决阈值**（7.0/4.0）的**完整定义**在本节；SSOT §2.3 仅速查。
> - **执行 S1 前必须**读本节 + SSOT §2.3。

| 总分 | 判决 | 后续动作 |
|------|------|----------|
| ≥ 7.0 | **PASS** | 生成终版需求.md → 触发 S1.5 业务澄清 → 等待人工 feedback |
| 4.0 – 6.9 | **NEEDS_REVISION** | 输出修改建议，等待修订后重审 |
| < 4.0 | **REJECT** | 输出失败报告，停止流水线 |

---

## S1 完整产出清单

S1 **始终**产出以下文件（**物料门禁失败**时仅产 `fail_report_S1.md`）：

| 文件 | 触发条件 | 说明 |
|------|----------|------|
| `raw/` | 物料门禁通过 | 解析后的 md / 图片 / OCR / index |
| `review_report.md` | 物料门禁通过 | 5维度评分报告 |
| `review_report.json` | 物料门禁通过 | 结构化评分数据 |
| `role_definitions.md` | 物料门禁通过 | 角色定义与分类（主/次/边界） |
| `requirement_objects.md` | 物料门禁通过 | 需求对象拆解（角色→对象→功能→故事 + 8 模块标签） |
| `requirement_objects.json` | 物料门禁通过 | 结构化拆解（供 S2 直接消费） |
| `clarification_checklist.md` | 物料门禁通过 | 待确认问题 + **问题需求清单**（S1.5 准入物料） |
| `终版需求.md` | 判决为 PASS / NEEDS_REVISION | AI 整理后的需求文档，由 S1.5 完善 |
| `fail_report_S1.md` | 物料门禁失败 或 判决为 REJECT | 失败报告，停止流水线 |

> **重要**：`终版需求.md` 是 S1 的产出，经 S1.5 人工 feedback 阶段完善后，才作为 S2 的正式输入。
> S1 阶段不对 `终版需求.md` 设置准出门槛评分——评分仅用于判断是否进入澄清流程。

---

## 输出规范

### 评分报告（review_report.md）

```markdown
# 需求评审报告

**版本**: v1.0
**日期**: YYYY-MM-DD
**判决**: [OK] PASS | [WARN] NEEDS_REVISION | [FAIL] REJECT

---

## 评分

| 维度 | 得分 | 满分 |
|------|------|------|
| 完整性 | X.X | 10.0 |
| 清晰度 | X.X | 10.0 |
| 一致性 | X.X | 10.0 |
| 可测试性 | X.X | 10.0 |
| 可行性 | X.X | 10.0 |
| **加权总分** | **X.X / 10.0** | — |

---

## 缺失信息

- ...

## 冲突项

- ...

## 待确认问题（→ clarification_checklist）

- ...

## 优先级建议

- P0 / P1 / P2 / P3

## 总结
```

### 失败报告（fail_report_S1.md）

```markdown
# 需求评审失败报告

**阶段**: S1 需求评审
**日期**: YYYY-MM-DD
**判决**: REJECT / NEEDS_REVISION

---

## 失败原因

### 缺失材料
（列出无法评审的具体原因）

### 冲突/矛盾项
（列出无法消解的矛盾）

### 不可行的要求
（列出技术上无法实现或依赖未定义的内容）

---

## 修改建议

### 必须修改项（阻断）
- ...

### 建议修改项（可选）
- ...

---

## 后续动作

[ ] 补充材料后重新提交 S1
[ ] 联系需求提出方确认模糊项
[ ] 终止流水线（REJECT 专用）

**重新提交时请附上更新后的需求文档。**
```

---

## 与其他阶段的衔接

| 上游 | 下游 |
|------|------|
| — | S1.5 业务澄清（S1 判决 PASS/NEEDS_REVISION 时） |

S1 通过后，**5 份物料**自动作为 S1.5 的输入：

| 物料 | S1.5 验收要求 |
|------|--------------|
| `review_report.md` | 5 维度评分 + 判决（PASS / NEEDS_REVISION） |
| `role_definitions.md` | 角色按"主/次/边界"分类 + 典型场景（无数量下限） |
| `requirement_objects.md` + `.json` | 功能 × 业务故事 1:1 配对（100% 覆盖率） + 8 模块标签 |
| `终版需求.md`（草稿） | S1 AI 整理后的需求文档，S1.5 完善 |
| `clarification_checklist.md` | **必须**含 `## 问题需求（→ S1.5 准入物料）` 节，P0/P1/P2 分级 + `SPECIAL_FLAG` 列 |

S1.5 完成人工 feedback 并生成 `exit_permission.json` 后，S2 方可执行。

> **S1.5 不可被缺省**——S2 执行前**必须**有 `exit_permission.json` 且 `can_proceed_to_s2 == true`，否则 S2 拒绝执行。
> 详见 `STAGE_S1.5 Clarification.mdc`（S1.5 业务澄清与准出规范）。

### 反向契约（S1 必须知道的事）

S1 评审时**必须**意识到：

1. **S1 产出 = S1.5 准入物料**——5 份物料缺 1 份即被 S1.5 退回
2. **S1.5 不可被缺省**——任何"跳过 S1.5 直接进 S2"的尝试**必须 fail**（S2 触发 `fail_report_S2.md`）
3. **功能 × 业务故事 1:1 配对 = 100% 覆盖率**——S1 偷工减料（5 个功能只配 1 个故事）→ S2 拆解覆盖率 < 100% → S7 风险点全量覆盖 FAIL
4. **强付费项（PURCHASE_STRONG）的 P0** 缺失 = S1.5 强门禁——`SPECIAL_FLAG = PURCHASE_STRONG` 的 P0 **必须**全部答完才能 `can_proceed_to_s2 = true`
5. **轻量跑通 ≠ 跳过 S1.5**——S1.5 仍要执行（产出 `exit_permission.json`），但 `quality_level = LOW`（P0 全填 + P1/P2 可不填）

---

## 代码入口

```python
# S1 的 5 维度评分和判决由 LLM 执行（通过 /aidocx-s1-review 或 conversation_skills 协作触发）
# 材料门禁使用（不产生评分、不影响 quality_level）：
from ai_workflow.requirement_reviewer_auto import check_material_gate

gate = check_material_gate(requirement_text)
# gate["gate_passed"]: True = 进入 S1 LLM 评审 / False = 物料门禁失败
```

---

## 关联文件

- Prompt 模板：`ai_workflow/prompts/requirement_review.md`
- 自动化引擎：`ai_workflow/requirement_reviewer_auto.py`
- 输出目录：`workflow_assets/<req_name>/「S1 需求评审」/<version>/`
