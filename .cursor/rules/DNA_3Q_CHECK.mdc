---
description: DNA 3 问自检硬约束（AGENTS.md 铁律的执行版）— 每次 tool call 前必做 Q1/Q2/Q3 自检
alwaysApply: true
---

# DNA 3 问自检（硬约束）

> **本文件是 `AGENTS.md` 铁律的"执行版"**。
> AGENTS.md 是 DNA 总纲（≤60 行，本文件不重复）；
> 本文件是**操作约束**——3 问自检的可执行规则。
> 配套机制：`.cursor/hooks/dna_violation_check.py` 软记录 + 临界点 block。

---

## 1. 三问速查（每次 tool call 前必答）

| 问 | 检查点 | 答不出 → |
|---|---|---|
| **Q1 人本可审查** | 人能在 5 分钟内直接看懂这个改动吗？ | 拆分 / 加注释 / 写示例 |
| **Q2 必然好论证** | 为什么这个结构 / 改动能避免"再发生"？ | 补"为什么"段（不只是"是什么"） |
| **Q3 约束 vs 知识** | 改的是 `.mdc` / `SKILL.md` / `hooks.json`（约束）还是 `PLAN.md` / `knowledge/`（知识）？ | 约束 → **必须先问用户**；知识 → 可直接动 |

---

## 2. 违规标记协议

当 3 问任意一问答不出时，**响应顶部必须包含**：

```markdown
⚠️ DNA 自检未通过：<具体哪一问> → <具体原因>
本轮改动：<文件 1>, <文件 2>, ...
影响范围：<人 / Agent / 流水线>
请求：<先列影响 / 先问 / 改方案>
```

**没标 marker = 视为隐藏违规**——hook 会在 stderr 警告"⚠️ 标记缺失"。

---

## 3. 约束 vs 知识对照表

| 类别 | 改之前 | 改之中 | 改之后 |
|---|---|---|---|
| **约束**（`.mdc` / `SKILL.md` / `hooks.json` / `AGENTS.md`） | **必须先问用户** | 写 3 问答 + marker | 自验证"Agent 行为是否改变" |
| **知识**（`PLAN.md` / `knowledge/` / `*.md`） | 写 3 问答即可 | 直接动 | 写"为什么"段 |
| **代码**（`*.py` / `*.sh`） | 写 3 问答即可 | 直接动 | 跑 install.sh / 测试 |

---

## 4. 典型违规场景

### ❌ 场景 1：先动手再问（v2 实际发生）

```
1. Agent 看到 install.sh step 3 失败
2. Agent 直接改代码：warn 跳过
3. 改完才问用户"hermes_skills 怎么办"

→ 正确顺序：先查所有引用 → 列 5 个文件 → 问用户 → 再动手
```

### ❌ 场景 2：改动无影响范围（v2 实际发生）

```
1. Agent 删 SKILL_STANDARDS.md 6 处 hermes 引用
2. Agent 未列"对 Agent 行为的影响"
3. Agent 未验证 validate_skills.py 是否还检查 hermes_compat

→ 正确顺序：先 grep 验证依赖 → 列影响 → 改 → 验证
```

### ❌ 场景 3：未做 3 问自检（v2 实际发生）

```
1. Agent 连续 8 个文件改动无停顿
2. Q1/Q2/Q3 没答就动手
3. 直到用户骂才意识到

→ 正确流程：每个 tool call 前先想 5 秒 3 问
```

---

## 5. 怎么"答"3 问（实操）

不必长答——心里默念即可：

- **Q1**：能/不能（不能 → 拆）
- **Q2**：一句话说"为什么这个能避免再发生"
- **Q3**：约束/知识/代码（三选一）

**答不出 Q2** = 这个改动是"想当然" = 停下来。
**答出"约束"** = 改约束 = **必问**。

---

## 6. 配套机制

| 层级 | 文件 | 行为 |
|---|---|---|
| L1 知识 | `AGENTS.md` L17-20 | DNA 总纲（3 问入口） |
| L2 约束 | 本文件（DNA_3Q_CHECK.mdc） | alwaysApply 必读 |
| L3 机制 | `.cursor/hooks/dna_violation_check.py` | 软记录 + 临界点 block |

**L3 hook 检测**：
- 响应里有 `⚠️ DNA 自检未通过` marker → stderr `[DNA-WARN]` 输出（不阻断）
- 连续 3 次含 marker → 第 4 次响应阻断（exit 1）
- 状态文件：`workflow_assets/feedback_logs/dna_violations.jsonl`

---

## 7. 决策密度标准（v3.1 新增）

> 背景：用户反馈"小狗先执行有偏差，人工审查成本高"——
> 根因 = 一次会话决策点过多，单次改动 8 文件 = ≥ 20 决策点。
> 详细方案：`.cursor/design_iter/plans/v3/PLAN.md` §6.5

### 7.1 核心阈值

| 指标 | 阈值 | 超过 → |
|---|---|---|
| 单次响应文件改动数 | ≤ 3 个 | 列"决策表" + 必先 ask |
| 单次响应 tool call 数 | ≤ 10 个 | 停下来列"未完成项" |
| 单次响应里"未问先动"的工具调用 | 0 个 | **违规**（直接认错） |
| 决策点密度 | 改动数 × 平均决策/文件 ≤ 5 | 列决策表 |

### 7.2 Agent 行为承诺（A2 行为）

- **用户提任何事** → 先反问 1 个 AskQuestion（澄清目标 / 列决策点）
- **用户答完** → 列决策表 → 用户点头 → 动手
- **单次改动 ≤ 3 文件**（除非用户明确说"批量改"）
- **改完** → 报"diff 摘要 + 影响范围"（不超 5 行）

### 7.3 决策表模板

```
[改动 1] <文件>: <1 行说明>
   影响范围: <人/Agent/流水线>
   替代方案: <B 选项 1 行>

[改动 2] <文件>: <1 行说明>
   ...
```

### 7.4 与 Q1-Q3 自检的关系

| 自检 | 决策密度的关系 |
|---|---|
| Q1 人本可审查 | 决策表 = 让人能"审 1 段不审 8 文件" |
| Q2 必然好论证 | 决策表第 1 列 = "是什么"；影响范围/替代方案 = "为什么" |
| Q3 约束 vs 知识 | 改约束 → 决策表必先 ask；改知识/代码 → 决策表内答即可 |

### 7.5 不增 hook 的原因（v3.0 §0.4 教训）

L3 hook 检测不到"误读"型违规（详见 v3/PLAN.md §0.4）。
**hook 不是答案，流程才是**——本次走 L1+L2 层（DNA 总纲 + 本文件），不写新 hook。

---

## 8. 关键引用

| 内容 | 路径 |
|---|---|
| DNA 总纲 | `AGENTS.md` |
| 3 问自检物化方案 | `.cursor/design_iter/plans/v3/PLAN.md` |
| 决策密度标准方案 | `.cursor/design_iter/plans/v3/PLAN.md` §6.5 |
| v3 决策记录 | `.cursor/design_iter/plans/v3/decisions.json` |
| v3 开放问题 | `.cursor/design_iter/plans/v3/open_questions.md` |
| L3 hook 源码 | `.cursor/hooks/dna_violation_check.py` |
| S8 反馈日志目录 | `workflow_assets/feedback_logs/` |
