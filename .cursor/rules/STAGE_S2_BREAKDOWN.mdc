---
description: AIDocxWorkFlow Stage 2 — 需求拆解（深度拆解：Epic → Story → 需求对象 → 功能点，独立阶段）
alwaysApply: false
---

# AIDocxWorkFlow S2 — 需求拆解

> **独立阶段**：可以单独调用。上游材料（S1.5 准出许可）审查合格后开始，失败写失败报告。
>
> **本版本核心升级**：拆解深度从「Epic/Story」一层扩展到完整的**需求对象级分解**，为 S3 迭代规划和 S5/S6 测试用例设计提供充足的执行物料。

---

## 阶段入口

**触发方式**：`/aidocx-s2-breakdown` 或粘贴 S1.5 准出许可路径

**前置材料（必须全部满足）**：

| 材料 | 来源 | 审查要求 |
|------|------|----------|
| 终版需求.md | `workflow_assets/<req_name>/「S1 需求评审」/<version>/终版需求.md` | 存在且内容完整 |
| S1.5 准出许可 | `workflow_assets/<req_name>/「S1 需求评审」/<version>/exit_permission.json` | 存在且 `can_proceed_to_s2 == true` |
| clarification_checklist.md | `workflow_assets/<req_name>/「S1 需求评审」/<version>/clarification_checklist.md` | 状态为「已处理」 |

**材料缺失或 `can_proceed_to_s2 == false` 时**：生成失败报告，停止 S2。

---

## 核心范式：五层业务语义分层

```
Release（版本基线，归集容器）
 └─ Epic（大系统 / 大型业务史诗）
     └─ Story（用户故事）
         └─ Object（需求对象）
             └─ FP（功能点）
```

> **设计原则**：
> - **粒度不由硬性数值决定**——每层按"业务自然划分"给出指导档位（重型/中型/轻型），禁止凑数/拆细
> - **业务语义驱动**——引入游戏行业专属字段（角色句式、模块归属、正向/异常流程、数据变更、验证手段）
> - **跨模块自动拆 OBJ**——一条业务涉及多模块时按模块拆多 OBJ（不标[待确认]）
> - **统一边界判定流程**——8 大模块匹配优先级 + 兜底评审归档

---

## 核心任务

将需求拆解为五层结构 **Release → Epic → Story → Object → FP**：

```
Release（版本基线，元数据归集容器，不进 ID 段）
 └─ Epic
     └─ Story
         └─ Object
             └─ FP
```

> **设计原则**：拆解粒度**不由硬性数值范围决定**，而由"业务自然划分"决定。
> 硬性范围（如"Epic 1-4 周""Story 1-3 人天""每个 Story 1-5 个""需求对象 3-8 个"）是 AI 偷懒的沃土——AI 会按范围下限交差，且 `summary.feature_point_count` 与实际 FPs 没有任何机制比对（实测：现有产物 summary=68 / 实际=0）。修正方案是**用业务自然划分 + 可机检的派生规则 + 物量守恒**代替硬性范围。

```
Epic
 └─ Story × N
     └─ 需求对象 × M（由"独立可测业务实体"决定，见 §第三层）
         ├─ 对象状态拆解（正常态 / 异常态 / 边界态）
         ├─ 交互形态（界面操作 / API 调用 / 系统事件）
         ├─ 业务逻辑（正常路径 / 判定分支 / 计算规则）
         ├─ 异常业务边界（超限 / 空值 / 冲突 / 幂等）
         ├─ 异常交互（超时 / 重试 / 降级 / 兜底）
         ├─ 关联模块推理（CONFIG / UI / BIZ / AUX / LINK / SPECIAL / LOG）
         ├─ 日志与埋点（操作日志 / 业务事件 / 监控埋点）
         └─ 关联协议（内部协议 / 外部 API / 数据同步协议）
         └─ 功能点 × K（由「可独立断言的行为单元」决定，见 §第四层）
             ├─ 分类维度（功能分类标签）
             ├─ 分类边界（边界值 / 等价类 / 异常触发条件）
             └─ 测试类型（正向 / 逆向 / 边界 / 异常）
```

> **设计原则**：拆解粒度**不由硬性数值范围决定**，而由"业务实体/页面/流程节点是否独立可测"决定。
> 硬性范围（如"每个 Story 1-5 个""需求对象 3-8 个"）是 AI 偷懒的沃土——AI 会按范围下限交差，且 `summary.feature_point_count` 与实际 FPs 没有任何机制比对（实测：现有产物 summary=68 / 实际=0）。修正方案是**用可机检的派生规则 + 物量守恒**代替硬性范围。

---

### 第一层：Release（版本基线，归集容器）

**定义**：一次完整发布包 / 热更包，作为所有需求的顶层归集容器。

**粒度（指导档位，禁止硬性卡死）**：
- 完整大版本：4-12 周完整客户端包
- 热更增量包：0.5-2 周配置 / 逻辑更新包
- 临时运营补丁：0.5-3 天紧急修复包

**实现策略（ID alias）**：
- v1.x 存量产物**不强制回填 REL 段**（避免破坏下游 S3/S4/S5/S6 链接）
- v2.0 起新拆解：在 backlog.json 顶层加 `release` 字段（**元数据级**，**不进 Epic/Story ID 段**）：

```json
{
  "release": {
    "id": "REL-2606-MAIN-01",
    "type": "MAIN" | "HOTFIX" | "PATCH",
    "cycle": "大版本" | "热更增量" | "运营补丁",
    "window": "2026-06 上线窗口",
    "owner": "主策/主测 姓名"
  }
}
```

**核心作用**：所有 Epic 必须归属唯一 Release，区分迭代基线，避免跨版本需求混杂。

### 第二层：Epic（大系统 / 大型业务史诗）

**粒度（按业务自然划分的三档，禁止硬性卡死工期）**：

| 档位 | 典型场景 | 周期指导 |
|------|----------|----------|
| 重型 Epic | 完整新系统（养成、抽卡、跨服竞技场）、老系统大规模重构 | 2-8 周 |
| 中型 Epic | 中型限时活动、商城重构、配置解析升级 | 1-3 周 |
| 轻型 Epic | 基础提示优化、日志埋点迭代、GM 工具迭代 | 0.25-1 周 |

- 格式：`{Module}-{Number}`，如 `CONFIG-001`
- v2.0 起支持 alias `REL-2606-MAIN-01-CONFIG-001` 作为冗余标记，**不影响主 ID**

**必填元数据**：
- `module`（8 大模块归属）
- `scale`（`HEAVY` / `MEDIUM` / `LIGHT`）
- `domain`（业务域描述）
- `related_systems`（关联系统列表）
- `release_window`（版本上线窗口）
- `owner`（负责人）
- `risk_level`（P0/P1/P2）

**拆分判定（满足任一条独立成 Epic）**：
1. 跨 3 个及以上模块
2. 交付周期 ≥ 1 周
3. 独立可灰度开关 + 独立配置包
4. 完整闭环业务（产出 - 消耗 - 结算 - 奖励）

### 第三层：Story（用户故事，可交付最小业务价值）

**粒度（按业务自然划分）**：

| 档位 | 典型场景 | 周期指导 |
|------|----------|----------|
| 标准 Story | 90% 游戏需求 | 0.5-3 人天 |
| 微型 Story | 单文案修改、单点埋点、红点修复 | 0.25 人天 |
| 重型 Story | 完整抽卡流程、充值全链路 | 3-7 人天 |

- 格式：`{EpicID}-{StoryNumber}`，如 `CONFIG-001-001`
- v2.0 起 alias：`{ReleaseID}-{EpicID}-S{StoryNumber}`，如 `REL-2606-MAIN-01-CONFIG-001-S001`

**游戏行业增强必填字段**：

| 字段 | 说明 | 必须 |
|------|------|------|
| `id` | 唯一编码 | ✅ |
| `title` | **标准化用户故事句式**：`作为[角色]，我希望[功能]，以便[价值]` | ✅ |
| `user_role` | 角色（玩家 / 运营 / GM / 策划） | ✅ |
| `precondition` | 前置依赖（配置 / 系统 / 版本 / 权限） | ✅ |
| `input_data` | 触发输入（玩家操作、配置参数、后台指令、网络场景） | ✅ |
| `acceptance_criteria` | ≥ 2 条，**区分三类**（开发验收 / 测试验收 / 策划数值验收） | ✅ |
| `scope_module` | 涉及的 8 大模块列表（多模块则后续 OBJ 自动拆分） | ✅ |
| `risk` | 风险等级 + 依赖项 | ✅ |
| `expected_output` | 业务最终产出（界面展示 / 数据变更 / 日志 / 奖励） | ✅ |
| `offline_verify` | 是否支持离线配置验证（`true` / `false`） | ✅ |
| `source` | 来源：`original` / `clarification` / `fallback` | ✅ |

**合法 vs 不合法**：
- ✅ 合法 Story：作为玩家，我希望完成每日任务领取积分奖励，以便兑换商城道具
- ❌ 不合法：每日任务红点显示（仅局部 UI，属于下层 Object）

### 第四层：Object（Story 内拆分单元）

**定义**：Story 内部**独立业务载体 / 独立场景域**，载体之间无强耦合、可单独验证、分属不同模块 / 不同交付对象。常见载体分类：配置表、页面弹窗、业务服务流程、第三方渠道交互、风控异常场景、埋点采集逻辑、缓存 / 热更逻辑、GM 操作能力。

**ID 格式**：`{StoryID}-OBJ-{两位自增序号}`，如 `CONFIG-001-001-OBJ-01`（v2.0 起 alias：`REL-2606-MAIN-01-CONFIG-001-S001-OBJ-01`）

**Object 数量**：**不设任何区间约束**，数量由下方"拆分判定规则 + 落地约束"自然产出。

---

#### 拆分判定规则（唯一依据，不靠主观"简单/复杂"）

满足**任意一条**，必须拆分独立新 Object：

1. **归属顶层 8 大模块不同**（CONFIG/UI/BIZ/AUX/LINK/SPECIAL/LOG/HINT 之一不同）
2. **业务载体完全隔离**（例：任务配置表、任务领取服务逻辑、任务奖励弹窗是 3 类完全独立载体）
3. **验证链路完全独立**（配置可用 Excel 工具校验、业务逻辑需要客户端操作、埋点需要日志平台查看——三者必须分开）
4. **交付责任人不同**（配置策划 / 前端 / 服务端 / 测试埋点 / 风控，分人开发验收）
5. **正常流程与异常 / 风控场景解耦**（正向领取奖励归 BIZ，弱网领取失败、作弊拦截归 SPECIAL，拆为两个 Object）

> **关键转变**：数量是"按规则自然产出的结果"，不是"拆解前必须遵守的硬性规则"。"先定区间再拆"被彻底删除。

---

#### 落地约束（解决拆分随意、过度拆分两种极端）

**约束 1：禁止过度拆分（无意义碎片化）**
- 同一个模块、同一个载体、同一套验证链路 → **不允许拆多个 Object**
- ❌ 错误示例：同一弹窗内红点、飘字、按钮提示同属 HINT 载体，被拆 3 个 OBJ
- ✅ 正确做法：合并为 1 个 HINT Object，内部用多条 FP 区分不同提示能力

**约束 2：禁止过度合并（多载体揉进同一个 Object）**
- 一个 Object 只能对应**一类业务载体**
- ❌ 错误示例：把"配置表 + 服务业务 + 第三方渠道"塞进一个 OBJ
- ✅ 正确做法：按 5 条判定规则强制拆分

**约束 3：边界模糊兜底（不强行凑数 / 不强行合并）**
无法判定是否拆分新 Object 时，**不强行凑数量**，执行两步评审：

1. 标注 `[待确认-Object拆分边界]`，列明两种拆分方案、差异影响
2. 同步主策划 / AI 拆解规则管理员评审，**归档判定结论**，更新团队标准案例库

---

#### 实战示例：玩家充值购买礼包完整全链路（自然产出 13 OBJ，不卡 12 上限）

| OBJ ID | 归属模块 | obj_name | 业务载体 |
|--------|----------|----------|----------|
| OBJ-01 | CONFIG | 礼包档位配置表校验 | 配置表 |
| OBJ-02 | UI | 充值商城礼包页面布局 | 页面 |
| OBJ-03 | BIZ | 下单 / 订单创建 / 库存扣减 | 服务流程 |
| OBJ-04 | LINK | 渠道 SDK 支付拉起 / 订单回调 | 第三方渠道 |
| OBJ-05 | SPECIAL | 重复下单 / 篡改金额 / 作弊拦截 | 风控 |
| OBJ-06 | BIZ | 支付成功后道具 / 货币发放事务 | 服务流程 |
| OBJ-07 | HINT | 充值成功弹窗 / 资源飘字 | 提示 |
| OBJ-08 | LOG | 付费资产流水 / 充值行为埋点 | 埋点 |
| OBJ-09 | AUX | 本地支付凭证缓存 / 断线续单 | 底层能力 |
| OBJ-10 | SPECIAL | 退款回滚 / 异常订单兜底 | 风控 |
| OBJ-11 | LINK | 渠道付费数据对账同步 | 第三方渠道 |
| OBJ-12 | HINT | 付费失败 Toast | 提示 |
| OBJ-13 | BIZ | 累充进度同步 / 累充奖励自动发放 | 服务流程 |

旧规则"复杂最多 12 个"会强制合并其中 2 个独立载体，导致单个 OBJ 混杂多模块逻辑，FP 拆解和测试用例生成出现交叉混乱。

---

#### 游戏行业 9 标准必填字段

| 字段 | 含义 | 说明 |
|------|------|------|
| `obj_name` | 对象名称 | |
| `belong_module` | 归属 8 大核心模块 | **单选**；多模块业务按规则 1 拆多 OBJ |
| `scene` | 业务场景描述 | |
| `input` | 触发输入条件 | 触发该对象的输入 / 前置 |
| `normal_flow` | 正向标准流程 | 输入→处理→输出 |
| `exception_flow` | 全分支异常流程 | 异常输入→处理→输出（兜底 / 重试 / 降级 / 风控） |
| `data_change` | 数据变更范围 | **5 选一**：`配置` / `内存` / `DB` / `缓存` / `第三方` |
| `output_display` | 对外表现 | 5 选多：`UI 弹窗` / `飘字` / `日志` / `推送` / `后台数据` |
| `verify_method` | 验证手段 | 5 选多：`配置工具` / `客户端操作` / `GM 指令` / `抓包` / `日志平台` |

> **字段迁移说明**：
> - 原 `object_states` → 拆分为 `normal_flow` 的状态分支 + `exception_flow` 的异常分支
> - 原 `interaction_forms` → 合并到 `normal_flow` / `exception_flow`
> - 原 `business_logic` → 改名为 `normal_flow`
> - 原 `exception_boundaries` → 合并到 `exception_flow`
> - 原 `exception_interactions` → 合并到 `exception_flow`
> - 原 `module_inference` → 简化为 `belong_module`（单选）
> - 原 `logs_and_tracking` → 拆分为 `data_change`（数据）+ `output_display`（日志部分）
> - 原 `related_protocols` → 合并到 `normal_flow` / `exception_flow`

### 第五层：FP（功能点，最小不可拆分执行单元）

**粒度（按业务自然拆分，无强制 3-8 限制）**：
- 极简 OBJ：1-2 个 FP（单文案修改、单点红点）
- 复杂 OBJ：8-15 个 FP（跨表配置校验、全链路支付流程）

- 格式：`{ObjectID}-FP-{N}`，如 `CONFIG-001-001-OBJ-1-FP-1`

**游戏行业 3 标准字段**：

| 字段 | 含义 | 说明 |
|------|------|------|
| `fp_desc` | 功能描述 | 一句话描述该 FP 做什么 |
| `check_type` | 校验类型 | `配置` / `界面` / `业务` / `日志` / `安全` 五选一 |
| `verify_standard` | 单一验收标准 | 一条可断言的标准（出现"且/同时/另外"必须拆分） |

> **粒度控制硬规则**：
> - 一个 FP 只描述**单一动作 / 单一规则**
> - 出现"且、同时、另外" → **必须拆分新 FP**
> - 例：❌ "下拉展示道具，道具不足置灰按钮" → ✅ 拆为 FP1（渲染）+ FP2（置灰）+ FP3（Toast）

**派生链规则**（与原范式保持一致，可机检）：
- 每个 FP 必须能溯源到 Object 的某个字段（normal_flow / exception_flow / data_change / output_display / verify_method）
- 派生链写入 `requirement_objects.md` 的「FP 派生链」段
- 字段级覆盖：5 个核心字段每个至少派生 ≥ 1 个 FP；不足则标【待补充】+ 原因
- `summary.feature_point_count` 必须 == `Σ FPs`，否则 **S2 失败**

---

## 统一边界判定流程（替代单纯 [待确认] 标注）

### 第一步：模块快速匹配（8 大模块归属判定，按优先级匹配）



### 第二步：跨模块场景处理(一条 Story/OBJ 多模块 → 拆多 OBJ)

一条业务同时涉及多模块时，**不标[待确认]，直接按模块拆多个独立 Object**：

**示例**——"领取任务奖励"Story 拆分为 4 个 OBJ：
- `OBJ01 (CONFIG)`：任务奖励配置表字段校验
- `OBJ02 (BIZ)`：服务端扣发奖励、更新任务状态落地 DB
- `OBJ03 (HINT)`：弹出奖励汇总弹窗 + 资源飘字
- `OBJ04 (LOG)`：产出资产流水埋点记录

### 第三步：无法匹配任一模块（兜底流程）

1. 填写【边界判定表】：列出 2 个候选模块、冲突场景、不确定原因
2. 标注 `[待确认-模块归属]`，绑定对应主策划 / 主测负责人
3. **24 小时内**完成评审，评审后更新模块归属并拆分 Object，**禁止长期搁置**
4. 评审记录归档到 `release` 元数据中（v2.0 起），作为后续拆解参考标准

### 第四步：Object 拆分边界模糊（兜底流程，补充 §第四层约束 3）

无法判定是否拆分新 Object 时，**不强行凑数量**，执行两步评审：

1. 标注 `[待确认-Object拆分边界]`，列明两种拆分方案、差异影响
2. 同步主策划 / AI 拆解规则管理员评审，**归档判定结论**，更新团队标准案例库

> 与"模块归属待确认"的区别：模块归属决定一个 OBJ 归哪个模块；拆分边界决定一条业务链是否要拆成多个 OBJ。两类待确认都要走评审，但走不同评审人。

---

## S1.5 准出许可的读取与使用

S2 执行前必须读取 `exit_permission.json`，根据其中字段调整拆解策略：

### 1. fallback_rules — 保底规则消费

`fallback_rules` 数组中的每条规则代表输入中存在的信息缺失，由 S2 在拆解时补充：

| 字段 | 含义 |
|------|------|
| `trigger` | 触发条件（标识哪个字段/规则存在缺失） |
| `rule` | S2 应补充的保底规则内容 |
| `source` | `clarification`（来自人工澄清）/ `derivation`（AI推导）/ `default`（默认值） |

**消费方式**：
- 拆解时若遇到 `trigger` 所指的缺失字段，按 `rule` 内容补充
- 保底规则生成的需求对象，需在字段中注明「来源：fallback」
- 拆解完成后，统计本次应用的 fallback_rules 条数

### 2. quality_level — 拆解深度调整

| quality_level | 拆解策略 |
|---------------|----------|
| HIGH | 标准拆解：Release → Epic → Story → Object → FP 五层，按 8 模块前缀分配 |
| MEDIUM | 标准拆解 + 优先处理 `s2_guidance.open_questions` 中的未决项，为其分配对应 Story |
| LOW | 标准拆解 + 应用所有 `fallback_rules` + 重点标注遗留项，在 backlog 中为每个遗留项生成 `SPECIAL-XXX` Story 承接 |

### 3. s2_guidance — 拆解引导

| 字段 | 用途 |
|------|------|
| `priority_epics` | 建议优先拆解的 Epic，排在 backlog 前面 |
| `risk_points` | 高风险区域，拆解时增加对应 Story 的边界测试覆盖 |
| `open_questions` | 未决问题，在 backlog 中为每个问题生成 TODO Story 并标注「待确认」 |

---

## 8 模块系统

> ⚠️ 模块定义见 [`.cursor/MODULES.md`](../MODULES.md)（项目级唯一真相源）。
> 本文件不重写模块表。如模块定义调整，只改 MODULES.md。

本阶段涉及的模块：`CONFIG` / `UI` / `BIZ` / `AUX` / `LINK` / `SPECIAL` / `LOG` / `HINT`
（`HINT` 仅作为 `scenario_test_points[].module` 字段的取值，不作 Epic ID 前缀）
完整定义见 [`.cursor/MODULES.md`](../MODULES.md)。

---

## 输入审查（门禁前置条件）

| 检查项 | 要求 | 缺失时 |
|--------|------|--------|
| exit_permission.json 存在 | 必须存在 | 生成失败报告 |
| can_proceed_to_s2 == true | P0 反馈已填写 | 生成失败报告 |
| 终版需求.md 存在 | 内容完整 | 生成失败报告 |
| Epic 数量 | ≥ 1 | 生成失败报告 |
| 每个 Story 有 ≥ 2 条 AC | 必须满足（区分三类验收） | 补充提示 |
| 每个 Object 有完整的 9 字段（新范式） | obj_name / belong_module / scene / input / normal_flow / exception_flow / data_change / output_display / verify_method | 标注缺失项 |
| Object 字段命名符合新范式 | 禁止使用旧字段（`object_states` / `interaction_forms` / `business_logic` 等） | 字段重命名 |
| `data_change` 5 选一 | `配置` / `内存` / `DB` / `缓存` / `第三方` 之一 | 字段纠正 |
| `output_display` 5 选多 | `UI 弹窗` / `飘字` / `日志` / `推送` / `后台数据` | 字段纠正 |
| `verify_method` 5 选多 | `配置工具` / `客户端操作` / `GM 指令` / `抓包` / `日志平台` | 字段纠正 |
| 模块归属单选 | `belong_module` 单选；多模块则按模块拆多 OBJ | 强制拆分 |
| Object 拆分判定 5 条客观规则 | 任意一条命中必须拆（不靠主观"简单/复杂"） | 补充拆分 |
| 禁止过度拆分 | 同一模块 + 同一载体 + 同一验证链路 → 1 个 OBJ | 合并 |
| 禁止过度合并 | 多类业务载体揉进同一 OBJ | 强制拆分 |
| Object 拆分边界模糊 | 标注 `[待确认-Object拆分边界]` + 评审归档 | 走评审流程 |
| 模块归属模糊 | 标注 `[待确认-模块归属]` + 24h 评审 | 走评审流程 |
| 模块前缀有效 | 来自 8 模块系统 | 纠正分配 |
| 无循环依赖 | Story 依赖关系无环 | 标记风险 |
| 物量守恒 | `summary.feature_point_count` == `Σ 各 Object FPs` | S2 失败 |
| 派生链完整 | 每个 Object 在 requirement_objects.md 中显式列出"从哪些字段派生出了哪些 FP" | 补充派生链 |
| 字段级 FP 覆盖 | Object 5 个核心字段（normal_flow / exception_flow / data_change / output_display / verify_method）每个至少派生 ≥ 1 个 FP | 字段旁标【待补充】+ 原因 |

---

## 输出规范

### 成功产出

| 文件 | 路径 |
|------|------|
| backlog.md（总览） | `workflow_assets/<req_name>/「S2 需求拆解」/<version>/backlog.md` |
| backlog.json（结构化数据） | `workflow_assets/<req_name>/「S2 需求拆解」/<version>/backlog.json` |
| **需求对象详细分解** | `workflow_assets/<req_name>/「S2 需求拆解」/<version>/requirement_objects.md` |
| **需求对象 JSON** | `workflow_assets/<req_name>/「S2 需求拆解」/<version>/requirement_objects.json` |

### backlog.md 结构

```markdown
# Requirement Backlog

**Version**: v1.0
**Date**: YYYY-MM-DD
**Source**: S1.5 exit_permission (quality_level: HIGH | MEDIUM | LOW)
**Fallback rules applied**: N / total

## Release

| 字段 | 值 |
|------|----|
| Release ID | `REL-2606-MAIN-01` |
| Type | MAIN / HOTFIX / PATCH |
| Cycle | 大版本 / 热更增量 / 运营补丁 |
| Window | 2026-06 上线窗口 |
| Owner | 主策/主测 姓名 |

## Epic & Story Summary

- Epics: N
- Stories: M
- Object 总数: K
- FP 总数: L
- Fallback Storys: X (marked with [fallback])
- [待确认-模块归属] 边界项: Y (需 24h 内评审)

## Epic & Story 一览

| Epic ID | 模块 | 档位 | 名称 | Object 数 | FP 数 | 估算 |
|---------|------|------|------|-----------|-------|------|
| ... | ... | HEAVY/MEDIUM/LIGHT | ... | ... | ... | ... |

## Epics & Stories

### {EpicID}: {Title}
**Module**: {Module} | **Scale**: HEAVY | MEDIUM | LIGHT | **Estimated**: N weeks
**Domain**: {业务域} | **Owner**: {负责人} | **Risk**: P0/P1/P2

#### {StoryID}: {Title}
**标准化用户故事**: 作为[角色]，我希望[功能]，以便[价值]
**User Role**: 玩家 / 运营 / GM / 策划

**Acceptance Criteria:**
- [开发验收] AC1: ...
- [测试验收] AC2: ...
- [策划数值验收] AC3: ...

**Precondition:** ...
**Input:** ...
**Expected Output:** ...
**Scope Module**: [BIZ, UI, ...]（多模块则 Story 内部已拆多 OBJ）
**Risk**: {level: P1, depends: [支付系统]}
**Offline Verify**: true / false

**Object 数**: N | **FP 数**: M | **派生链字段覆盖**: 5/5

（标注来源）
- 来源：S1.5 fallback | clarification | original
```

### requirement_objects.md 结构（新增）

```markdown
# 需求对象详细分解

**关联 Story**: {StoryID}
**需求对象总数**: N
**功能点总数**: M

---

## {ObjectID}: {obj_name}

**所属 Story**: {StoryID}
**归属模块**: {belong_module}（单选）
**业务场景**: {scene}

### 输入
{input}

### normal_flow（正向标准流程）
{输入→处理→输出}

### exception_flow（异常分支流程）
{异常输入→处理→输出}

### data_change（数据变更范围）
{`配置` / `内存` / `DB` / `缓存` / `第三方` 五选一}

### output_display（对外表现）
{`UI 弹窗` / `飘字` / `日志` / `推送` / `后台数据` 五选多}

### verify_method（验证手段）
{`配置工具` / `客户端操作` / `GM 指令` / `抓包` / `日志平台` 五选多}

### FP 派生链（必填，自检用）

| 派生来源字段 | 派生出 FP 列表 | 覆盖？ |
|--------------|----------------|--------|
| normal_flow | FP-N1, FP-N2 | ✅ / 【待补充】+原因 |
| exception_flow | FP-N3 | ✅ / 【待补充】+原因 |
| data_change | FP-N4 | ✅ / 【待补充】+原因 |
| output_display | FP-N5 | ✅ / 【待补充】+原因 |
| verify_method | FP-N6 | ✅ / 【待补充】+原因 |

**派生链完整性**: 5/5 字段有派生 | **本 Object FP 数**: K

#### 功能点

##### {FP_ID}: {fp_desc}
- **check_type**: {配置 / 界面 / 业务 / 日志 / 安全}
- **verify_standard**: {单一可断言标准}

---
```

---

## 与其他阶段的衔接

| 上游 | 下游 |
|------|------|
| S1.5 准出许可 | S2.5 迭代规划（使用 backlog.json + requirement_objects.json） |
| S1.5 准出许可 | S3 原型导出（使用 Object 的 `normal_flow` / `exception_flow` 流程描述） |
| S1.5 准出许可 | S5 测试点生成（使用功能点 + 分类边界 + 异常场景） |

**S3 迭代规划物料**：
- `backlog.json` → Epic 工时估算、Story 顺序、依赖关系
- `requirement_objects.json` → 任务拆分、工时评估输入

**S5/S6 测试用例物料**：
- `requirement_objects.json` → Object 9 字段（尤其是 `normal_flow` / `exception_flow` / `data_change`）
- `requirement_objects.json` → FP（`fp_desc` / `check_type` / `verify_standard`）

---

## 失败报告（S2 拆解失败）

`workflow_assets/<req_name>/「S2 需求拆解」/<version>/fail_report_S2.md`

```markdown
# 需求拆解失败报告

**阶段**: S2 需求拆解
**日期**: YYYY-MM-DD
**上游准出**: can_proceed={true|false}, quality_level={HIGH|MEDIUM|LOW}

---

## 失败原因

（列出无法完成拆解的具体原因）

---

## 无法处理的 Epic / Story / 需求对象

- ...

## 分类边界待确认项

- [ ] ...
```

---

## 代码入口

```python
# 读取 S1.5 准出许可
from ai_workflow.stage_s1_input import read_exit_permission

permission = read_exit_permission(req_name="游戏道具商城系统", version="v1.0")
# permission['exit_permission']['can_proceed_to_s2'] → bool
# permission['fallback_rules'] → list
# permission['quality_level'] → 'HIGH' | 'MEDIUM' | 'LOW'

# 手动触发（对话模式）
# AI 根据 exit_permission + 终版需求.md 生成 Epic/Story/需求对象/功能点

# 自动保存（四文件）
from ai_workflow.conversation_skills import save_stage2_output
save_stage2_output(version, req_text, raw_output, parsed, req_name)
```

---

## 实战示例：玩家充值购买礼包完整全链路（自然产出 13 OBJ，不卡 12 上限）

按第四层 5 条拆分判定规则自然产出，**不预设上限**：

| OBJ ID | 归属模块 | obj_name | 业务载体 |
|--------|----------|----------|----------|
| OBJ-01 | CONFIG | 礼包档位配置表校验 | 配置表 |
| OBJ-02 | UI | 充值商城礼包页面布局 | 页面 |
| OBJ-03 | BIZ | 下单 / 订单创建 / 库存扣减 | 服务流程 |
| OBJ-04 | LINK | 渠道 SDK 支付拉起 / 订单回调 | 第三方渠道 |
| OBJ-05 | SPECIAL | 重复下单 / 篡改金额 / 作弊拦截 | 风控 |
| OBJ-06 | BIZ | 支付成功后道具 / 货币发放事务 | 服务流程 |
| OBJ-07 | HINT | 充值成功弹窗 / 资源飘字 | 提示 |
| OBJ-08 | LOG | 付费资产流水 / 充值行为埋点 | 埋点 |
| OBJ-09 | AUX | 本地支付凭证缓存 / 断线续单 | 底层能力 |
| OBJ-10 | SPECIAL | 退款回滚 / 异常订单兜底 | 风控 |
| OBJ-11 | LINK | 渠道付费数据对账同步 | 第三方渠道 |
| OBJ-12 | HINT | 付费失败 Toast | 提示 |
| OBJ-13 | BIZ | 累充进度同步 / 累充奖励自动发放 | 服务流程 |

**反例（旧规则下会被强行合并）**：
- OBJ-09 AUX 本地凭证缓存（底层能力）+ OBJ-13 BIZ 累充进度同步（业务逻辑）—— 业务载体完全不同，按规则 1（模块不同）+ 规则 2（载体不同）必须拆
- OBJ-05 SPECIAL 反作弊风控 + OBJ-10 SPECIAL 退款回滚 —— 同模块同，但业务载体不同（一个是"事前拦截"，一个是"事后回滚"），按规则 2 必须拆
- OBJ-04 LINK 渠道拉起 + OBJ-11 LINK 渠道对账 —— 同模块同，但验证链路完全不同（拉起靠客户端调试，对账靠抓包 + 后台日志），按规则 3 必须拆

**与"无规则硬卡 12 上限"对比**：
旧规则会强制要求合并上述 3 组中的至少 1 组，导致单 OBJ 内 FP 跨多模块、测试用例生成时场景错乱。

---

## 关联文件

- Prompt 模板：`ai_workflow/prompts/requirement_breakdown.md`
- 保存逻辑：`ai_workflow/conversation_skills.py` → `save_stage2_output()`
- 准出许可读取：`ai_workflow/stage_s1_input.py` → `read_exit_permission()`
- 输出目录：`workflow_assets/<req_name>/「S2 需求拆解」/<version>/`
