---
description: AIDocxWorkFlow Stage 6 — 测试用例生成（独立阶段）
alwaysApply: false
---

# AIDocxWorkFlow S6 — 测试用例生成

> **独立阶段**：可以单独调用。上游材料（S5 test_points.json）审查合格后开始，失败写失败报告。

---

## 阶段入口

**触发方式**：`/aidocx-s6-test-cases` 或粘贴 S5 test_points.json

**前置材料（必须全部满足）**：

| 材料 | 来源 | 审查要求 |
|------|------|----------|
| S5 test_points.json | `workflow_assets/<req_name>/「S5 测试点生成」/<version>/test_points.json` | 存在且含 Story 列表 |
| S2 backlog（参考） | `workflow_assets/<req_name>/「S2 需求拆解」/<version>/backlog.md` | 存在则作为优先级参考 |
| S4 business_flow.md（参考） | `workflow_assets/<req_name>/「S4 流程图导出」/<version>/business_flow.md` | 存在则用于补充 EXCEPTION/NEGATIVE 用例步骤 |

**材料缺失时**：S4 可缺（仅警告），S5 必须存在，否则生成失败报告。

---

## 字段语义定义（禁止颠倒）

> 用例设计是 AI 独立创作，不是模板填充。以下字段命名和内容要求必须严格遵守。

| 字段 | 定义 | 内容要求 |
|------|------|----------|
| **用例描述** | 用例的标题，唯一标识 | **纯 Story 名称**（S2 backlog 中 Epic/Story 的 title），禁止加后缀、禁止加括号说明、禁止任何分隔符 |
| **功能描述** | 该用例验证的具体功能点 | **自然语言**，描述"验证什么"，禁止复制 S4 节点名称，禁止带 `—` 后缀，禁止带括号说明 |
| **前置条件** | 进入用例前系统所处的初始状态 | 具体数值（如余额=10000、道具ID=xxx），禁止模糊描述 |
| **操作步骤** | 玩家或策划的具体行动 | **每步必须是具体操作**：包含 UI 元素名称（按钮名、输入框名）或具体数值；禁止任何模板语言 |
| **预期结果** | 操作后系统应产生的行为 | **纯业务结果**，禁止引用 S4 编号、禁止括号说明、禁止标注来源 |

**示例对比**：

```text
✅ 用例描述: 购买确认流程
✅ 功能描述: 余额不足时购买按钮禁用
✅ 操作步骤: 1. 玩家余额=1000游戏币，道具单价=500
            2. 尝试购买数量=3（总价=1500）
            3. 系统检测余额不足
✅ 预期结果: 1. 购买确认弹窗显示余额不足提示
            2. 【购买】按钮禁用（灰色不可点击）
            3. 无法提交订单

❌ 用例描述: 购买确认弹窗 — 正常展示道具信息          （加了 Story 外的后缀）
❌ 功能描述: 余额不足时购买按钮禁用（引用S4-R01）      （带了括号引用）
❌ 操作步骤: 执行操作：验证结果                          （模板语言）
❌ 预期结果: 返回余额不足提示（引用S4-1.3异常树）       （带了括号引用）
```

---

## 用例格式（10 列）

```json
[
  {
    "用例ID": "界面-TC-001",
    "模块": "界面",
    "用例描述": "购买确认流程",
    "功能描述": "购买确认弹窗正常展示道具信息",
    "前置条件": "玩家已登录，余额充足，道具可购买",
    "操作步骤": "1. 玩家已登录，进入道具详情页\n2. 点击【购买】按钮\n3. 系统展示购买确认弹窗",
    "预期结果": "1. 弹窗展示道具名称\n2. 弹窗展示购买数量\n3. 弹窗展示总价\n4. 弹窗展示当前余额充足提示",
    "优先级": "P0",
    "用例状态": "Draft",
    "备注": ""
  }
]
```

### 步骤写作规则

- 每步必须包含具体 UI 元素名称（按钮名、输入框名、菜单名）或具体数值（价格、数量、时间戳）
- **禁止使用通用模板**：「执行操作：」「执行：」「准备符合前置条件的测试环境」「执行测试输入中的操作」全部禁止
- 每步独立可验证（能判断该步通过/失败）
- 最大 8 步，超过则拆分为多个用例
- 涉及配置的用例，必须包含具体的配置字段名和配置值

---

## 质量门禁（生成后自检）

> AI 生成用例后，必须逐条自检以下问题。发现任意一条立即修正，再输出。

### 自检清单

生成用例后，扫描所有用例检查以下问题，如有则修正：

- [ ] **用例描述** 是否只包含纯 Story 标题（无 `—`、无括号、无分隔符）？
- [ ] **功能描述** 是否为自然语言（无 S4 节点名称、无括号说明）？
- [ ] **操作步骤** 是否无任何模板语言（无「执行操作：」「执行：」等）？
- [ ] **预期结果** 是否无任何括号引用（无 `（引用S4-xxx）`、`（引用CONFIG-xxx）`）？
- [ ] 所有字段是否无残留引用标记（全文搜索"引用"应为0）？

**扫描命令**：生成后对 JSON 文件执行以下检查
```python
import json
with open("test_cases.json") as f:
    cases = json.load(f)
for c in cases:
    for fld in ["用例描述","功能描述","前置条件","操作步骤","预期结果"]:
        if "引用" in c.get(fld,""): print(f"[{c['用例ID']}] {fld} 含引用")
        if "执行操作：" in c.get(fld,""): print(f"[{c['用例ID']}] {fld} 含模板")
        if "—" in c.get(fld,""): print(f"[{c['用例ID']}] {fld} 含—")
```

---

## 优先级判断

| 优先级 | 适用场景 | 步骤详细程度 |
|--------|----------|--------------|
| P0 | 支付、购买、VIP折扣、促销、安全、日志 | 全量详细，含边界值 |
| P1 | 普通业务逻辑、订单、邮件、权限 | 精简用例+核心测试点 |
| P2 | 界面展示、搜索、分页、加载 | 核心测试点，不需要详细步骤 |

---

## 输入审查（门禁前置条件）

| 检查项 | 要求 | 缺失时 |
|--------|------|--------|
| test_points.json 存在 | 必须 | 失败报告 |
| Story 数量 ≥ 1 | 必须 | 失败报告 |
| 每个 Story 有至少 1 个 scenario_test_point | 必须 | 失败报告 |
| 模块前缀有效 | 必须 | 自动纠正 |

---

## S4 参考规则（重要）

S4 流程图、时序图和异常决策树是 S6 **补充用例内容**的重要来源，但引用规则如下：

- **可以参考**：阅读 S4 理解业务流程、异常路径、风险场景，据此设计用例的步骤和预期结果
- **禁止照抄**：不得将 S4 的节点名称、异常树编号、风险点名直接写入用例任何字段
- **如需注明来源**：仅在「备注」字段中写 `[参考S4-章节]`（非强制），不得在其他字段中出现

```text
✅ 预期结果: 以促销价格结算，服务器时间戳精确到秒，不存在边界争议
❌ 预期结果: 以促销价格结算（引用S4-R07风险点：服务器时间戳）
```

---

## 输出规范

### 成功产出（3 个文件）

1. **Markdown**：`「S6 测试用例生成」/<version>/test_cases.md`
2. **JSON**：`「S6 测试用例生成」/<version>/test_cases.json`
3. **Excel**：`「S6 测试用例生成」/<version>/test_cases.xlsx`（3 个 Sheet）

### Excel Sheet 说明

| Sheet | 内容 |
|-------|------|
| Sheet1 | 用例详情（含执行追踪列） |
| Sheet2 | 执行追踪（按优先级批次组织） |
| Sheet3 | 分层统计（P0/P1/P2 + 模块分布） |

### 失败报告（S6 测试用例失败）

`workflow_assets/<req_name>/「S6 测试用例生成」/<version>/fail_report_S6.md`

---

## 与其他阶段的衔接

| 上游 | 下游 |
|------|------|
| S5 测试点生成 | S7 用例审查 |
| S4 流程图（参考） | S7 用例审查 |

S6 产出（test_cases.xlsx/.json/.md）作为 S7 的输入。

---

## 代码入口

```python
# 自动格式化（Python）
from ai_workflow.test_case_formatter import format_test_cases, compute_layered_stats
cases, meta = format_test_cases(tp_list, breakdown, test_points, 'test_cases')

# Excel 生成
from ai_workflow.excel.test_case_writer import write_test_cases
write_test_cases(cases, version, filename, metadata=meta)

# 保存所有产出
from ai_workflow.s6_report import generate_s6_report
generate_s6_report(cases, version, req_name, filename, duration_s=0.0,
                    s5_tp_count=total_tp, s5_output_path=tp_path, save=True)
```

---

## 关联文件

- Prompt 模板：`ai_workflow/prompts/test_case_gen.md`
- 格式化引擎：`ai_workflow/test_case_formatter.py` → `format_test_cases()`
- Excel 写入：`ai_workflow/excel/test_case_writer.py` → `write_test_cases()`
- 报告生成：`ai_workflow/s6_report.py` → `generate_s6_report()`
- 输出目录：`workflow_assets/<req_name>/「S6 测试用例生成」/<version>/`
